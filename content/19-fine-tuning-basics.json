{
  "id": "fine-tuning-basics",
  "order": 19,
  "section": "Datasets & Training",
  "title": "Fine-Tuning Basics",
  "lesson": "<p><strong>Fine-tuning</strong> means taking a model that someone else already trained and nudging its internal settings so it gets better at <em>your</em> specific job. You are not building a brain from scratch — that part is done. You are sending an already-educated graduate on a short, focused course. This lesson is the map of the whole territory: what fine-tuning actually changes, the main flavours of it, and when you should reach for it instead of a simpler tool.</p>\n<h4>The hired-chef analogy</h4>\n<p>Imagine you hire a chef who trained for years and can cook almost any cuisine. That long, expensive education is <a class=\"xref\" href=\"#training-vs-inference\">pretraining</a> — the model has already read most of the internet and learned grammar, facts, and reasoning. But your restaurant only serves one thing: precise, beautiful sushi, plated your house way. You do not re-teach the chef to cook. You give them a few hundred photos of <em>exactly</em> the plate you want, and they adjust. That adjustment is fine-tuning. The chef keeps everything they already knew and layers a new specialty on top.</p>\n<h4>What is actually changing inside</h4>\n<p>A model is a giant pile of numbers called <a class=\"xref\" href=\"#parameters\">parameters</a> — the dials, often billions of them, that decide what word comes next. Fine-tuning shows the model example after example and, each time it answers wrong, slightly tweaks those dials toward the right answer.</p>\n<p>The size of each tweak is set by the <strong>learning rate</strong> (how big a step you take when correcting; too big and the model forgets everything, too small and it barely moves). Doing this with a curated set of prompt-and-answer pairs is the most common kind, called <a class=\"xref\" href=\"#sft-datasets\">SFT (Supervised Fine-Tuning)</a>. \"Supervised\" just means you supply the correct answer each time.</p>\n<h4>Full fine-tuning vs the cheap, clever way</h4>\n<p>There are two broad routes:</p>\n<ul><li><strong>Full fine-tuning</strong> tweaks <em>every</em> dial in the model. It is powerful but brutal: a mid-size model can need tens of gigabytes of GPU memory and serious money to update billions of parameters.</li><li><strong>PEFT</strong> (Parameter-Efficient Fine-Tuning) is the smarter modern default. Instead of moving all the dials, you freeze the original model and bolt on a tiny set of new, trainable dials.</li></ul>\n<p>The most popular PEFT form is <a class=\"xref\" href=\"#lora\">LoRA</a>, and its memory-saving cousin <a class=\"xref\" href=\"#qlora\">QLoRA</a>. See the whole family under <a class=\"xref\" href=\"#peft\">PEFT</a>.</p>\n<p>Second analogy: full fine-tuning is repainting an entire house. PEFT is sticking up a few well-placed posters — far cheaper, fast to swap out, and you can peel them off without damaging the walls. Because a LoRA result is a small separate file (often just tens of megabytes), you can keep one base model and ship dozens of lightweight specialists on top of it.</p>\n<table class=\"cmp\"><thead><tr><th>Full fine-tuning</th><th>PEFT (e.g. LoRA)</th></tr></thead><tbody><tr><td>Updates every parameter</td><td>Freezes the model, trains a tiny add-on</td></tr><tr><td>Needs lots of GPU memory + cost</td><td>Runs on a single modest GPU</td></tr><tr><td>One big new model file</td><td>Small adapter file, easy to swap</td></tr><tr><td>Best for deep behaviour changes</td><td>Great for style, tone, niche tasks</td></tr></tbody></table>\n<h4>A minimal training loop</h4>\n<p>You rarely write this by hand — libraries do it — but seeing the shape demystifies it. The model reads a batch of examples, measures how wrong it was (the <strong>loss</strong>, a single number where lower is better), and nudges its dials to lower that number:</p>\n<pre><code class=\"lang-python\">for batch in dataset:           # a few examples at a time\n    outputs = model(batch)      # model guesses the answers\n    loss = outputs.loss         # how wrong it was (lower = better)\n    loss.backward()             # work out which dials to nudge\n    optimizer.step()            # nudge them by the learning rate\n    optimizer.zero_grad()       # reset for the next batch</code></pre>\n<p>Run that over your dataset a small number of times (each full pass is an <strong>epoch</strong>) and the saved result is a <a class=\"xref\" href=\"#model-checkpoints\">checkpoint</a> — a snapshot of the dials you can load and run.</p>\n<h4>When should you fine-tune at all?</h4>\n<p>This is the most important judgement call, and beginners get it backwards. Fine-tuning is excellent for teaching <em>behaviour</em>: a consistent format, a brand voice, a narrow skill like \"turn this lab report into structured JSON.\" It is a poor and leaky way to inject <em>fresh facts</em>.</p>\n<p>If you need the model to know your latest docs or prices, use <a class=\"xref\" href=\"#rag\">RAG</a> (feeding facts in at question time) instead of baking them into the dials. A good rule of thumb:</p>\n<ul><li>Try a clearer <a class=\"xref\" href=\"#prompt-engineering\">prompt</a> first — fastest and free.</li><li>Then reach for <a class=\"xref\" href=\"#rag\">RAG</a> when the model needs facts it does not have.</li><li>Only fine-tune when you need a <em>behaviour</em> that prompting cannot reliably produce.</li></ul>\n<p>Fine-tuning also has richer relatives — <a class=\"xref\" href=\"#instruction-tuning\">Instruction Tuning</a> for following commands, <a class=\"xref\" href=\"#dpo\">DPO</a> for aligning to human preferences, and <a class=\"xref\" href=\"#continued-pretraining\">Continued Pretraining</a> for soaking up a whole new domain.</p>\n<p class=\"callout warn\">⚠️ Fine-tuning can cause <em>catastrophic forgetting</em>: push too hard on a narrow task and the model gets worse at everything else. Keep the learning rate low and the runs short.</p>",
  "mentalModel": "Fine-tuning is sending an already-educated graduate on a short, focused course — you keep everything they know and add one specialty. With PEFT you do not even rewire their brain; you hand them a small cheat-sheet (the adapter) they can pick up, swap, or put down.",
  "takeaways": [
    "Fine-tuning nudges an existing model's dials — it doesn't train one from scratch.",
    "Prefer PEFT/LoRA over full fine-tuning: cheaper, faster, swappable adapters.",
    "Tune behaviour and style; use <a class=\"xref\" href=\"#rag\">RAG</a> for fresh facts, not fine-tuning."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"Pretrained model plus your examples produces a specialized model, via full fine-tuning or PEFT\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"34\" text-anchor=\"middle\">Two routes from a base model to a specialist</text><rect class=\"dg-box dg-box-accent\" x=\"40\" y=\"140\" width=\"150\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"115\" y=\"168\" text-anchor=\"middle\">Pretrained model</text><text class=\"dg-sub\" x=\"115\" y=\"188\" text-anchor=\"middle\">billions of dials</text><rect class=\"dg-box\" x=\"40\" y=\"232\" width=\"150\" height=\"58\" rx=\"12\"/><text class=\"dg-label\" x=\"115\" y=\"256\" text-anchor=\"middle\">Your examples</text><text class=\"dg-sub\" x=\"115\" y=\"276\" text-anchor=\"middle\">prompt + answer pairs</text><line class=\"dg-edge\" x1=\"190\" y1=\"173\" x2=\"290\" y2=\"130\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"190\" y1=\"261\" x2=\"290\" y2=\"130\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box dg-box-warn\" x=\"290\" y=\"96\" width=\"170\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"375\" y=\"124\" text-anchor=\"middle\">Full fine-tune</text><text class=\"dg-sub\" x=\"375\" y=\"144\" text-anchor=\"middle\">updates every dial</text><line class=\"dg-edge\" x1=\"190\" y1=\"185\" x2=\"290\" y2=\"235\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"190\" y1=\"250\" x2=\"290\" y2=\"235\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box dg-box-good\" x=\"290\" y=\"205\" width=\"170\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"375\" y=\"233\" text-anchor=\"middle\">PEFT / LoRA</text><text class=\"dg-sub\" x=\"375\" y=\"253\" text-anchor=\"middle\">freeze + tiny add-on</text><line class=\"dg-edge\" x1=\"460\" y1=\"129\" x2=\"560\" y2=\"165\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"460\" y1=\"238\" x2=\"560\" y2=\"180\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box dg-box-2\" x=\"560\" y=\"140\" width=\"130\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"625\" y=\"168\" text-anchor=\"middle\">Specialist</text><text class=\"dg-sub\" x=\"625\" y=\"188\" text-anchor=\"middle\">your task, your voice</text><text class=\"dg-note\" x=\"360\" y=\"322\" text-anchor=\"middle\">Same base model + your data; full fine-tune is costly, PEFT is the cheap swappable default.</text></svg>",
    "caption": "Your base model plus a few prompt-answer pairs becomes a specialist — either by full fine-tuning (every dial) or the cheaper PEFT/LoRA route (a tiny add-on)."
  },
  "mistakes": [
    "<strong>Fine-tuning to add facts.</strong> The model stores new facts in a leaky, unreliable way and will still hallucinate. For knowledge that changes or must be exact, use <a class=\"xref\" href=\"#rag\">RAG</a>; fine-tune for behaviour and format instead.",
    "<strong>Reaching for it too early.</strong> Many problems vanish with a sharper prompt or a few in-context examples. Try <a class=\"xref\" href=\"#prompt-engineering\">prompt engineering</a> and RAG first — they are faster, cheaper, and easier to change.",
    "<strong>Learning rate too high.</strong> Big correction steps make the model overwrite its general skills — \"catastrophic forgetting.\" Start small (a tiny fraction like 1e-4 or 2e-4 for LoRA) and watch that it improves on your task without breaking on others.",
    "<strong>Training too long.</strong> Run for too many epochs and the model memorises your exact examples (<em>overfitting</em>) instead of learning the pattern. It then nails the training set and flops on anything new. Stop when validation loss stops dropping.",
    "<strong>Choosing full fine-tuning by default.</strong> It burns memory and money for little gain on most tasks. <a class=\"xref\" href=\"#lora\">LoRA</a> / <a class=\"xref\" href=\"#qlora\">QLoRA</a> usually match it for a fraction of the cost and give you swappable adapter files.",
    "<strong>No held-out test set.</strong> If you judge only on data the model trained on, great scores mean nothing — it may have just memorised. Always keep a separate slice it never sees during training to measure real progress."
  ],
  "exercise": "<p>Plan (don't run) a fine-tune on paper. Pick one narrow behaviour you want — say, \"rewrite any customer email into a polite two-sentence reply.\" Write down: (1) the exact behaviour in one sentence, (2) why a plain prompt or <a class=\"xref\" href=\"#rag\">RAG</a> can't already do it well, (3) whether you'd use full fine-tuning or <a class=\"xref\" href=\"#lora\">LoRA</a> and why, and (4) how you'd split your examples into a training set and a held-out test set. Then hand-write 8 prompt-and-ideal-answer pairs in the SFT message format, keeping the answer style identical across all 8.</p><p><strong>Stretch goal:</strong> Decide your learning rate and number of epochs <em>before</em> training, and write one sentence predicting what would go wrong if you doubled each. (Hint: one risk is forgetting, the other is overfitting.)</p>",
  "solution": "<p><strong>Hint:</strong> A solid plan looks like — Behaviour: \"polite two-sentence email reply.\" Why not prompt/RAG: prompting gives inconsistent length and tone across edge cases; RAG adds no facts here, so it's irrelevant. Method: LoRA, because the task is pure style/format, needs little memory, and the adapter is easy to swap. Split: e.g. 80% train, 20% held-out test, with no overlap. For the stretch goal: doubling the <em>learning rate</em> risks catastrophic forgetting — the model gets erratic and loses general skill; doubling the <em>epochs</em> risks overfitting — it memorises your 8 examples and reproduces them word-for-word instead of generalising. Good starting points for LoRA are a learning rate around 2e-4 and 1–3 epochs, then stop as soon as the held-out loss stops improving.</p>",
  "summary": "<p>Fine-tuning takes an already-trained model and nudges its parameters to specialise it for your task, usually with prompt-and-answer pairs (SFT). Full fine-tuning updates every dial; the cheaper, swappable default is PEFT/LoRA, which freezes the model and trains a tiny add-on. Use it for behaviour, tone, and format — not for storing fresh facts (use RAG for that) — and guard against forgetting and overfitting with a low learning rate, short runs, and a held-out test set.</p>",
  "keyTerms": [
    { "term": "Fine-tuning", "def": "Taking a pretrained model and further training it on a smaller, focused dataset so it gets better at a specific task or style." },
    { "term": "Parameters", "def": "The billions of internal numbers (the \"dials\") that a model adjusts during training to decide what to output." },
    { "term": "Learning rate", "def": "How big a step the model takes when correcting itself. Too high overwrites old skills; too low barely learns." },
    { "term": "Loss", "def": "A single number measuring how wrong the model's answers are on a batch. Training works to push it down." },
    { "term": "Epoch", "def": "One full pass through the entire training dataset. More epochs means more practice, but too many causes overfitting." },
    { "term": "Full fine-tuning", "def": "Updating every parameter in the model. Powerful but memory-hungry and expensive." },
    { "term": "PEFT", "def": "Parameter-Efficient Fine-Tuning: freeze the original model and train only a small add-on, saving memory and cost." },
    { "term": "LoRA", "def": "The most popular PEFT method: it trains tiny extra matrices and saves them as a small, swappable adapter file." },
    { "term": "Overfitting", "def": "When a model memorises its training examples instead of learning the pattern, so it fails on new inputs." },
    { "term": "Catastrophic forgetting", "def": "When fine-tuning too hard on a narrow task makes the model worse at the general skills it used to have." }
  ],
  "quiz": [
    {
      "q": "You need a model to always answer using your latest internal pricing, which changes weekly. What is the best approach?",
      "options": [
        "Fine-tune the model on the prices every week",
        "Use RAG to feed the current prices in at question time",
        "Increase the learning rate so prices stick faster",
        "Train for more epochs so the model memorises them"
      ],
      "answer": 1,
      "explain": "Fine-tuning stores facts in a leaky, hard-to-update way and prices change constantly. RAG injects the current facts at question time, so they are always exact and fresh."
    },
    {
      "q": "What is the main difference between full fine-tuning and PEFT methods like LoRA?",
      "options": [
        "Full fine-tuning works only on small models; PEFT only on large ones",
        "PEFT updates every parameter while full fine-tuning freezes most of them",
        "Full fine-tuning updates every parameter; PEFT freezes the model and trains a tiny add-on",
        "They are identical, just different names from different libraries"
      ],
      "answer": 2,
      "explain": "Full fine-tuning rewrites every dial in the model, which is costly. PEFT (e.g. LoRA) freezes the base model and trains a small set of new dials, saving memory and producing a swappable adapter."
    },
    {
      "q": "After 10 epochs your model scores perfectly on the training data but does badly on emails it has never seen. What most likely happened?",
      "options": [
        "Overfitting — it memorised the examples instead of the pattern",
        "The learning rate was far too low to learn anything",
        "Catastrophic forgetting of its pretraining",
        "The held-out test set was contaminated with training data"
      ],
      "answer": 0,
      "explain": "Great training scores plus poor performance on new inputs is the classic signature of overfitting. Running for too many epochs lets the model memorise specific examples rather than the general rule."
    },
    {
      "q": "Which job is fine-tuning genuinely the right tool for?",
      "options": [
        "Giving the model today's news headlines",
        "Making the model always reply in your brand's concise, formal voice",
        "Letting the model look up a customer's order status",
        "Storing a 500-page product manual word-for-word"
      ],
      "answer": 1,
      "explain": "Fine-tuning excels at teaching consistent behaviour, tone, and format. Fresh facts, lookups, and large reference documents are better handled by RAG, not baked into the parameters."
    }
  ]
}
