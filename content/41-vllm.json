{
  "id": "vllm",
  "order": 41,
  "section": "Local AI Ecosystem",
  "title": "vLLM",
  "lesson": "<p><strong>vLLM</strong> is a free, open-source program whose only job is to run an open-weight language model <em>fast, for many users at once</em>. Think of it as the engine you bolt under the hood when you stop playing with a model on your laptop and start serving real traffic. If <a class=\"xref\" href=\"#ollama\">Ollama</a> is the friendly hatchback that gets one person around town, vLLM is the articulated lorry built to haul hundreds of passengers down the motorway all day without stopping. Both drive the same roads; they are built for completely different loads.</p>\n<h4>The problem it was born to solve</h4>\n<p>When a model writes an answer, it produces one <a class=\"xref\" href=\"#tokens\">token</a> (a word or word-piece) at a time, and at each step it reuses a <a class=\"xref\" href=\"#kv-cache\">KV cache</a> (the saved keys and values of every earlier token, so it never re-reads the conversation from scratch). That cache lives in <a class=\"xref\" href=\"#vram-basics\">GPU memory (VRAM)</a> and grows with every token. The old, naive way of serving handed each request one big contiguous slab of memory sized for the worst case. Most of every slab sat empty. It was like a hotel that books out a whole floor per guest in case they bring a hundred friends — almost every room ends up dark, and the hotel fills up after a handful of guests.</p>\n<h4>PagedAttention: the headline trick</h4>\n<p>vLLM's famous invention is <strong>PagedAttention</strong>. Instead of one giant slab per request, it chops the KV cache into many small, equal-sized <em>blocks</em> and hands them out only as a conversation actually needs them — exactly how a computer's operating system gives programs memory in little <em>pages</em> rather than one fixed chunk. The hotel now rents rooms one night at a time as guests arrive, so almost no room sits empty. The payoff is huge: far less wasted VRAM, which means many more conversations packed onto the same GPU, which means much higher <strong>throughput</strong> (total tokens served per second across everyone).</p>\n<h4>Continuous batching: keep the kitchen full</h4>\n<p>The second big idea is <strong>continuous batching</strong>. A GPU is happiest when it processes many requests together in one go (this grouping is <a class=\"xref\" href=\"#batch-inference\">batching</a>). Naive servers wait for a fixed group to all finish before starting the next — so one slow request stalls everyone, and a 5-word reply has to wait for a 500-word one. vLLM instead slots a new request into the running batch the instant any slot frees up, and lets finished ones leave early. Picture a chef who adds the next ticket to the pan the moment a finished dish leaves it, rather than cooking in rigid rounds of four. The GPU never idles waiting for the slowpoke, so it stays busy and earns its keep. Together, PagedAttention frees up the memory and continuous batching fills the freed space with work — the two tricks reinforce each other.</p>\n<h4>Running it</h4>\n<p>vLLM serves an <strong>OpenAI-compatible API</strong> — it speaks the same request shape as OpenAI, so existing client code works by just changing the base URL. Starting a server is roughly one command:</p>\n<pre><code class=\"lang-bash\">vllm serve meta-llama/Llama-3.1-8B-Instruct \\\n  --max-model-len 8192 \\\n  --gpu-memory-utilization 0.90 \\\n  --port 8000</code></pre>\n<p>Then any app talks to it like a normal chat API:</p>\n<pre><code class=\"lang-python\">from openai import OpenAI\nclient = OpenAI(base_url=\"http://localhost:8000/v1\", api_key=\"x\")\nresp = client.chat.completions.create(\n    model=\"meta-llama/Llama-3.1-8B-Instruct\",\n    messages=[{\"role\": \"user\", \"content\": \"Say hi in 5 words.\"}],\n)\nprint(resp.choices[0].message.content)</code></pre>\n<h4>Where it fits</h4>\n<p>vLLM is the default high-throughput choice in 2026 for <a class=\"xref\" href=\"#model-serving\">serving</a> open-weight models on <a class=\"xref\" href=\"#gpu-basics\">GPUs</a> (rivals include TGI and SGLang). It loads models straight from <a class=\"xref\" href=\"#hugging-face\">Hugging Face</a>, supports <a class=\"xref\" href=\"#quantization\">quantized</a> weights to fit bigger models in less VRAM, uses fast math like <a class=\"xref\" href=\"#flash-attention\">Flash Attention</a> under the hood, can split one giant model across several GPUs, and even reuses a shared prompt prefix across calls (prefix caching) — a big win when every request carries the same long <a class=\"xref\" href=\"#system-prompts\">system prompt</a>.</p>\n<p>The honest catch: vLLM is built for NVIDIA-style data-centre and gaming GPUs running on Linux. It is overkill for a single chat on your own machine, where <a class=\"xref\" href=\"#ollama\">Ollama</a> or <a class=\"xref\" href=\"#llama-cpp\">llama.cpp</a> are friendlier, and it does not target Apple Silicon (use <a class=\"xref\" href=\"#mlx\">MLX</a> there). Reach for vLLM when you have a real GPU and need to serve many people quickly and cheaply per token.</p>\n<p class=\"callout tip\">💡 The rule of thumb: one user on a laptop → Ollama. Many users hitting an <a class=\"xref\" href=\"#api-serving\">API</a> on a GPU → vLLM. The throughput gap between them under heavy load is not small; it is often several-fold.</p>",
  "mentalModel": "Ollama is the hatchback that drives one person to the shops; vLLM is the articulated lorry built to haul hundreds down the motorway non-stop. PagedAttention rents GPU memory room-by-room instead of booking a whole floor per guest, and continuous batching keeps the kitchen cooking new tickets the instant a plate leaves — so the expensive GPU never sits idle.",
  "mistakes": [
    "<strong>Using vLLM for a single local chat.</strong> It is engineered for many simultaneous users on a real GPU. For one person on a laptop it is heavy and fiddly to set up — <a class=\"xref\" href=\"#ollama\">Ollama</a> or <a class=\"xref\" href=\"#llama-cpp\">llama.cpp</a> will get you chatting in minutes with far less ceremony.",
    "<strong>Expecting it to run on a Mac.</strong> vLLM targets NVIDIA-class GPUs on Linux. It does not serve well (or at all) on Apple Silicon — on a Mac reach for <a class=\"xref\" href=\"#mlx\">MLX</a> or llama.cpp instead. Check your hardware before you fight the installer.",
    "<strong>Cranking <code>--gpu-memory-utilization</code> to fit a bigger model and then crashing under load.</strong> That setting reserves VRAM for the model <em>and</em> the growing <a class=\"xref\" href=\"#kv-cache\">KV cache</a>. Leave headroom: a model that loads fine can still hit out-of-memory once many long conversations fill the cache.",
    "<strong>Setting <code>--max-model-len</code> far higher than you need.</strong> A bigger context window reserves more cache per request, so you serve fewer users at once. Size it to your real prompts plus answers, not to the model's theoretical maximum, or your throughput quietly tanks.",
    "<strong>Measuring speed with one request and declaring victory.</strong> vLLM's whole advantage is throughput <em>under concurrency</em>. Testing with a single call hides the win and tells you nothing about 200 simultaneous users — always load-test with real <a class=\"xref\" href=\"#speed-benchmarking\">speed benchmarking</a>.",
    "<strong>Assuming PagedAttention or batching changes the answer.</strong> They are pure speed and memory tricks; the model's output is identical with or without them. They make serving cheaper and faster, not the model smarter."
  ],
  "exercise": "<p>Feel the throughput win firsthand. On a machine with an NVIDIA GPU (or a rented <a class=\"xref\" href=\"#cloud-gpus\">cloud GPU</a>), start a small instruct model with <code>vllm serve</code> (a 1B–8B model is plenty). First send a single chat request with the OpenAI client and record how many total tokens-per-second you get. Then fire 20 requests at the same time (a short <code>asyncio</code> script, or even 20 parallel <code>curl</code> calls) and record the <em>combined</em> tokens-per-second across all of them. Compare the two numbers and write one sentence explaining why the total climbed.</p><p><strong>Stretch goal:</strong> Raise <code>--max-model-len</code> (or push concurrency higher) until vLLM starts rejecting requests or warning about cache space, and explain in one sentence why the <a class=\"xref\" href=\"#kv-cache\">KV cache</a> — not the model weights — was the thing that ran out of room.</p>",
  "solution": "<p><strong>Hint:</strong> With one request the GPU is mostly idle, waiting on a single slow token stream, so total throughput is low. When 20 arrive together, continuous batching packs them into the same forward passes and PagedAttention fits all their caches without waste, so the GPU does useful work on many streams at once — combined tokens-per-second jumps several-fold while each user's individual latency rises only modestly. For the stretch goal: the model weights are a one-time fixed cost loaded once, but <em>every</em> concurrent conversation needs its own slice of KV cache, and that slice grows with context length. Push <code>max-model-len</code> or concurrency high enough and the total cache outgrows the VRAM vLLM set aside, so it queues or refuses new requests — even though the weights fit comfortably.</p>",
  "summary": "<p>vLLM is the go-to open-source engine for serving open-weight models fast to many users on GPUs. Its two big tricks are PagedAttention, which stores the KV cache in small reusable blocks to waste far less VRAM, and continuous batching, which keeps the GPU full by slotting new requests into the running batch instantly. It exposes an OpenAI-compatible API and loads models from Hugging Face. Use it for production-scale serving on NVIDIA GPUs — not for a single chat on a laptop or on a Mac, where Ollama, llama.cpp, or MLX fit better.</p>",
  "keyTerms": [
    { "term": "vLLM", "def": "A free, open-source serving engine that runs open-weight language models with high throughput for many simultaneous users on GPUs." },
    { "term": "PagedAttention", "def": "vLLM's core trick: storing the KV cache in many small, reusable blocks (like an operating system's memory pages) instead of one big slab per request, so it wastes far less VRAM." },
    { "term": "Continuous batching", "def": "Slotting new requests into a batch already running on the GPU the instant a slot frees up, instead of waiting for a fixed group to finish — keeps the GPU busy." },
    { "term": "Throughput", "def": "Total work served per second, usually tokens per second across all users combined. vLLM is built to maximise this; high throughput means the GPU is well used." },
    { "term": "KV cache", "def": "The stored keys and values of every token seen so far, kept in GPU memory so the model reuses them instead of re-reading the past. It grows with every token and eats VRAM." },
    { "term": "OpenAI-compatible API", "def": "Exposing a server using the same request and response shape as OpenAI's API, so existing client code works by only changing the base URL." },
    { "term": "max-model-len", "def": "The maximum context length (input plus output tokens) vLLM will allow per request. Higher means more KV-cache memory reserved per user, so fewer users fit at once." },
    { "term": "gpu-memory-utilization", "def": "A vLLM setting for what fraction of GPU memory to claim for weights and the KV cache. Too high leaves no headroom and risks out-of-memory crashes under load." },
    { "term": "Prefix caching", "def": "Reusing the cached keys and values of a shared prompt prefix across requests, so identical boilerplate like a system prompt is processed once rather than every call." }
  ]
}
