{
  "id": "axolotl",
  "order": 45,
  "section": "Local AI Ecosystem",
  "title": "Axolotl",
  "lesson": "<p><strong>Axolotl</strong> is a free, open-source tool that fine-tunes language models from a single configuration file. You do not write training code. You fill in a <code>.yaml</code> file — a plain text file of <em>setting: value</em> pairs — that says which model to start from, which dataset to feed it, and which fine-tuning method to use. Then you run one command and Axolotl does the rest. It wraps the messy machinery of <a class=\"xref\" href=\"#hugging-face\">Hugging Face</a>, <a class=\"xref\" href=\"#peft\">PEFT</a>, and the <a class=\"xref\" href=\"#trl-library\">TRL Library</a> into one tidy front door.</p>\n<h4>Why a tool like this exists</h4>\n<p>Fine-tuning by hand means gluing together a dozen libraries: load the model, quantize it, attach a <a class=\"xref\" href=\"#lora\">LoRA</a> adapter, tokenize and format the data, set up the trainer, pick the learning rate, save checkpoints, and so on.</p>\n<p>Every step has knobs, and a wrong type or a missing argument breaks the whole run an hour in. Axolotl collects all those knobs into one file and ships sensible defaults, so a beginner can train a real model on day one instead of week three.</p>\n<h4>Two ways to picture it</h4>\n<p><strong>Analogy — the bread machine.</strong> Baking bread from scratch means measuring flour, proving yeast, kneading, timing the oven. A bread machine asks you to pour in the ingredients and pick a program; it handles the kneading and baking. Axolotl is the bread machine of fine-tuning. The YAML file is your ingredient list and program choice. You still need good ingredients (a clean dataset) and the right program (the matching method), but you are spared the manual kneading.</p>\n<p><strong>Analogy — a recipe card versus loose notes.</strong> Writing training code by hand is like cooking from scribbled notes on twelve sticky pads — easy to lose one, easy to copy a number wrong. A YAML config is a single printed recipe card. Anyone can read it, copy it, tweak one line, and reproduce your exact dish.</p>\n<p>That reproducibility is the quiet superpower: you share the card and a teammate gets the same loaf, not a slightly different one because they guessed a temperature. In real teams a fine-tune you cannot reproduce six months later is a fine-tune you cannot safely improve or debug.</p>\n<h4>What the config controls</h4>\n<p>A typical Axolotl config covers a few groups of settings:</p>\n<ul><li>The <em>base model</em> to start from.</li><li>The <em>dataset</em> and its format.</li><li>The <em>method</em> — full fine-tune, LoRA, or <a class=\"xref\" href=\"#qlora\">QLoRA</a>.</li><li>The <em>training hyperparameters</em> — learning rate, batch size, how many passes over the data.</li></ul>\n<p>Here is a trimmed example for a small QLoRA run:</p>\n<pre><code class=\"lang-yaml\">base_model: meta-llama/Llama-3.1-8B\nload_in_4bit: true          # QLoRA: load base in 4-bit\nadapter: qlora              # train a LoRA adapter\nlora_r: 16                  # adapter size\ndatasets:\n  - path: ./my_data.jsonl\n    type: alpaca            # known instruction format\nsequence_len: 2048          # max tokens per example\nmicro_batch_size: 2\nnum_epochs: 3\nlearning_rate: 0.0002\noutput_dir: ./out</code></pre>\n<p>Then you launch training with one line:</p>\n<pre><code class=\"lang-bash\">axolotl train config.yaml</code></pre>\n<p>The word <em>epoch</em> means one full pass over your whole dataset; <code>num_epochs: 3</code> means the model sees every example three times. <em>Hyperparameters</em> are the dials you set before training that shape <em>how</em> it learns, as opposed to the weights the model learns by itself.</p>\n<h4>Where it fits in the ecosystem</h4>\n<p>Axolotl does not invent new training math. It orchestrates existing pieces:</p>\n<ul><li><a class=\"xref\" href=\"#peft\">PEFT</a> provides the LoRA adapters.</li><li>The <a class=\"xref\" href=\"#trl-library\">TRL Library</a> provides trainers for methods like <a class=\"xref\" href=\"#dpo\">DPO</a> (training a model to prefer better answers over worse ones).</li><li><a class=\"xref\" href=\"#hugging-face\">Hugging Face</a> provides the models and tokenizers (the code that splits text into <a class=\"xref\" href=\"#tokens\">tokens</a>).</li></ul>\n<p>Axolotl is the conductor, not the orchestra. Because the orchestra is the same one everyone else uses, your trained model is fully compatible with the wider toolset — there is no lock-in. It also supports multi-GPU training and advanced data formats out of the box, which is why teams reach for it once a job grows past one card.</p>\n<p>A common real-world flow: collect a few thousand examples of the behavior you want, format them, point a config at a small base model, run a cheap QLoRA pass overnight, and test the result the next morning. If it looks promising, scale the same config to a bigger model or more GPUs by changing two or three lines. The config is the unit of work you iterate on.</p>\n<table class=\"cmp\"><thead><tr><th>Axolotl</th><th>Unsloth</th></tr></thead><tbody><tr><td>Config-file driven (YAML)</td><td>Python-script driven</td></tr><tr><td>Many models, multi-GPU, many methods</td><td>One GPU, heavily speed-optimized kernels</td></tr><tr><td>Great for reproducible, scaled jobs</td><td>Fastest single-GPU training and lowest memory</td></tr><tr><td>Wraps PEFT/TRL/HF broadly</td><td>Custom hand-tuned code paths</td></tr></tbody></table>\n<p>Both share the same goal — easy fine-tuning — but pick different routes. <a class=\"xref\" href=\"#unsloth\">Unsloth</a> chases raw speed on one card; Axolotl chases breadth and reproducibility. Many people prototype in one and scale in the other.</p>\n<p class=\"callout tip\">💡 The hardest part of using Axolotl is rarely the tool. It is your <a class=\"xref\" href=\"#sft-datasets\">dataset</a> and choosing the right <code>type</code> so your data is formatted into the chat or instruction shape the model expects. Get the data right and the YAML almost writes itself.</p>",
  "mentalModel": "Axolotl is a bread machine for fine-tuning: you pour in your ingredients (a base model and a dataset), pick a program (LoRA, QLoRA, or full fine-tune) on a single recipe card (the YAML config), press start, and it does the kneading and baking — wrapping Hugging Face, PEFT, and TRL underneath.",
  "takeaways": [
    "One YAML config plus one command replaces hand-written training code.",
    "Axolotl is a conductor — it orchestrates Hugging Face, PEFT, and TRL.",
    "The hard part is your dataset and its <code>type</code>, not the tool."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"One Axolotl YAML config drives Hugging Face, PEFT and TRL to produce a fine-tuned adapter\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"32\" text-anchor=\"middle\">One config, many libraries, one output</text><rect class=\"dg-box dg-box-accent\" x=\"40\" y=\"130\" width=\"150\" height=\"70\" rx=\"12\"/><text class=\"dg-label\" x=\"115\" y=\"160\" text-anchor=\"middle\">config.yaml</text><text class=\"dg-sub\" x=\"115\" y=\"182\" text-anchor=\"middle\">model + data + method</text><rect class=\"dg-box\" x=\"290\" y=\"60\" width=\"150\" height=\"50\" rx=\"12\"/><text class=\"dg-label\" x=\"365\" y=\"90\" text-anchor=\"middle\">Hugging Face</text><rect class=\"dg-box\" x=\"290\" y=\"140\" width=\"150\" height=\"50\" rx=\"12\"/><text class=\"dg-label\" x=\"365\" y=\"170\" text-anchor=\"middle\">PEFT (LoRA)</text><rect class=\"dg-box\" x=\"290\" y=\"220\" width=\"150\" height=\"50\" rx=\"12\"/><text class=\"dg-label\" x=\"365\" y=\"250\" text-anchor=\"middle\">TRL trainer</text><rect class=\"dg-box dg-box-good\" x=\"540\" y=\"130\" width=\"150\" height=\"70\" rx=\"12\"/><text class=\"dg-label\" x=\"615\" y=\"160\" text-anchor=\"middle\">Fine-tuned</text><text class=\"dg-sub\" x=\"615\" y=\"182\" text-anchor=\"middle\">adapter / model</text><line class=\"dg-edge\" x1=\"190\" y1=\"155\" x2=\"285\" y2=\"90\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"190\" y1=\"165\" x2=\"285\" y2=\"165\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"190\" y1=\"175\" x2=\"285\" y2=\"240\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"440\" y1=\"90\" x2=\"535\" y2=\"155\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"440\" y1=\"165\" x2=\"535\" y2=\"165\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"440\" y1=\"240\" x2=\"535\" y2=\"175\" marker-end=\"url(#arrow)\"/><text class=\"dg-note\" x=\"360\" y=\"312\" text-anchor=\"middle\">Axolotl reads the YAML and conducts the three libraries for you</text></svg>",
    "caption": "Axolotl turns one YAML config into orchestrated calls to Hugging Face, PEFT, and TRL, producing a fine-tuned adapter or model."
  },
  "quiz": [
    {
      "q": "What is the single thing you mainly edit to define an Axolotl training run?",
      "options": [
        "A Python training script with a custom training loop",
        "A YAML configuration file of setting: value pairs",
        "A Jupyter notebook you run cell by cell",
        "The model's weights directly in a hex editor"
      ],
      "answer": 1,
      "explain": "Axolotl is config-driven: you fill in a YAML file naming the base model, dataset, method, and hyperparameters, then run one command. You don't hand-write the training loop."
    },
    {
      "q": "You set adapter: qlora and your run finishes. What did Axolotl actually save?",
      "options": [
        "A full standalone model ready to serve as-is",
        "Only the cleaned dataset, not a model",
        "A small LoRA adapter you must merge into the base to get a standalone model",
        "Nothing — QLoRA only validates the config without training"
      ],
      "answer": 2,
      "explain": "QLoRA trains a small adapter on top of a frozen base. To get a single standalone model you merge the adapter into the base afterward (e.g. axolotl merge-lora)."
    },
    {
      "q": "Your model trained fine but answers in a weird, broken turn-by-turn format. What's the most likely culprit in the config?",
      "options": [
        "num_epochs was set to 3 instead of 1",
        "output_dir pointed to the wrong folder",
        "The dataset type / chat template didn't match the data and model's expected format",
        "load_in_4bit was set to true"
      ],
      "answer": 2,
      "explain": "The type field controls how raw rows become the prompt/chat shape the model expects. A mismatch teaches the wrong template, so the model never learns the right turn structure."
    },
    {
      "q": "When would a team reach for Axolotl over Unsloth?",
      "options": [
        "They need the absolute fastest single-GPU run with the lowest memory",
        "They want a reproducible config they can scale across many GPUs and methods",
        "They want to avoid Hugging Face, PEFT, and TRL entirely",
        "They need to serve the model for inference instead of training it"
      ],
      "answer": 1,
      "explain": "Axolotl chases breadth and reproducibility — many models, multi-GPU, many methods, all from a shareable config. Unsloth instead optimizes for the fastest, lowest-memory single-GPU run."
    }
  ],
  "mistakes": [
    "<strong>Picking the wrong dataset <code>type</code>.</strong> The <code>type</code> field tells Axolotl how to turn your raw rows into the prompt format the model was trained on. If you pick <code>alpaca</code> when your data is really chat-shaped, the model learns the wrong template and your results suffer. Match the type to your data and to the model's expected chat format.",
    "<strong>Confusing Axolotl with a model.</strong> Axolotl is a <em>training tool</em>, not an LLM. It produces an adapter or a fine-tuned model; it does not answer prompts itself. To run the result you still need an inference tool like <a class=\"xref\" href=\"#vllm\">vLLM</a> or <a class=\"xref\" href=\"#ollama\">Ollama</a>.",
    "<strong>Setting <code>sequence_len</code> too high for your <a class=\"xref\" href=\"#vram-basics\">VRAM</a>.</strong> Longer sequences blow up memory fast because of the <a class=\"xref\" href=\"#kv-cache\">KV cache</a> and activations. A run that fits at 2048 tokens can crash at 8192. Start small and raise it only if memory allows.",
    "<strong>Forgetting that the output is just an adapter.</strong> With <code>adapter: qlora</code>, Axolotl saves a small LoRA file, not a full model. To get a standalone model you must merge the adapter into the base afterward — see <a class=\"xref\" href=\"#adapter-tuning\">Adapter Tuning</a>.",
    "<strong>Ignoring the sample-packing and chat-template options.</strong> Axolotl can pack many short examples into one sequence for speed, and can apply a model's exact chat template automatically. Skipping these defaults can mean slower training or a model that never learns the right turn-by-turn format.",
    "<strong>Blaming the tool when training diverges.</strong> If loss explodes or the model outputs garbage, the cause is usually data or a too-high learning rate, not Axolotl. Lower <code>learning_rate</code>, inspect a few formatted examples, and validate your dataset before assuming a bug."
  ],
  "exercise": "<p>Write a minimal Axolotl YAML config that fine-tunes a small open model (e.g. an 8B Llama or a 3B Qwen) with <strong>QLoRA</strong> on a tiny instruction dataset of about 100 examples in a <code>.jsonl</code> file. Set <code>load_in_4bit: true</code>, <code>adapter: qlora</code>, <code>lora_r: 16</code>, <code>num_epochs: 1</code>, and a small <code>micro_batch_size</code>. Run <code>axolotl train config.yaml</code> and watch the loss number drop over the run.</p><p><strong>Stretch goal:</strong> After training, merge the adapter into the base model, export it to <a class=\"xref\" href=\"#gguf-models\">GGUF</a> format, and load it in <a class=\"xref\" href=\"#ollama\">Ollama</a> to chat with your fine-tuned model locally.</p>",
  "solution": "<p><strong>Hint:</strong> Each row of your <code>.jsonl</code> should look like <code>{\"instruction\": \"...\", \"input\": \"\", \"output\": \"...\"}</code> if you use <code>type: alpaca</code>. A healthy run shows the training loss starting around 1–2 and drifting downward; a flat or exploding loss means the learning rate or data is off. With 100 examples and one epoch, do not expect magic — the point is to see the full pipeline work end to end. For the stretch goal, Axolotl has a built-in merge step (<code>axolotl merge-lora config.yaml</code>), and tools like <a class=\"xref\" href=\"#llama-cpp\">llama.cpp</a> handle the GGUF conversion that Ollama then loads.</p>",
  "summary": "<p>Axolotl turns fine-tuning into filling out one YAML config file and running one command. It orchestrates Hugging Face, PEFT, and TRL under the hood, supports LoRA, QLoRA, and full fine-tunes across one or many GPUs, and shines at reproducible, scaled jobs. The real work is preparing a clean dataset and matching its format; the tool handles the rest.</p>",
  "keyTerms": [
    { "term": "Axolotl", "def": "An open-source fine-tuning tool driven by a single YAML config file. It wraps Hugging Face, PEFT, and TRL so you train models without writing training code." },
    { "term": "YAML config", "def": "A plain text file of setting: value pairs that defines an Axolotl run — base model, dataset, method, and hyperparameters — making the recipe easy to share and reproduce." },
    { "term": "Base model", "def": "The pretrained model you start fine-tuning from, such as Llama-3.1-8B. Axolotl adapts it to your data rather than training from scratch." },
    { "term": "Adapter", "def": "A small set of trainable weights (LoRA/QLoRA) that Axolotl learns instead of changing the whole model. You merge it into the base afterward to get a standalone model." },
    { "term": "Epoch", "def": "One full pass over your entire dataset during training. num_epochs: 3 means the model sees every example three times." },
    { "term": "Hyperparameter", "def": "A dial you set before training that shapes how learning happens — like learning rate, batch size, or number of epochs — distinct from the weights the model learns itself." },
    { "term": "Dataset type", "def": "The Axolotl field that tells it how to format your raw rows into the prompt shape the model expects (e.g. alpaca for instruction data, or a chat template)." },
    { "term": "Sample packing", "def": "Combining several short training examples into one longer sequence so the GPU wastes less effort on padding, speeding up training." }
  ]
}
