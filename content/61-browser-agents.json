{
  "id": "browser-agents",
  "order": 61,
  "section": "Agents & Workflows",
  "title": "Browser Agents",
  "lesson": "<p>Most software talks to other software through tidy, pre-agreed channels called <strong>APIs</strong> (an API is an official \"side door\" a service opens for programs to plug into). But huge chunks of the world have no such door. Booking a niche hotel, filling a government form, checking a supplier portal, pulling a report from an old internal dashboard — these live only behind a normal web page meant for human eyes and a mouse.</p><p>A <strong>browser agent</strong> is an <a class=\"xref\" href=\"#ai-agents\">AI Agent</a> that drives a real web browser the way a person would: it looks at the page, decides where to click or type, does it, looks again, and keeps going until the task is done.</p><h4>Analogy 1: a remote-controlled intern</h4><p>Picture hiring an intern who works from home and shares their screen. You say \"refund the order from Tuesday.\" They open the site, squint at the screen, find the orders tab, click it, scroll, spot Tuesday's order, hit refund, confirm.</p><p>You never tell them the exact pixel to click — they figure it out from what they see. A browser agent is that intern, except the \"eyes\" are a model reading the page and the \"hand\" is code that controls the browser.</p><h4>What the agent actually \"sees\"</h4><p>A browser agent runs in a loop, and the loop is the whole idea. Each turn it gathers the current state of the page, sends that to the model, gets back one action, performs it, and repeats. There are two main ways to show the page to the model:</p><ul><li><strong>Screenshots (pixels).</strong> The agent sends an image of the page to a <a class=\"xref\" href=\"#vlms\">VLM</a> (a vision-language model — one that can read pictures, not just text). The model literally looks at the screen and says \"click the blue Submit button at the bottom.\" This works on any site, even ones built as a wall of images.</li><li><strong>Accessibility tree / HTML (text).</strong> Instead of a picture, the agent hands the model a cleaned-up text outline of the page: every button, link, and field, each with a number. The model replies \"click element 14.\" This is cheaper, faster, and less error-prone than guessing pixel coordinates.</li></ul><p>Many real systems do both: a text outline for precision, plus a screenshot when the layout is confusing. Either way, the model never controls the browser directly. It only emits actions — <code>click</code>, <code>type</code>, <code>scroll</code>, <code>navigate</code> — which is just <a class=\"xref\" href=\"#tool-calling\">Tool Calling</a> aimed at a browser instead of an API.</p><h4>The perceive–decide–act loop</h4><p>Here is the cycle in plain pseudo-code:</p><pre><code class=\"lang-python\">goal = \"Find the cheapest flight to Lisbon next Friday\"\nstate = browser.snapshot()        # screenshot + labelled elements\nwhile not done:\n    action = model.decide(goal, state, history)\n    # e.g. {\"type\": \"click\", \"element_id\": 23}\n    browser.do(action)\n    state = browser.snapshot()     # look again after acting\n    done = model.is_goal_met(goal, state)</code></pre><p>This \"look again after every action\" step is what separates an agent from a fixed script. A normal automation script breaks the moment a button moves. The agent re-reads the page each turn, so it can adapt when the site looks different than expected. That makes it a kind of <a class=\"xref\" href=\"#agentic-workflows\">Agentic Workflow</a> where the environment is a live web page.</p><h4>Analogy 2: GPS that recalculates</h4><p>An old paper map gives you one fixed route; miss a turn and you are lost. A satnav recalculates after every wrong turn because it keeps checking where you actually are.</p><p>A hard-coded macro is the paper map. A browser agent is the satnav: it keeps re-checking the page and re-planning, which is why it survives pop-ups, cookie banners, and redesigned layouts that would shatter a brittle script.</p><h4>Where this matters in real products</h4><p>By 2026 this is a hot area: providers ship hosted \"computer use\" browser agents, and open tools like Playwright-based frameworks let you build your own. Real uses include:</p><ul><li>Automated QA testing of websites</li><li>Scraping data from sites with no API</li><li>Filling repetitive forms and comparison shopping</li><li>Onboarding flows that touch five different legacy portals</li></ul><p>It overlaps heavily with <a class=\"xref\" href=\"#ai-automation\">AI Automation</a>, and a complex job is often split across several agents (a <a class=\"xref\" href=\"#multi-agent-systems\">Multi-Agent System</a>) — one to plan, one to drive the browser, one to check the result.</p><p class=\"callout warn\">⚠️ Browser agents are powerful and risky: a wrong click can buy the wrong thing, submit a real form, or delete data. Always test in a throwaway account, and for anything that spends money or changes records, keep a human approval step in the loop.</p><p>The honest current state: browser agents are genuinely useful but still <em>flaky</em>. They get lost on dense pages, misread tiny buttons, and slow tasks down because every step is a model call. Treat them as a capable-but-clumsy intern who needs guardrails, not a flawless robot.</p>",
  "mentalModel": "<p>A browser agent is a remote intern sharing your screen: each turn it looks at the page, picks one action (click/type/scroll), does it, then looks again — a perceive–decide–act loop that re-checks reality after every move, like a satnav recalculating.</p>",
  "takeaways": [
    "Browser agents drive a real browser when a site has no API.",
    "They loop: look at the page, take one action, look again.",
    "Powerful but flaky — cap steps and gate risky clicks with a human."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"The perceive-decide-act loop of a browser agent\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"34\" text-anchor=\"middle\">The perceive–decide–act loop</text><rect class=\"dg-box\" x=\"40\" y=\"110\" width=\"150\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"115\" y=\"138\" text-anchor=\"middle\">Web page</text><text class=\"dg-sub\" x=\"115\" y=\"158\" text-anchor=\"middle\">live, no API</text><rect class=\"dg-box dg-box-accent\" x=\"250\" y=\"110\" width=\"160\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"330\" y=\"138\" text-anchor=\"middle\">Snapshot</text><text class=\"dg-sub\" x=\"330\" y=\"158\" text-anchor=\"middle\">pixels + elements</text><rect class=\"dg-box dg-box-2\" x=\"470\" y=\"110\" width=\"160\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"550\" y=\"138\" text-anchor=\"middle\">Model decides</text><text class=\"dg-sub\" x=\"550\" y=\"158\" text-anchor=\"middle\">one action</text><rect class=\"dg-box dg-box-good\" x=\"250\" y=\"250\" width=\"160\" height=\"60\" rx=\"12\"/><text class=\"dg-label\" x=\"330\" y=\"286\" text-anchor=\"middle\">Act: click / type</text><rect class=\"dg-box dg-box-warn\" x=\"470\" y=\"250\" width=\"160\" height=\"60\" rx=\"12\"/><text class=\"dg-label\" x=\"550\" y=\"278\" text-anchor=\"middle\">Goal met?</text><text class=\"dg-sub\" x=\"550\" y=\"297\" text-anchor=\"middle\">stop or loop</text><line class=\"dg-edge\" x1=\"190\" y1=\"143\" x2=\"242\" y2=\"143\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"410\" y1=\"143\" x2=\"462\" y2=\"143\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"550\" y1=\"176\" x2=\"550\" y2=\"242\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"470\" y1=\"280\" x2=\"412\" y2=\"280\" marker-end=\"url(#arrow)\"/><path class=\"dg-edge\" d=\"M250,280 C150,280 115,230 115,184\" marker-end=\"url(#arrow)\"/><text class=\"dg-note\" x=\"175\" y=\"248\" text-anchor=\"middle\">look again</text></svg>",
    "caption": "The agent snapshots the page, picks one action, acts, then re-snapshots and loops until the goal is met."
  },
  "mistakes": [
    "<strong>Confusing it with a fixed script.</strong> A recorded macro replays the exact same clicks and shatters when a button moves. A browser agent re-reads the page each turn and adapts. If you wanted rock-solid repeatability on a stable site, a plain script is cheaper and more reliable — reach for an agent only when the page changes or needs judgement.",
    "<strong>Letting it run with no guardrails on real accounts.</strong> The model can hallucinate a click and end up buying something, sending an email, or deleting a record. Always start in a sandbox or test account, and gate any irreversible action (payments, submissions, deletions) behind a human approval step.",
    "<strong>Feeding it the raw, full HTML.</strong> A modern page is tens of thousands of tokens of nested tags and scripts. Dumping all of it blows the <a class=\"xref\" href=\"#context-windows\">context window</a> and confuses the model. Send a cleaned outline (the accessibility tree) of just the interactive elements instead.",
    "<strong>Relying on pixel coordinates alone.</strong> Telling the model to 'click at x=412, y=830' is fragile — it misses by a few pixels, screens resize, layouts shift. Prefer labelling elements with stable IDs so the action is 'click element 14', not a guess at where it sits.",
    "<strong>No step limit or loop guard.</strong> Agents can get stuck clicking the same dead button forever, burning money and time. Always cap the number of steps, detect when the page hasn't changed after an action, and bail out or ask for help instead of looping.",
    "<strong>Ignoring how slow and expensive it is.</strong> Every single step is a model call plus a page load, so a ten-action task can take a minute and cost real tokens. Don't use a browser agent for something a simple API call or script could do in 200 milliseconds."
  ],
  "exercise": "<p>Sketch (on paper or in pseudo-code) the perceive–decide–act loop for one concrete task: <em>\"Go to a public library catalogue site and check whether a specific book is available.\"</em> Write out, step by step, what the agent sees and what single action it takes each turn — for example: turn 1 sees the homepage, action = type the title into the search box; turn 2 sees results, action = click the matching book; turn 3 sees the book page, action = read the availability status; done. Note where the loop checks 'is the goal met yet?'</p><p><strong>Stretch goal:</strong> Add three guardrails to your design: a maximum step count, a rule for what the agent does if the same page appears twice in a row (it's stuck), and a human-approval checkpoint — then explain which one real-world failure each guardrail prevents.</p>",
  "solution": "<p>A clean loop looks like: <code>snapshot → model picks one action → browser does it → snapshot again → is goal met?</code> repeated until done. For the library task: (1) homepage → type title in search; (2) results list → click the matching title; (3) book page → read 'Available / On loan'; goal met, stop and report. The key insight is that the agent re-snapshots <em>after</em> every action, so it reacts to the real new page rather than what it assumed would appear. Guardrails: a step cap (say 15) stops infinite loops that burn tokens; a 'same page twice → stuck' rule catches a button that does nothing so the agent asks for help instead of spinning; and a human-approval checkpoint before any irreversible click (placing a hold, paying a fine) prevents the agent from taking a real, costly action on a wrong guess. Together they turn a clumsy intern into a safe one.</p>",
  "summary": "<p>A browser agent is an AI that drives a real web browser like a person — useful precisely when a site has no API. It runs a perceive–decide–act loop: look at the page (as a screenshot for a VLM, or as a labelled text outline), pick one action (click/type/scroll/navigate via tool calling), do it, then look again. That re-checking is what lets it adapt where rigid scripts break. As of 2026 they are powerful but flaky and slow, so cap the steps, prefer element IDs over pixels, and keep a human in the loop for anything irreversible.</p>",
  "keyTerms": [
    { "term": "Browser agent", "def": "An AI agent that controls a real web browser — clicking, typing, scrolling, navigating — to complete tasks on websites the way a human would." },
    { "term": "Perceive–decide–act loop", "def": "The core cycle of an agent: capture the current page state, send it to the model for one action, perform that action, then capture the page again and repeat." },
    { "term": "API", "def": "Application Programming Interface — a structured 'side door' a service exposes so programs can talk to it directly. Browser agents are for sites that have no such door." },
    { "term": "Accessibility tree", "def": "A cleaned-up text outline of a web page listing its interactive elements (buttons, links, fields), often with ID numbers, given to the model instead of raw HTML or pixels." },
    { "term": "VLM (Vision-Language Model)", "def": "A model that can read images as well as text, letting a browser agent literally 'look at' a screenshot of the page to decide what to click." },
    { "term": "Computer use", "def": "The broader capability (and the name some providers use) for an AI controlling a screen, mouse, and keyboard — driving a browser is the most common form." },
    { "term": "Headless browser", "def": "A real browser running without a visible window, controlled by code (e.g. via Playwright). Browser agents usually drive one of these behind the scenes." },
    { "term": "Guardrail", "def": "A safety limit on an agent — a step cap, a stuck-detector, or a human-approval gate before irreversible actions like payments or deletions." },
    { "term": "Element ID", "def": "A stable label attached to a page element so the agent can say 'click element 14' instead of guessing fragile pixel coordinates." }
  ],
  "quiz": [
    {
      "q": "When is a browser agent the right tool, rather than a normal API call or a recorded macro?",
      "options": [
        "When the website exposes a clean, documented API",
        "When the task lives only behind a human web page with no API and the layout may change",
        "When you need the exact same clicks replayed on a stable site every time",
        "When you need the fastest possible response time"
      ],
      "answer": 1,
      "explain": "Browser agents shine precisely when there is no API and the page can shift, because they re-read the page and adapt. If an API exists or a macro suffices, those are cheaper and faster."
    },
    {
      "q": "Why is sending the model a labelled accessibility tree usually better than raw pixel coordinates?",
      "options": [
        "It looks nicer to the human watching",
        "Coordinates are illegal in most browsers",
        "Element IDs are stable, so 'click element 14' survives resizes and small layout shifts",
        "The accessibility tree always uses fewer tokens than a screenshot of any page"
      ],
      "answer": 2,
      "explain": "Pixel coordinates are fragile — they miss when screens resize or layouts move. A stable element ID lets the action target the right control regardless of where it sits."
    },
    {
      "q": "What single design choice most distinguishes a browser agent from a brittle automation script?",
      "options": [
        "It re-snapshots the page after every action and re-decides",
        "It runs faster than a hard-coded script",
        "It never needs a model call",
        "It memorizes the exact pixel of every button up front"
      ],
      "answer": 0,
      "explain": "Looking again after each action is the whole idea — like a satnav recalculating, the agent reacts to the real new page instead of replaying fixed steps that break when a button moves."
    },
    {
      "q": "Your agent must place a real order that charges a credit card. What is the safest pattern?",
      "options": [
        "Let it run fully autonomously to save time",
        "Remove the step cap so it never gives up before paying",
        "Gate the irreversible payment behind a human-approval checkpoint",
        "Feed it the full raw HTML so it has maximum context"
      ],
      "answer": 2,
      "explain": "A wrong click can spend real money, so any irreversible action should require human approval. Removing guardrails or dumping raw HTML makes failures more likely, not less."
    }
  ]
}
