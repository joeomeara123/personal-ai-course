{
  "id": "model-serving",
  "order": 34,
  "section": "Inference & Optimization",
  "title": "Model Serving",
  "lesson": "<p><strong>Model serving</strong> is the work of turning a trained model sitting in a folder into a live service that real people and apps can hit, around the clock, fast, and without falling over. Training builds the brain; serving is the front desk, the phone line, and the whole reception team that lets the world talk to that brain. A model file alone does nothing useful — someone has to load it onto a GPU, keep it warm, accept incoming requests, run them, and hand back the answer. That is the job of a <strong>serving engine</strong> (a program whose only purpose is to run a model efficiently for many callers at once).</p>\n<h4>Analogy 1: a restaurant kitchen</h4>\n<p>Imagine the model is a brilliant chef. Serving is everything <em>around</em> the chef that turns one cook into a working restaurant: waiters taking orders (the API layer), a ticket rail where orders queue up, a system that groups several orders so the chef cooks them together instead of one plate at a time (this grouping is <a class=\"xref\" href=\"#batch-inference\">batching</a>), and a manager who hires a second chef when the dining room fills up (autoscaling). The chef's skill never changes — but a great kitchen can serve 500 covers a night while a bad one serves 20 and burns half of them.</p>\n<h4>What actually happens on each request</h4>\n<p>A user sends some text. The serving engine turns it into <a class=\"xref\" href=\"#tokens\">tokens</a> (the little chunks a model reads), runs them through the model on the GPU, and streams back generated tokens one by one. The slow, expensive part is generation, because the model produces output one token at a time and must re-read the conversation each step. Serving engines exist to make that loop cheap. The two numbers everyone watches are <strong>latency</strong> (how long one user waits) and <strong>throughput</strong> (total tokens served per second across everyone). These pull against each other — see <a class=\"xref\" href=\"#latency-vs-quality\">latency vs quality tradeoffs</a>.</p>\n<h4>The tricks a serving engine uses</h4>\n<ul><li><strong>Continuous batching:</strong> instead of waiting for a fixed group, the engine slots new requests into the running batch the moment a slot frees up. Like a chef who adds a new ticket to the pan the second a dish leaves it, rather than waiting for the whole batch to finish.</li><li><strong>KV cache management:</strong> the model remembers earlier tokens in a <a class=\"xref\" href=\"#kv-cache\">KV cache</a> so it does not re-compute them. The engine packs many users' caches into GPU memory cleverly (vLLM's famous <em>PagedAttention</em> does this like an operating system managing RAM).</li><li><strong>Fast kernels:</strong> optimised math like <a class=\"xref\" href=\"#flash-attention\">Flash Attention</a> and tricks from <a class=\"xref\" href=\"#inference-optimization\">inference optimization</a> squeeze more out of each GPU.</li><li><strong>Quantized weights:</strong> shipping the model in fewer bits via <a class=\"xref\" href=\"#quantization\">quantization</a> so it fits and runs faster.</li></ul>\n<h4>Analogy 2: a single laptop vs a power grid</h4>\n<p>Running a model on your own machine — see <a class=\"xref\" href=\"#local-inference\">local inference</a> with tools like <a class=\"xref\" href=\"#ollama\">Ollama</a> — is like having a generator in your garage: great for you, but it cannot light a whole city. Production serving is the power grid: many generators (GPUs), load balancers that route demand, spare capacity for the evening spike, and a way to add plants when the city grows. The same model can sit in both; the difference is everything wrapped around it.</p>\n<h4>What you choose when serving</h4>\n<p>For open-weight models, <a class=\"xref\" href=\"#vllm\">vLLM</a> is the most common high-throughput engine in 2026; others include TGI and SGLang. You run it behind an <a class=\"xref\" href=\"#api-serving\">API</a> so apps talk to it over HTTP, usually in an OpenAI-compatible shape so client code is portable. Starting a vLLM server is roughly one command:</p>\n<pre><code class=\"lang-bash\">vllm serve meta-llama/Llama-3.1-8B-Instruct \\\n  --max-model-len 8192 \\\n  --gpu-memory-utilization 0.90 \\\n  --port 8000</code></pre>\n<p>Then any client hits it like a normal chat API:</p>\n<pre><code class=\"lang-python\">from openai import OpenAI\nclient = OpenAI(base_url=\"http://localhost:8000/v1\", api_key=\"x\")\nresp = client.chat.completions.create(\n    model=\"meta-llama/Llama-3.1-8B-Instruct\",\n    messages=[{\"role\": \"user\", \"content\": \"Say hi in 5 words.\"}],\n    stream=True,            # stream tokens as they are made\n)\nfor chunk in resp:\n    print(chunk.choices[0].delta.content or \"\", end=\"\")</code></pre>\n<p>The big decision is <strong>build vs buy</strong>. You can rent <a class=\"xref\" href=\"#cloud-gpus\">cloud GPUs</a> and run vLLM yourself (full control, you manage scaling and on-call), or call a hosted API and let someone else serve (no ops, but per-token cost and less control). Either way the same ideas apply: keep the GPU busy, batch aggressively, watch <a class=\"xref\" href=\"#vram-basics\">VRAM</a>, and measure with real <a class=\"xref\" href=\"#speed-benchmarking\">speed benchmarks</a>.</p>\n<p class=\"callout tip\">💡 A handy mental split: <em>time-to-first-token</em> (how fast the reply <em>starts</em>, which users feel most) versus <em>tokens-per-second</em> (how fast it keeps flowing). Streaming makes a slow model <em>feel</em> fast by showing words as they appear.</p>",
  "mentalModel": "The model is a chef; serving is the whole restaurant around it — waiters (the API), a ticket rail (the request queue), grouping orders to cook together (batching), and a manager who hires more chefs at the dinner rush (autoscaling). Same chef, vastly different number of happy diners.",
  "mistakes": [
    "<strong>Confusing serving with training.</strong> Training teaches the model and is a one-off heavy job; serving runs the finished model for users, forever, and is judged on latency, throughput, and uptime — completely different concerns and hardware sizing.",
    "<strong>Running one request at a time on an expensive GPU.</strong> A naive loop that handles a single user before starting the next leaves the GPU mostly idle. Without <a class=\"xref\" href=\"#batch-inference\">batching</a> you might use 5% of a card you are paying full price for. Use a real serving engine.",
    "<strong>Ignoring time-to-first-token and not streaming.</strong> Users judge speed by when words start appearing, not total time. A reply that streams in feels snappy; the same reply delivered in one silent lump after 8 seconds feels broken.",
    "<strong>Forgetting the KV cache eats VRAM.</strong> Every concurrent user's conversation is stored in the <a class=\"xref\" href=\"#kv-cache\">KV cache</a>. Long contexts and many users can blow past <a class=\"xref\" href=\"#vram-basics\">VRAM</a> and crash the server, even though the model weights fit fine. Cap <code>max-model-len</code> and concurrency.",
    "<strong>Sizing for average load, not peak.</strong> If you provision for the typical hour, the evening spike will queue requests and latency explodes. Plan headroom or set up autoscaling so extra GPUs spin up before users feel it.",
    "<strong>Skipping a real load test.</strong> 'It worked when I tried it' tells you nothing about 200 simultaneous users. Always measure throughput and tail latency under realistic concurrency with proper <a class=\"xref\" href=\"#speed-benchmarking\">speed benchmarking</a> before launch."
  ],
  "exercise": "<p>Spin up a serving engine and feel the difference batching makes. On a machine or rented <a class=\"xref\" href=\"#cloud-gpus\">cloud GPU</a>, start a small model with <code>vllm serve</code> (use a 1B–8B instruct model). Hit it with a single streamed request from the OpenAI client and note the time-to-first-token. Then fire 20 requests at the same time (use a quick script with <code>asyncio</code> or even 20 parallel <code>curl</code> calls) and record total tokens-per-second across all of them. Compare the throughput of 1 request vs 20 — you should see total throughput rise sharply because the engine is batching them onto the GPU together.</p><p><strong>Stretch goal:</strong> Lower <code>--gpu-memory-utilization</code> or raise <code>--max-model-len</code> until the server refuses or crashes under load, and explain in one sentence why the <a class=\"xref\" href=\"#kv-cache\">KV cache</a>, not the model weights, was the thing that ran out of room.</p>",
  "solution": "<p><strong>Hint:</strong> Single-request throughput is limited by one user's token stream, but the GPU is mostly idle during it. When you send 20 at once, continuous batching packs them into the same forward passes, so total tokens-per-second climbs far above 20&times; nothing — often several-fold over the single-request number — while each individual user's latency rises only modestly. For the stretch goal: the weights are a fixed cost loaded once, but every concurrent conversation needs its own slice of KV cache, and that cost grows with both context length and number of users. Push <code>max-model-len</code> or concurrency high enough and the cache outgrows free VRAM, so the engine either rejects requests or the process is killed — even though the model itself fit comfortably.</p>",
  "summary": "<p>Model serving is everything around a trained model that turns it into a fast, reliable, multi-user service: an API, a request queue, batching, KV-cache management, and scaling. The goals are low latency (one user's wait) and high throughput (total tokens served), and the main job is keeping the expensive GPU busy. In 2026 the default open-model engine is vLLM behind an OpenAI-compatible API; the core choice is run-it-yourself on cloud GPUs versus call a hosted endpoint.</p>",
  "keyTerms": [
    { "term": "Model serving", "def": "The job of running a trained model as a live service that accepts requests from apps and users and returns answers fast and reliably." },
    { "term": "Serving engine", "def": "A program built only to run a model efficiently for many callers at once, handling batching, the KV cache, and GPU memory. vLLM, TGI, and SGLang are examples." },
    { "term": "Throughput", "def": "Total work served per second — usually measured in tokens per second across all users combined. High throughput means the GPU is being used well." },
    { "term": "Latency", "def": "How long one user waits. Often split into time-to-first-token (when the reply starts) and tokens-per-second (how fast it then flows)." },
    { "term": "Continuous batching", "def": "Slotting new requests into a batch already running on the GPU as soon as a slot frees, instead of waiting for a fixed group — keeps the GPU full." },
    { "term": "Time-to-first-token", "def": "The delay before the very first output token appears. It is what users feel most, and streaming hides slowness by showing words as they are made." },
    { "term": "Streaming", "def": "Sending output tokens to the client one at a time as the model produces them, so the answer appears to type itself instead of arriving in one lump." },
    { "term": "Autoscaling", "def": "Automatically adding or removing GPUs (servers) as demand rises and falls, so you have enough capacity at peak without paying for idle machines off-peak." },
    { "term": "OpenAI-compatible API", "def": "Exposing your own server using the same request and response shape as OpenAI's API, so existing client code works against it with only a changed base URL." }
  ]
}
