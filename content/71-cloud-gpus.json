{
  "id": "cloud-gpus",
  "order": 71,
  "section": "Deployment",
  "title": "Cloud GPUs",
  "lesson": "<p>A <strong>cloud GPU</strong> is exactly what it sounds like: a powerful graphics chip that lives in someone else's data centre, which you rent by the hour over the internet instead of buying. You never see the metal. You ask a provider for, say, one H100 with 80GB of memory, and minutes later you have a remote machine with that chip plugged in, ready to run your code. When you're done, you switch it off and stop paying.</p>\n<p>This lesson is about <em>how</em> to rent wisely — because the difference between a smart rental and a careless one is often the difference between a $40 bill and a $4,000 one.</p>\n<h4>Why rent instead of buy?</h4>\n<p>Think of it like a wedding. You could buy a marquee, a hundred chairs, and a sound system — or you could hire them for the one weekend you need them. A data-centre <a class=\"xref\" href=\"#gpu-basics\">GPU</a> like an H100 or the newer B200 can cost $30,000+ to buy, plus power and cooling.</p>\n<p>Most people need that horsepower only in bursts: a weekend of <a class=\"xref\" href=\"#fine-tuning-basics\">fine-tuning</a>, or a few hours of heavy <a class=\"xref\" href=\"#batch-inference\">batch work</a>. Renting turns a huge up-front purchase into a small, pay-as-you-go cost. The flip side: the meter is <em>always running</em>, so an idle rented GPU quietly drains money like a taxi stuck in traffic with the fare still ticking.</p>\n<h4>The three flavours of cloud GPU</h4>\n<p>Providers sell roughly the same hardware in three different shapes, and picking the wrong shape is the most common beginner mistake:</p>\n<table class=\"cmp\"><thead><tr><th>Type</th><th>What it is &amp; when to use it</th></tr></thead><tbody><tr><td><strong>Raw VM / bare GPU</strong></td><td>A whole machine with a GPU attached (e.g. from RunPod, Lambda, Vast.ai, or the big clouds AWS/GCP/Azure). You install everything. Best for fine-tuning and full control.</td></tr><tr><td><strong>Serverless GPU</strong></td><td>You hand over a container; it spins a GPU up only when a request arrives, then off again (e.g. Modal, Replicate, RunPod Serverless). Best for spiky, occasional <a class=\"xref\" href=\"#api-serving\">API</a> traffic — you pay near-zero when idle.</td></tr><tr><td><strong>Managed model API</strong></td><td>You don't touch a GPU at all; you call an endpoint and pay per token (e.g. Together, Fireworks, Baseten). Easiest, but least control — see <a class=\"xref\" href=\"#cost-per-token\">cost-per-token</a>.</td></tr></tbody></table>\n<h4>On-demand vs spot: the cheap-but-evictable seat</h4>\n<p>Here's the second analogy. Booking a GPU comes in two fares, just like flights. <strong>On-demand</strong> is a full-price ticket: it's guaranteed, you keep it as long as you pay. <strong>Spot</strong> (also called preemptible) is a standby seat at a steep discount — often 50-80% cheaper — but the airline can bump you the moment a full-fare passenger shows up.</p>\n<p>With spot GPUs the provider can yank the machine away with seconds of warning. That's fine for fault-tolerant work that saves <a class=\"xref\" href=\"#model-checkpoints\">checkpoints</a> often (so you just resume), and a disaster for a live service that can't blink out.</p>\n<h4>The number that decides everything: does it fit in VRAM?</h4>\n<p>Before renting, your first question is the same as always: will the model fit in the GPU's <a class=\"xref\" href=\"#vram-basics\">VRAM</a> (the chip's own dedicated memory)? A rough rule for <em>running</em> a model:</p>\n<ul><li>Multiply parameter count in billions by <strong>2</strong> for 16-bit weights.</li><li>By <strong>~1</strong> for 8-bit, <strong>~0.5</strong> for 4-bit.</li><li>Then add headroom for the <a class=\"xref\" href=\"#kv-cache\">KV cache</a> and overhead.</li></ul>\n<p>So a 70B model in 16-bit needs ~140GB — two 80GB cards. Quantize it to 4-bit (see <a class=\"xref\" href=\"#quantization\">Quantization</a>) and it drops to ~35GB, fitting on a single card. Sizing the GPU to the job is how you avoid renting an expensive monster you don't need.</p>\n<p>Most rentals are driven by a command line. Spinning up a serverless GPU function can be as short as this:</p>\n<pre><code class=\"lang-python\">import modal\napp = modal.App(\"my-llm\")\n\n# Ask for one A100 (80GB) only while a request is running\n@app.function(gpu=\"A100-80GB\", timeout=600)\ndef generate(prompt: str) -&gt; str:\n    from vllm import LLM\n    llm = LLM(\"meta-llama/Llama-3.1-8B-Instruct\")\n    return llm.generate(prompt)[0].outputs[0].text</code></pre>\n<p>And once you have a raw rented box, you confirm the GPU is really there before doing anything else:</p>\n<pre><code class=\"lang-bash\"># Lists the rented GPU, its total/used VRAM and live utilization\nnvidia-smi\n# A 0% utilization here while you're billed is money burning</code></pre>\n<h4>The landscape in 2026</h4>\n<p>The biggest hidden costs are rarely the GPU hours themselves. <strong>Egress</strong> (data-transfer fees for pulling files <em>out</em> of a cloud) and <strong>storage</strong> for fat model weights can dominate a bill.</p>\n<p>Cold starts bite too: a serverless GPU may take 20-60 seconds to load a large model into VRAM on the first request, which feels broken to a waiting user unless you keep one instance warm.</p>\n<p class=\"callout tip\">💡 The headline to keep: a cloud GPU is a rented taxi with the meter always running. Size it to the model with the VRAM math, pick the right flavour and fare for your workload, save checkpoints if you go spot, and — above all — <em>turn it off</em> when you stop using it.</p>",
  "mentalModel": "A cloud GPU is a rented taxi with the meter always running. You hop in (spin it up), pay only while it moves, and — crucially — you must remember to get out (shut it down), or the fare keeps climbing all night while you sleep.",
  "takeaways": [
    "A cloud GPU is a chip you rent by the hour — the meter never stops.",
    "Pick the shape (raw VM, serverless, managed API) and fare (on-demand vs spot) for the job.",
    "Do the VRAM math first, then turn the GPU off the second you're idle."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"Choosing a cloud GPU shape and fare by workload\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"34\" text-anchor=\"middle\">Match the workload to the rental</text><rect class=\"dg-box dg-box-accent\" x=\"290\" y=\"62\" width=\"140\" height=\"56\" rx=\"12\"/><text class=\"dg-label\" x=\"360\" y=\"86\" text-anchor=\"middle\">Your workload</text><text class=\"dg-sub\" x=\"360\" y=\"104\" text-anchor=\"middle\">what + how often?</text><line class=\"dg-edge\" x1=\"330\" y1=\"118\" x2=\"150\" y2=\"176\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"360\" y1=\"118\" x2=\"360\" y2=\"176\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"390\" y1=\"118\" x2=\"570\" y2=\"176\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box\" x=\"40\" y=\"180\" width=\"200\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"140\" y=\"206\" text-anchor=\"middle\">Raw VM</text><text class=\"dg-sub\" x=\"140\" y=\"226\" text-anchor=\"middle\">fine-tuning, full control</text><rect class=\"dg-box\" x=\"260\" y=\"180\" width=\"200\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"360\" y=\"206\" text-anchor=\"middle\">Serverless</text><text class=\"dg-sub\" x=\"360\" y=\"226\" text-anchor=\"middle\">spiky API traffic</text><rect class=\"dg-box\" x=\"480\" y=\"180\" width=\"200\" height=\"66\" rx=\"12\"/><text class=\"dg-label\" x=\"580\" y=\"206\" text-anchor=\"middle\">Managed API</text><text class=\"dg-sub\" x=\"580\" y=\"226\" text-anchor=\"middle\">pay per token, zero ops</text><rect class=\"dg-box dg-box-good\" x=\"110\" y=\"280\" width=\"220\" height=\"40\" rx=\"12\"/><text class=\"dg-label\" x=\"220\" y=\"305\" text-anchor=\"middle\">Spot: cheap, evictable</text><rect class=\"dg-box dg-box-warn\" x=\"390\" y=\"280\" width=\"220\" height=\"40\" rx=\"12\"/><text class=\"dg-label\" x=\"500\" y=\"305\" text-anchor=\"middle\">On-demand: reliable</text></svg>",
    "caption": "Start from your workload, pick a rental shape, then choose a spot or on-demand fare to match it."
  },
  "mistakes": [
    "<strong>Leaving an idle GPU running.</strong> The single most expensive mistake. A rented 8x-H100 box left on overnight by accident can cost more than a month's rent. The meter doesn't care whether you're using it — set auto-shutdown timers and check <code>nvidia-smi</code> for 0% utilization while you're being billed.",
    "<strong>Renting the biggest card 'to be safe'.</strong> An 80GB H100 for an 8B model that fits comfortably on a 24GB card just burns money. Do the <a class=\"xref\" href=\"#vram-basics\">VRAM</a> math first, <a class=\"xref\" href=\"#quantization\">quantize</a> if it helps, then pick the smallest card that fits with headroom.",
    "<strong>Using spot instances for a live service.</strong> Spot GPUs are 50-80% cheaper but can be evicted with seconds of notice. Perfect for fault-tolerant training that saves <a class=\"xref\" href=\"#model-checkpoints\">checkpoints</a>; catastrophic for a user-facing <a class=\"xref\" href=\"#api-serving\">API</a> that suddenly goes dark mid-request.",
    "<strong>Ignoring egress and storage fees.</strong> Beginners budget for GPU-hours and get blindsided by data-transfer charges for pulling weights out, plus storage for the model files sitting around. On big clouds, egress alone can rival the compute cost — keep data and compute in the same region.",
    "<strong>Forgetting cold starts on serverless.</strong> A serverless GPU loads a large model from disk into VRAM on the first request, often 20-60 seconds. Users assume it's broken. Keep one instance 'warm', or pick a smaller model, if low latency matters — this is a core <a class=\"xref\" href=\"#latency-vs-quality\">latency tradeoff</a>.",
    "<strong>Assuming any GPU 'just works'.</strong> Cheap marketplace GPUs may have mismatched CUDA drivers, slow disks, or no fast interconnect between multiple cards (which matters for big models). Run <code>nvidia-smi</code> and a quick test load before trusting a box for a long, expensive job."
  ],
  "exercise": "<p>Pick one serverless or pay-as-you-go provider with a free tier or small credit (RunPod, Modal, and Replicate are common starting points). Spin up a single small GPU and run <code>nvidia-smi</code> to confirm the card, its total <a class=\"xref\" href=\"#vram-basics\">VRAM</a>, and that utilization starts near 0%. Then load a small model (a 1-8B parameter model from <a class=\"xref\" href=\"#hugging-face\">Hugging Face</a>) and generate one short reply. Finally — and this is the real lesson — <strong>shut the GPU down</strong> and confirm in the dashboard that billing has stopped.</p><p><strong>Stretch goal:</strong> Take a 7-8B model and work out, on paper, the VRAM it needs at 16-bit, 8-bit, and 4-bit using the lesson's rule (billions of params times 2, 1, and 0.5, plus headroom). Then deliberately rent the smallest card each version fits on, time the cold start (first request) versus a warm request, and compare the hourly price. You'll feel the three real levers of cloud cost — <a class=\"xref\" href=\"#quantization\">quantization</a>, card size, and cold starts — in one sitting.</p>",
  "solution": "<p>On the small model, <code>nvidia-smi</code> should show the model's weights occupying VRAM after loading, and utilization spiking only while generating — idle the rest of the time, which is exactly why serverless saves money. After you click shut down, the provider dashboard should show the instance stopped and the hourly charge ending; if it still says 'running', you're still paying, which is the whole point of the exercise. For the stretch goal, expect roughly 14-16GB at 16-bit, 7-9GB at 8-bit, and 4-5GB at 4-bit for a 7-8B model (the extra is KV cache and overhead) — so the 4-bit version fits a cheap 8-12GB card while the 16-bit version needs a 24GB one. The cold start should be clearly slower than warm requests because the weights must be read from disk into VRAM first; serving engines like <a class=\"xref\" href=\"#vllm\">vLLM</a> and keeping an instance warm are the usual fixes.</p>",
  "summary": "<p>A cloud GPU is a powerful chip you rent by the hour in someone else's data centre, with the meter always running — so the golden rule is to size it to the job and switch it off when idle. Choose the right shape (raw VM for control, serverless for spiky traffic, managed API for zero ops) and the right fare (on-demand for reliability, cheaper spot for fault-tolerant work that checkpoints). Always start from the VRAM math: does the model fit, and can quantization shrink it onto a cheaper card? Watch out for hidden egress, storage, and cold-start costs.</p>",
  "keyTerms": [
    { "term": "Cloud GPU", "def": "A powerful graphics chip in a remote data centre that you rent by the hour over the internet instead of buying and hosting it yourself." },
    { "term": "On-demand instance", "def": "A full-price GPU rental that is guaranteed and stays yours as long as you pay. Reliable but the most expensive per hour." },
    { "term": "Spot / preemptible instance", "def": "A heavily discounted GPU (often 50-80% off) that the provider can reclaim with little warning. Great for fault-tolerant jobs that checkpoint, risky for live services." },
    { "term": "Serverless GPU", "def": "A model where a GPU spins up only while a request runs and off again afterward, so you pay almost nothing when idle. Trade-off: slow cold starts." },
    { "term": "Cold start", "def": "The delay (often 20-60s) when a fresh GPU must load a large model from disk into VRAM before it can answer the first request." },
    { "term": "Egress", "def": "Fees a cloud charges for moving data out of its network. Often a surprise line item that can rival GPU-hour costs for large model files." },
    { "term": "VRAM (video memory)", "def": "The GPU's own dedicated, fast memory where model weights and working data live. If a model doesn't fit in VRAM, it won't run normally." },
    { "term": "Bare metal / raw VM", "def": "A rented machine with a GPU where you install and control everything yourself — most flexible, best for full training or fine-tuning runs." }
  ],
  "quiz": [
    {
      "q": "You're running a small internal tool that gets a burst of requests for an hour each morning and is silent the rest of the day. Which cloud GPU shape fits best?",
      "options": [
        "A 24/7 on-demand raw VM kept running all day",
        "A serverless GPU that spins up per request and idles to near-zero cost",
        "A spot instance left running continuously",
        "Buying a physical GPU for the office"
      ],
      "answer": 1,
      "explain": "Spiky, occasional traffic is the ideal case for serverless: you pay almost nothing during the long idle stretches and only spin a GPU up when a request actually arrives."
    },
    {
      "q": "You want to fine-tune a 70B model in 16-bit precision. Roughly how much VRAM do the weights alone need, before headroom?",
      "options": [
        "About 35GB",
        "About 70GB",
        "About 140GB",
        "About 280GB"
      ],
      "answer": 2,
      "explain": "The rough rule is billions of parameters times 2 for 16-bit weights: 70 × 2 ≈ 140GB, which means two 80GB cards. Quantizing to 4-bit (×0.5) would cut it to ~35GB."
    },
    {
      "q": "A teammate runs a customer-facing chatbot API on a spot (preemptible) GPU to save money. What is the main risk?",
      "options": [
        "Spot GPUs are slower per token than on-demand ones",
        "The provider can evict the GPU with seconds of notice, taking the service offline mid-request",
        "Spot GPUs have less VRAM than on-demand ones",
        "Spot pricing is actually more expensive for live traffic"
      ],
      "answer": 1,
      "explain": "Spot/preemptible instances trade reliability for a steep discount: the provider can reclaim them at any moment, which is fine for checkpointed training but breaks a service that must stay live."
    },
    {
      "q": "Your monthly cloud bill is far higher than the GPU-hours you expected to pay for. Which hidden cost is the most likely culprit?",
      "options": [
        "The cost of running nvidia-smi too often",
        "Egress (data-transfer-out) and storage fees for large model weights",
        "A surcharge for using on-demand instead of spot",
        "Paying per token on a raw VM"
      ],
      "answer": 1,
      "explain": "Egress (pulling data out of the cloud) and storing fat model files are classic surprise line items that can rival or exceed compute cost; keeping data and compute in the same region helps. Per-token billing only applies to managed APIs, not raw VMs."
    }
  ]
}
