{
  "id": "instruction-tuning",
  "order": 13,
  "section": "Datasets & Training",
  "title": "Instruction Tuning",
  "lesson": "<p><strong>Instruction tuning</strong> is the step that turns a clever-but-clueless text predictor into something that actually <em>follows your instructions</em>. It is the difference between a model that mumbles plausible text and one that hears \"Summarise this email in two sentences\" and does exactly that.</p>\n<h4>Why a raw model ignores you</h4>\n<p>A model fresh out of <a class=\"xref\" href=\"#training-vs-inference\">pretraining</a> has only learned one game: <em>guess the next word</em> across a huge pile of internet text. That is it.</p>\n<p>So if you type \"List three uses for a paperclip,\" it might happily continue with \"List five uses for a rubber band, List ten uses for...\" because on the internet, lines like that often come in clusters. It is not being difficult. It genuinely does not know that text-after-a-question is supposed to be an <em>answer</em>. It just completes patterns.</p>\n<p>Think of a brilliant chef who has read every cookbook ever written but never worked in a kitchen with customers. Ask for \"a quick pasta dish\" and they might recite the history of pasta, or start a new recipe mid-sentence. The raw talent is all there. What is missing is the habit of <em>treating a request as a request</em>. Instruction tuning is the apprenticeship that builds that habit.</p>\n<h4>How it works: examples of being helpful</h4>\n<p>Instruction tuning is a flavour of <a class=\"xref\" href=\"#fine-tuning-basics\">fine-tuning</a> done with a special kind of data: thousands of <em>instruction → response</em> pairs. Each example shows a task and a good completion of that task.</p>\n<p>We keep using the same next-word-prediction training, but now every example teaches one pattern: \"when you see an instruction, produce a helpful answer.\" These collections are exactly what the <a class=\"xref\" href=\"#sft-datasets\">SFT Datasets</a> topic covers (SFT means <em>supervised fine-tuning</em> — \"supervised\" just means we show the model the correct answers).</p>\n<p>A second analogy: it is like a new hire who knows the whole industry but not <em>your office</em>. You hand them a binder of \"here's a typical request, here's how we handle it\" examples. After enough of them, they internalise the style and start handling new requests the same way. They have not gained new facts so much as a new <em>job description</em>.</p>\n<p>Crucially, the model generalises. Train on \"translate, summarise, rewrite, explain\" and it gets the broad idea of <em>follow the instruction</em> — so it can often handle a task you never put in the data. That generalisation is the whole magic.</p>\n<h4>What an example looks like</h4>\n<p>The data is usually structured so the model can tell which part is the human's instruction and which part is the reply it should learn to produce. A common format:</p>\n<pre><code class=\"lang-json\">{\n  \"instruction\": \"Rewrite this sentence to be more polite.\",\n  \"input\": \"Send me the report now.\",\n  \"output\": \"Could you please send me the report when you get a chance?\"\n}</code></pre>\n<p>During training these fields get wrapped in a <em>chat template</em> (fixed marker text, like <code>&lt;|user|&gt;</code> and <code>&lt;|assistant|&gt;</code>, that labels who is speaking). The model is only graded on predicting the <code>output</code> part — not the instruction. That detail matters: you want it to learn how to <em>answer</em>, not how to <em>echo the question</em>. See <a class=\"xref\" href=\"#dataset-formatting\">Dataset Formatting</a> for how those templates are built.</p>\n<h4>Where it sits in the pipeline</h4>\n<p>The modern recipe for a chat model is roughly three stages:</p>\n<ul><li><strong>Pretraining</strong> — learn language from raw text.</li><li><strong>Instruction tuning</strong> — learn to follow requests.</li><li><strong>Preference tuning</strong> — learn which answers humans actually <em>like</em>, via <a class=\"xref\" href=\"#preference-datasets\">Preference Datasets</a> and methods like <a class=\"xref\" href=\"#dpo\">DPO</a> or <a class=\"xref\" href=\"#rlhf\">RLHF</a>.</li></ul>\n<p>Instruction tuning is the bridge in the middle. It does not make the model <em>nicer</em> or <em>safer</em> on its own — it makes it <em>obedient to the task shape</em>. The polish comes later.</p>\n<table class=\"cmp\"><thead><tr><th>Base (pretrained) model</th><th>Instruction-tuned model</th></tr></thead><tbody><tr><td>Continues text by pattern</td><td>Answers the request</td></tr><tr><td>\"List three uses\" → more lists</td><td>\"List three uses\" → three uses</td></tr><tr><td>Needs clever prompting to behave</td><td>Behaves with plain requests</td></tr><tr><td>Great raw material, awkward to use</td><td>Ready for chat-style products</td></tr></tbody></table>\n<p>In practice you rarely instruction-tune a giant model from scratch yourself. You either use an already-tuned model (most \"-Instruct\" or \"-Chat\" models you download are post-this-step), or you do a <em>light</em> instruction-tune on top of one using a cheap method like <a class=\"xref\" href=\"#lora\">LoRA</a> to teach your own task style.</p>\n<p>Either way, knowing this step exists explains the single most common beginner confusion: \"why does the base model ignore my instructions?\" Because nobody taught it that instructions were special yet.</p>\n<p class=\"callout tip\">💡 Rule of thumb: a few thousand <em>high-quality, varied</em> instruction examples beat a million repetitive ones. Diversity of tasks teaches generalisation; sheer volume of the same thing teaches a rut.</p>",
  "mentalModel": "A genius chef who has read every cookbook but never served a customer. Instruction tuning is the apprenticeship that teaches one habit: when someone makes a request, treat it as a request and actually fulfil it.",
  "takeaways": [
    "It teaches the <strong>habit of answering</strong>, not new facts.",
    "Train on many varied instruction → response pairs; grade only the answer.",
    "Sits between pretraining and preference tuning (<a class=\"xref\" href=\"#dpo\">DPO</a>/<a class=\"xref\" href=\"#rlhf\">RLHF</a>)."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"Instruction tuning turns a rambling base model into one that answers\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"34\" text-anchor=\"middle\">Instruction tuning: from rambler to responder</text><rect class=\"dg-box\" x=\"40\" y=\"120\" width=\"170\" height=\"80\" rx=\"12\"/><text class=\"dg-label\" x=\"125\" y=\"150\" text-anchor=\"middle\">Base model</text><text class=\"dg-sub\" x=\"125\" y=\"172\" text-anchor=\"middle\">predicts next word</text><text class=\"dg-note\" x=\"125\" y=\"190\" text-anchor=\"middle\">\"List 3 uses\" → more lists</text><rect class=\"dg-box dg-box-accent\" x=\"275\" y=\"120\" width=\"170\" height=\"80\" rx=\"12\"/><text class=\"dg-label\" x=\"360\" y=\"150\" text-anchor=\"middle\">Instruction tuning</text><text class=\"dg-sub\" x=\"360\" y=\"172\" text-anchor=\"middle\">SFT on instruction →</text><text class=\"dg-sub\" x=\"360\" y=\"190\" text-anchor=\"middle\">response pairs</text><rect class=\"dg-box dg-box-good\" x=\"510\" y=\"120\" width=\"170\" height=\"80\" rx=\"12\"/><text class=\"dg-label\" x=\"595\" y=\"150\" text-anchor=\"middle\">Instruct model</text><text class=\"dg-sub\" x=\"595\" y=\"172\" text-anchor=\"middle\">answers the request</text><text class=\"dg-note\" x=\"595\" y=\"190\" text-anchor=\"middle\">\"List 3 uses\" → 3 uses</text><line class=\"dg-edge\" x1=\"210\" y1=\"160\" x2=\"267\" y2=\"160\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"445\" y1=\"160\" x2=\"502\" y2=\"160\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box dg-box-2\" x=\"230\" y=\"245\" width=\"260\" height=\"60\" rx=\"12\"/><text class=\"dg-label\" x=\"360\" y=\"272\" text-anchor=\"middle\">Loss masking</text><text class=\"dg-sub\" x=\"360\" y=\"292\" text-anchor=\"middle\">grade only the output, not the instruction</text><line class=\"dg-edge\" x1=\"360\" y1=\"200\" x2=\"360\" y2=\"243\" marker-end=\"url(#arrow)\"/></svg>",
    "caption": "A base model that rambles becomes an instruction-following model by training on instruction → response pairs, grading only the answer."
  },
  "quiz": [
    {
      "q": "Why does a fresh base model often ignore a plain instruction like \"List three uses for a paperclip\"?",
      "options": [
        "It was only trained to predict the next word, so it just continues the text pattern.",
        "It is too small to understand English grammar.",
        "Its safety filters block any request that sounds like a command.",
        "It ran out of context window before reaching the question."
      ],
      "answer": 0,
      "explain": "Pretraining only teaches next-word prediction, so the model continues patterns instead of treating text-after-a-question as an answer. Nobody has taught it that instructions are special yet."
    },
    {
      "q": "During instruction tuning, which part of each example is the model actually graded on?",
      "options": [
        "The whole sequence, instruction and answer equally",
        "Only the instruction, so it learns to recognise requests",
        "Only the output (the answer), with the instruction tokens masked",
        "A random half of the tokens each step"
      ],
      "answer": 2,
      "explain": "You mask the instruction and compute loss only on the output, so the model learns to produce answers rather than echo questions."
    },
    {
      "q": "A team needs their model to know yesterday's product prices. Is instruction tuning the right tool?",
      "options": [
        "Yes — instruction tuning is how you inject fresh facts.",
        "No — it teaches behaviour, not facts; use RAG or continued pretraining for fresh knowledge.",
        "Yes, but only if you use at least a million examples.",
        "No — only preference tuning can add new facts."
      ],
      "answer": 1,
      "explain": "Instruction tuning teaches the habit of answering, not new knowledge. Fresh, changing facts belong in RAG or continued pretraining."
    },
    {
      "q": "You have 2,000 instruction examples but they are almost all \"summarise this paragraph.\" What is the likely result?",
      "options": [
        "The model will generalise well to translation and rewriting too.",
        "The model learns one trick and struggles with task types it never saw.",
        "Training will fail because the dataset is too small.",
        "The model becomes safer and more polite across all tasks."
      ],
      "answer": 1,
      "explain": "Generalisation comes from variety of task types. A repetitive dataset teaches one narrow trick rather than the broad skill of following any instruction."
    }
  ],
  "mistakes": [
    "<strong>Confusing instruction tuning with teaching new facts.</strong> It mostly teaches <em>behaviour</em> — \"answer the task.\" If you need fresh, up-to-date knowledge, reach for <a class=\"xref\" href=\"#rag\">RAG</a> or <a class=\"xref\" href=\"#continued-pretraining\">Continued Pretraining</a>, not a handful of instruction pairs.",
    "<strong>Grading the model on the whole sequence, including the instruction.</strong> You should mask (ignore) the instruction tokens and only train on the <code>output</code>. Otherwise the model wastes effort learning to reproduce questions instead of answering them.",
    "<strong>Using a tiny, repetitive dataset.</strong> A thousand near-identical \"summarise this\" examples teaches one trick, not the general skill of following instructions. Variety of task types is what makes it generalise to unseen requests.",
    "<strong>Forgetting the chat template the model expects.</strong> Each model family has its own marker format. Train or prompt with the wrong template and quality drops sharply, even though nothing looks broken. Match the template the base model was tuned with — see <a class=\"xref\" href=\"#dataset-formatting\">Dataset Formatting</a>.",
    "<strong>Expecting it to make the model safe or polite.</strong> Instruction tuning makes a model <em>obey the task shape</em>. Tone, refusals, and human preference are a separate later step (<a class=\"xref\" href=\"#dpo\">DPO</a> / <a class=\"xref\" href=\"#rlhf\">RLHF</a>).",
    "<strong>Over-training and causing catastrophic forgetting.</strong> Hammer too many epochs (full passes over the data) and the model can lose general ability while it overfits your narrow examples. Use few epochs and watch a held-out validation set."
  ],
  "exercise": "<p>Grab a small <em>base</em> model (the non-instruct version, e.g. a 1B Llama or Qwen base) and a small instruction-tuned sibling of the same family. Give both the exact same plain prompt, like <code>\"Give me three tips for sleeping better.\"</code>, with no clever prompt engineering. Write down both outputs side by side. You should clearly see the base model drift or ramble, while the instruct version answers directly.</p><p><strong>Stretch goal:</strong> Build a tiny 30-example instruction dataset in the <code>{instruction, input, output}</code> format for one narrow task (say, turning messy notes into a clean bullet list), then LoRA-fine-tune the base model on it and re-run the same prompt to see the behaviour shift.</p>",
  "solution": "<p><strong>Hint:</strong> The base model will often continue your prompt rather than answer it — repeating the question, inventing a follow-up question, or starting a list and never finishing it. The instruct model gives a clean, direct three-tip answer because it was shown thousands of \"request → answer\" examples. For the stretch goal: wrap each example in the model's chat template, mask the instruction tokens so the loss is computed only on <code>output</code>, train with <a class=\"xref\" href=\"#lora\">LoRA</a> for just 1–3 epochs, and keep the learning rate low. Even 30 examples will visibly nudge the style, though you need far more variety for real generalisation.</p>",
  "summary": "<p>Instruction tuning fine-tunes a raw text-predictor on thousands of <em>instruction → response</em> pairs so it learns the habit of actually doing what it is asked. It teaches behaviour, not facts, and it generalises to new tasks. It sits between pretraining and preference tuning, and it is why every <em>-Instruct</em> or <em>-Chat</em> model feels usable while a base model just rambles.</p>",
  "keyTerms": [
    { "term": "Instruction tuning", "def": "Fine-tuning a model on instruction-and-response examples so it learns to follow requests instead of just continuing text." },
    { "term": "Base model", "def": "A model that has only been pretrained on raw text. It predicts the next word well but does not yet treat instructions as requests to answer." },
    { "term": "SFT (supervised fine-tuning)", "def": "Training a model by showing it correct input-output examples. 'Supervised' means the right answers are provided. Instruction tuning is a kind of SFT." },
    { "term": "Instruction → response pair", "def": "A single training example: a task (the instruction, sometimes with extra input) plus the desired answer (the output)." },
    { "term": "Chat template", "def": "Fixed marker text that labels who is speaking (user vs assistant), wrapped around examples so the model can tell instruction from answer." },
    { "term": "Loss masking", "def": "Ignoring the instruction tokens when scoring training, so the model is only graded on producing the answer, not on echoing the question." },
    { "term": "Generalisation", "def": "The ability to handle a task that was never in the training data, learned by seeing many varied instruction types." },
    { "term": "Catastrophic forgetting", "def": "When over-training on narrow data makes a model lose abilities it previously had. Avoided with few epochs and a validation set." },
    { "term": "Epoch", "def": "One full pass over the entire training dataset. Instruction tuning usually needs only a small number of them." }
  ]
}
