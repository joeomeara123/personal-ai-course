{
  "id": "coding-models",
  "order": 66,
  "section": "Model Types",
  "title": "Coding Models",
  "lesson": "<p>A <strong>coding model</strong> is a language model that has been specially trained and tuned to read, write, fix, and explain computer code. It's still a normal <a class=\"xref\" href=\"#llm-basics\">LLM</a> under the hood the same kind of next-word predictor but its diet and its homework were different. Think of two doctors who went to the same medical school. One then spent ten years only doing surgery. Both are doctors; you still want the surgeon holding the scalpel. A general model can write code; a coding model has spent its 'extra ten years' on it.</p>\n<h4>What makes a model good at code</h4>\n<p>Three things, mostly.</p>\n<p><strong>1. The training diet.</strong> During pretraining (the long first stage where a model reads a giant pile of text to learn patterns), coding models are fed huge amounts of source code from public repositories, plus documentation, bug reports, and pull requests. Then <a class=\"xref\" href=\"#instruction-tuning\">Instruction Tuning</a> (teaching the model to follow requests, not just continue text) is done with coding tasks: 'write a function that does X', 'find the bug', 'explain this snippet'. Many also use <a class=\"xref\" href=\"#rlhf\">RLHF</a> where the reward is partly <em>did the code actually run and pass tests</em>, not just 'did it look nice'. That last part matters enormously: you can <em>execute</em> code, so unlike an essay, correctness is checkable. A model can be trained against a hard, automatic judge a test suite that either passes or fails. Most other writing tasks have no such clean grader, which is one reason coding models improved so fast.</p>\n<p><strong>2. Fill-in-the-middle (FIM).</strong> Plain text models only predict what comes <em>next</em>. But when you code, the cursor is usually in the <em>middle</em> of a file there's code above and below. Coding models are trained on a special trick: hide a chunk in the middle and ask the model to fill it using both sides. Analogy: a normal model finishes your sentence; an FIM model is the friend who can fill the missing word in a Mad Libs page because they can see the whole sentence around the gap. This is what powers inline autocomplete in editors.</p>\n<p><strong>3. Long, code-aware context.</strong> Real code lives across many files. Coding models tend to ship with bigger <a class=\"xref\" href=\"#context-windows\">Context Windows</a> (the model's short-term memory, measured in tokens) so they can hold whole files and their imports at once.</p>\n<h4>Where you actually meet them</h4>\n<ul><li><strong>Editor autocomplete</strong> the grey ghost text that finishes your line as you type (FIM doing its job).</li><li><strong>Chat assistants for code</strong> 'why is this test failing?', 'refactor this to async'.</li><li><strong>Agents</strong> the brains inside an <a class=\"xref\" href=\"#building-copilots\">AI copilot</a> or coding agent that reads files, runs commands, and edits a whole repo. See <a class=\"xref\" href=\"#ai-coding-workflows\">AI Coding Workflows</a>.</li></ul>\n<p>In 2026, popular options include open models like Qwen-Coder, DeepSeek-Coder, and Codestral, alongside the strong general flagships (the big chat models) that are now excellent at code too. The line is blurring: many 'general' models are quietly coding-heavy, while dedicated coders win on speed and on cheap local autocomplete.</p>\n<h4>The fill-in-the-middle format, made concrete</h4>\n<p>Under the hood, FIM uses special marker tokens so the model knows which part is the gap. Simplified, a request looks like this:</p>\n<pre><code class=\"lang-text\">&lt;PRE&gt; def add(a, b):\n    &lt;SUF&gt;\n    return result &lt;MID&gt;</code></pre>\n<p><code>&lt;PRE&gt;</code> is the code before the cursor, <code>&lt;SUF&gt;</code> the code after, and <code>&lt;MID&gt;</code> is where the model writes the missing line (here, probably <code>result = a + b</code>). Your editor builds this automatically; you just see the suggestion appear.</p>\n<h4>Choosing one: a quick comparison</h4>\n<table class=\"cmp\"><thead><tr><th>General model</th><th>Dedicated coding model</th></tr></thead><tbody><tr><td>Great all-rounder; strong at code in chat</td><td>Tuned hard on code; best at FIM autocomplete</td></tr><tr><td>Often pricier / bigger</td><td>Comes in small sizes you can run locally</td></tr><tr><td>Best for reasoning across docs + code</td><td>Best for fast, in-editor completions</td></tr></tbody></table>\n<p>A common real-world setup uses <em>both</em>: a small, fast coding model for instant inline suggestions, and a big model for the hard 'think through the whole feature' chat.</p>\n<p class=\"callout warn\">⚠️ A coding model that writes syntactically perfect code is still guessing. It does not run your code in its head. It can invent a library function that doesn't exist (a <strong>hallucination</strong>). Always run it.</p>\n<p>The single biggest mindset shift: code from a model is a fast <em>first draft</em>, not a verified answer. You verify the same way you verify a junior developer's work run it, test it, read it. The model's superpower is speed and breadth; yours is judgement. Used that way an outline machine you steer, not an oracle you obey a good coding model is one of the most useful tools an engineer has ever had.</p>",
  "mentalModel": "A coding model is a general LLM that did a long apprenticeship in a code shop: it read mountains of source code, learned to fill gaps in the middle of a file (not just the end), and was rewarded when its code actually ran. Treat its output as a fast first draft from a tireless junior dev always run and test it.",
  "mistakes": [
    "<strong>Trusting code because it looks right.</strong> Perfect-looking syntax is not the same as correct behaviour. The model can't run the code in its head always execute and test it.",
    "<strong>Assuming a library or function exists.</strong> Coding models confidently call <code>some_helper()</code> that was never written or import a package that doesn't exist. Check imports and APIs against real docs.",
    "<strong>Using a chat model for autocomplete.</strong> Inline ghost-text needs fill-in-the-middle and low latency. A big chat-only model that can't do FIM (or is too slow) makes a sluggish, awkward autocomplete.",
    "<strong>Feeding too little context.</strong> Ask for a fix while showing only one function, and the model invents the missing pieces. Give it the imports, the types, and the related files when they matter.",
    "<strong>Ignoring the training cutoff.</strong> A model trained before a framework's new version will write old, deprecated patterns. For fresh libraries, paste current docs or use a tool that fetches them.",
    "<strong>Pasting secrets into prompts.</strong> API keys, passwords, and private code sent to a hosted coding model leave your machine. Strip secrets, or use a model you run locally."
  ],
  "exercise": "<p>Pick a small, real task in a language you know say, 'parse a CSV file and return the average of one column'. Ask a coding model (hosted chat or a local one like Qwen-Coder via <a class=\"xref\" href=\"#ollama\">Ollama</a>) to write it. Then actually run the code on a sample file. Note: did it run first try? Did it handle an empty file or a missing column? Now hand it a deliberately broken version and ask it to find and fix the bug, then run again to confirm.</p><p><strong>Stretch goal:</strong> Write three unit tests <em>first</em> (including one edge case, like a blank row), then ask the model to write code that passes all three. Run the tests. If any fail, paste the failure message back and let the model iterate. Count how many rounds it takes to go green this is exactly how coding agents work.</p>",
  "solution": "<p>You'll usually find the happy path works on the first try, but edge cases (empty file, missing column, blank rows) are where models slip they optimise for the obvious case unless you ask. That's why the test-first approach is so powerful: the tests pin down exactly what 'correct' means, so the model has a concrete target and a clear pass/fail signal. Pasting the real failure message back almost always fixes it within a round or two, because the model can now <em>see</em> what went wrong. Lesson: the value isn't 'model writes flawless code'; it's the tight loop of generate, run, read the error, regenerate which is just a faster version of how you'd debug yourself.</p>",
  "summary": "<p>A coding model is an LLM specialised for code: trained on source code, tuned on coding tasks, often rewarded for code that actually runs, and taught fill-in-the-middle so it can complete the gap your cursor sits in (not just the end of a line). Use a small fast coder for inline autocomplete and a big model for hard reasoning often both at once. Crucially, its output is a first draft: always run, test, and verify before you trust it.</p>",
  "keyTerms": [
    { "term": "Coding model", "def": "A language model trained and tuned specifically to read, write, fix, and explain code; still an LLM, just with a code-heavy education." },
    { "term": "Fill-in-the-middle (FIM)", "def": "A training format where the model fills a gap using both the code before AND after it not just predicting what comes next. Powers inline autocomplete." },
    { "term": "Autocomplete / ghost text", "def": "The grey suggested code your editor shows as you type, generated on the fly by a fast coding model using FIM." },
    { "term": "Pretraining diet", "def": "The pile of text a model first learns from. For coders it's heavy on source code, docs, bug reports, and pull requests." },
    { "term": "Hallucination", "def": "When a model invents something false but plausible such as a function or library that doesn't actually exist." },
    { "term": "Training cutoff", "def": "The date after which a model has seen no data, so it may write outdated, deprecated code for newer library versions." },
    { "term": "Test-driven loop", "def": "Writing tests first, then having the model generate code, running the tests, and feeding failures back the core loop of coding agents." },
    { "term": "Marker tokens", "def": "Special symbols (like &lt;PRE&gt;, &lt;SUF&gt;, &lt;MID&gt;) that tell an FIM model which part is the prefix, the suffix, and the gap to fill." }
  ]
}
