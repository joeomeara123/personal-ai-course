{
  "id": "vram-basics",
  "order": 37,
  "section": "Inference & Optimization",
  "title": "VRAM Basics",
  "lesson": "<p><strong>VRAM</strong> stands for <em>Video RAM</em> — the fast memory that lives right on a <a class=\"xref\" href=\"#gpu-basics\">GPU</a> (the chip that does the heavy maths for AI). When people ask \"will this model run on my machine?\", they're almost always really asking \"does it fit in VRAM?\" Get this one idea right and a huge amount of AI engineering stops feeling like magic and starts feeling like packing a suitcase.</p>\n<h4>Why VRAM and not normal RAM?</h4>\n<p>Your computer has two kinds of memory. There's ordinary system RAM, which the CPU uses, and there's VRAM, glued physically next to the GPU. The GPU can only crunch numbers it can reach in VRAM. Pulling data across from system RAM is like a chef who keeps the ingredients in a shed across the car park: technically possible, painfully slow.</p>\n<p>So the rule is blunt — <strong>if the model and its working data don't fit in VRAM, the GPU can't run it at full speed.</strong></p>\n<p>Think of VRAM as the chef's countertop, and the GPU's compute cores as the chef's hands. A bigger countertop doesn't make the chef's hands faster — it just lets them lay out more ingredients at once. That's the key split: <em>VRAM is space, the GPU cores are speed.</em> A 24 GB card and a 24 GB card with twice the cores hold the same model but cook at different rates.</p>\n<h4>The three things competing for the space</h4>\n<p>When a model is loaded for <a class=\"xref\" href=\"#training-vs-inference\">inference</a> (answering prompts), VRAM is shared between three tenants:</p>\n<ul><li><strong>Weights</strong> — the model's learned numbers (its <a class=\"xref\" href=\"#parameters\">parameters</a>). This is the biggest, fixed chunk.</li><li><strong>The <a class=\"xref\" href=\"#kv-cache\">KV cache</a></strong> — scratch memory that grows with every token in the conversation. Long <a class=\"xref\" href=\"#context-windows\">context windows</a> and many users make this balloon.</li><li><strong>Activations and overhead</strong> — temporary numbers the model produces mid-calculation, plus a slice the framework reserves for itself.</li></ul>\n<h4>The one formula worth memorising</h4>\n<p>Weights dominate, and their size is easy to estimate. Each parameter is just a number, and a number takes up a fixed number of <em>bytes</em> depending on its precision (how many bits of detail you keep). So:</p>\n<p><strong>VRAM for weights ≈ parameters × bytes-per-parameter.</strong></p>\n<p>The common precisions are:</p>\n<table class=\"cmp\"><thead><tr><th>Precision</th><th>Bytes / parameter</th><th>7B model needs roughly</th></tr></thead><tbody><tr><td>FP32 (full)</td><td>4</td><td>~28 GB</td></tr><tr><td>FP16 / BF16 (half)</td><td>2</td><td>~14 GB</td></tr><tr><td>8-bit (INT8)</td><td>1</td><td>~7 GB</td></tr><tr><td>4-bit (e.g. Q4)</td><td>~0.5</td><td>~3.5 GB</td></tr></tbody></table>\n<p>This is the magic behind <a class=\"xref\" href=\"#quantization\">quantization</a> — storing each weight in fewer bits. Shrinking a 7-billion-parameter model from 16-bit to 4-bit cuts its weights from ~14 GB to ~3.5 GB, the difference between \"needs a data-centre card\" and \"runs on a gaming laptop.\" It's the same trick that lets <a class=\"xref\" href=\"#qlora\">QLoRA</a> fine-tune big models on small cards, and what <a class=\"xref\" href=\"#gguf-models\">GGUF</a> files package up for tools like <a class=\"xref\" href=\"#ollama\">Ollama</a> and <a class=\"xref\" href=\"#llama-cpp\">llama.cpp</a>.</p>\n<p class=\"callout tip\">💡 Now add a safety margin. Budget <strong>roughly 20% on top</strong> of the weights for the KV cache, activations, and framework overhead. A 4-bit 7B model (~3.5 GB of weights) realistically wants ~4.5 GB of free VRAM, and more if conversations get long.</p>\n<pre><code class=\"lang-python\">params = 7_000_000_000      # 7B model\nbytes_per_param = 0.5       # 4-bit quantization\nweights_gb = params * bytes_per_param / 1e9\ntotal_gb = weights_gb * 1.2 # add ~20% headroom\nprint(f\"weights: {weights_gb:.1f} GB, budget: {total_gb:.1f} GB\")\n# weights: 3.5 GB, budget: 4.2 GB</code></pre>\n<h4>Checking what you've actually got</h4>\n<p>On an NVIDIA machine, one command shows your card and how much VRAM is free versus used:</p>\n<pre><code class=\"lang-bash\">nvidia-smi --query-gpu=name,memory.total,memory.used,memory.free --format=csv</code></pre>\n<p>Watch the <code>memory.used</code> figure climb as a model loads and as a chat gets longer — you are literally watching the three tenants fill the countertop.</p>\n<h4>When it doesn't fit</h4>\n<p>Run out of VRAM and you'll see a dreaded <em>CUDA out of memory</em> (OOM) error — the GPU's polite way of saying \"countertop full.\" Your options, roughly in order of preference:</p>\n<ul><li>Pick a smaller or more quantized model.</li><li>Shorten the context or batch size.</li><li><em>Offload</em> some layers to system RAM (works, but slow — like fetching from that shed).</li><li>Rent a bigger card.</li></ul>\n<p>Engines like <a class=\"xref\" href=\"#vllm\">vLLM</a> squeeze more conversations onto one card with smart cache management, and Apple Silicon Macs blur the line entirely with <a class=\"xref\" href=\"#mlx\">unified memory</a>, where CPU and GPU share one pool. If you can't or won't buy hardware, <a class=\"xref\" href=\"#cloud-gpus\">cloud GPUs</a> rent VRAM by the hour. Whatever route you take, the question never changes: <em>does it fit?</em></p>",
  "mentalModel": "VRAM is the chef's countertop, the GPU cores are the chef's hands. Loading a model lays out three things on that counter — the fixed weights, the growing KV-cache scratch space, and temporary activations. If they overflow the counter, cooking stops. Quantization just means chopping the ingredients smaller so more fits.",
  "takeaways": [
    "VRAM is space on the GPU; the question is always \"does it fit?\"",
    "Weights ≈ parameters × bytes-per-parameter; add ~20% headroom.",
    "Quantization shrinks bytes-per-parameter, not the parameter count."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"What fills a GPU's VRAM and whether the model fits\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"34\" text-anchor=\"middle\">What fills a 24 GB GPU's VRAM</text><rect class=\"dg-box\" x=\"40\" y=\"70\" width=\"230\" height=\"230\" rx=\"12\"/><text class=\"dg-sub\" x=\"155\" y=\"94\" text-anchor=\"middle\">VRAM budget (24 GB)</text><rect class=\"dg-box dg-box-accent\" x=\"60\" y=\"110\" width=\"190\" height=\"90\" rx=\"12\"/><text class=\"dg-label\" x=\"155\" y=\"148\" text-anchor=\"middle\">Weights</text><text class=\"dg-sub\" x=\"155\" y=\"170\" text-anchor=\"middle\">biggest, fixed</text><rect class=\"dg-box dg-box-2\" x=\"60\" y=\"212\" width=\"190\" height=\"48\" rx=\"12\"/><text class=\"dg-label\" x=\"155\" y=\"241\" text-anchor=\"middle\">KV cache (grows)</text><rect class=\"dg-box dg-box-warn\" x=\"60\" y=\"268\" width=\"190\" height=\"24\" rx=\"12\"/><text class=\"dg-sub\" x=\"155\" y=\"285\" text-anchor=\"middle\">activations + ~20% overhead</text><line class=\"dg-edge\" x1=\"272\" y1=\"185\" x2=\"352\" y2=\"185\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box-good\" x=\"360\" y=\"110\" width=\"150\" height=\"60\" rx=\"12\"/><text class=\"dg-label\" x=\"435\" y=\"137\" text-anchor=\"middle\">Fits?</text><text class=\"dg-sub\" x=\"435\" y=\"157\" text-anchor=\"middle\">total &lt; VRAM</text><line class=\"dg-edge\" x1=\"512\" y1=\"140\" x2=\"592\" y2=\"140\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box dg-box-good\" x=\"600\" y=\"110\" width=\"100\" height=\"60\" rx=\"12\"/><text class=\"dg-label\" x=\"650\" y=\"145\" text-anchor=\"middle\">Runs fast</text><line class=\"dg-edge\" x1=\"435\" y1=\"172\" x2=\"435\" y2=\"222\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box dg-box-warn\" x=\"360\" y=\"224\" width=\"150\" height=\"60\" rx=\"12\"/><text class=\"dg-label\" x=\"435\" y=\"251\" text-anchor=\"middle\">No: OOM error</text><text class=\"dg-sub\" x=\"435\" y=\"271\" text-anchor=\"middle\">quantize / offload</text></svg>",
    "caption": "Weights, KV cache and overhead all share one VRAM budget; if their total fits within the card the model runs fast, otherwise you hit an out-of-memory error."
  },
  "mistakes": [
    "<strong>Confusing VRAM with the GPU's speed.</strong> VRAM is <em>space</em>, not compute power. A card with more VRAM holds a bigger model but doesn't necessarily run it faster — that's down to the number of cores and memory bandwidth. Two cards with the same 24 GB can have wildly different speeds.",
    "<strong>Budgeting only for the weights.</strong> People compute that a model's weights are 14 GB, see a 16 GB card, and load it — then hit <em>out of memory</em> the moment a chat gets long, because the <a class=\"xref\" href=\"#kv-cache\">KV cache</a> and activations also need room. Always add ~20% headroom, more for long <a class=\"xref\" href=\"#context-windows\">contexts</a>.",
    "<strong>Assuming system RAM can stand in for VRAM.</strong> A 64 GB-RAM laptop with a 6 GB GPU still can't run a 14 GB model at full speed — only what fits in VRAM runs fast. Offloading the rest to system RAM works but can be many times slower.",
    "<strong>Forgetting that quantization changes the byte count, not the parameter count.</strong> A '7B' model is always 7 billion parameters; 4-bit just means each one takes ~0.5 bytes instead of 2. Mixing up parameters (how many numbers) with precision (bytes each) leads to VRAM estimates that are off by 4x or more.",
    "<strong>Ignoring batch size and number of users.</strong> The weights are loaded once, but the KV cache is <em>per request</em>. Serving 10 simultaneous conversations needs roughly 10x the cache space, which is why a model that 'fits' for one user OOMs under real <a class=\"xref\" href=\"#batch-inference\">batch</a> load.",
    "<strong>Treating reported VRAM as fully usable.</strong> The display, the operating system, and the framework reserve a slice before your model gets any. A '24 GB' card might hand you ~22 GB in practice, so don't plan to fill it to the brim."
  ],
  "exercise": "<p>Pick a model you'd like to run — say a 7B or 8B model — and a precision (16-bit or 4-bit). Using the formula <code>parameters × bytes-per-parameter × 1.2</code>, estimate the VRAM it needs, by hand or with the Python snippet from the lesson. Then run <code>nvidia-smi</code> (or, on a Mac, check your unified memory) to see how much VRAM you actually have free. Decide: does it fit? Write down your estimate and your verdict before you ever try to load it.</p><p><strong>Stretch goal:</strong> Actually load the model with <a class=\"xref\" href=\"#ollama\">Ollama</a> or <a class=\"xref\" href=\"#llama-cpp\">llama.cpp</a>, watch <code>memory.used</code> in <code>nvidia-smi</code> before loading, right after loading, and after a long 2,000-token conversation. Note how much each of the three tenants — weights, KV cache, overhead — actually claimed, and compare it to your estimate.</p>",
  "solution": "<p>For an 8B model in 4-bit: <code>8e9 × 0.5 bytes = 4 GB</code> of weights; times 1.2 gives a ~4.8 GB budget. In 16-bit it's <code>8e9 × 2 = 16 GB</code> of weights, ~19 GB with headroom — so the 4-bit version fits a 6 GB card while the 16-bit version needs a 24 GB card. For the stretch goal you should see three jumps: a small base reservation when the framework starts, a big step up (close to your weight estimate) when the model loads, and a slow further climb as the conversation lengthens and the KV cache grows. If the loaded figure is noticeably higher than your weight estimate, that gap is the activations and framework overhead — exactly the ~20% the headroom factor is there to cover.</p>",
  "summary": "<p>VRAM is the fast memory on the GPU, and the central question of running models locally is simply \"does it fit?\" Three things share the space: the fixed weights (by far the biggest), the KV cache that grows with conversation length and user count, and temporary activations. Estimate weights with <code>parameters × bytes-per-parameter</code>, where precision sets the bytes (4 for FP32 down to ~0.5 for 4-bit) — this is why quantization is so powerful. Add ~20% headroom, check with <code>nvidia-smi</code>, and if it overflows you'll hit an out-of-memory error.</p>",
  "keyTerms": [
    { "term": "VRAM", "def": "Video RAM — the fast memory built onto a GPU. The model's weights and working data must fit here to run at full speed; system RAM is far too slow a substitute." },
    { "term": "Weights", "def": "The model's learned numbers (its parameters). The largest, fixed consumer of VRAM, sized by the parameter count times the bytes used per parameter." },
    { "term": "Bytes per parameter", "def": "How much memory each weight takes, set by its precision: 4 bytes for FP32, 2 for FP16/BF16, 1 for 8-bit, ~0.5 for 4-bit. Multiply by parameter count to estimate weight memory." },
    { "term": "Precision", "def": "How many bits of detail are kept per number. Lower precision (fewer bits) shrinks VRAM use at some cost to accuracy — the core idea behind quantization." },
    { "term": "Activations", "def": "The temporary numbers a model produces partway through a calculation. Short-lived but real VRAM users, part of the overhead you budget headroom for." },
    { "term": "Out of memory (OOM)", "def": "The error you hit when the GPU runs out of VRAM, often shown as 'CUDA out of memory'. The fix is a smaller/more-quantized model, shorter context, offloading, or a bigger card." },
    { "term": "Offloading", "def": "Moving some model layers from VRAM into slower system RAM (or disk) so an oversized model still runs. It works but drags speed down, because the GPU must fetch data from far away." },
    { "term": "Unified memory", "def": "An architecture (notably Apple Silicon) where CPU and GPU share one memory pool, so there is no separate VRAM number — the model competes with everything else for the same RAM." },
    { "term": "Headroom", "def": "Spare VRAM left free beyond the weights — roughly 20% — to hold the KV cache, activations, and framework overhead. Skip it and long chats trigger out-of-memory errors." }
  ],
  "quiz": [
    {
      "q": "A 13B-parameter model is stored in 4-bit (~0.5 bytes/param). Roughly how much free VRAM should you budget, including headroom?",
      "options": ["~6.5 GB", "~7.8 GB", "~13 GB", "~26 GB"],
      "answer": 1,
      "explain": "Weights ≈ 13e9 × 0.5 = 6.5 GB, then × 1.2 for ~20% headroom ≈ 7.8 GB. The 6.5 GB option forgets the headroom; 13 GB and 26 GB use the wrong bytes-per-parameter."
    },
    {
      "q": "You have a laptop with 64 GB of system RAM but only a 6 GB GPU. Will a 16-bit 13B model (~26 GB) run fast?",
      "options": ["Yes — the 64 GB of system RAM covers it easily", "No — only what fits in the 6 GB of VRAM runs at full speed", "Yes — the GPU borrows system RAM at the same speed", "Only if you close other apps to free system RAM"],
      "answer": 1,
      "explain": "The GPU can only crunch numbers that fit in its own VRAM. System RAM can hold offloaded layers, but fetching them is far slower, so it won't run fast."
    },
    {
      "q": "Two GPUs both have 24 GB of VRAM, but one has twice as many compute cores. Compared on the same model, which is true?",
      "options": ["The one with more cores holds a bigger model", "Both hold the same model, but the higher-core card generates tokens faster", "The higher-core card has less VRAM available for weights", "They behave identically because VRAM is what matters"],
      "answer": 1,
      "explain": "VRAM is space and cores are speed. Both 24 GB cards fit the same model, but the card with more cores does the maths faster, so it generates tokens more quickly."
    },
    {
      "q": "Your 7B model loads fine and chats happily for one user, but OOMs when 12 people use it at once. The most likely culprit is:",
      "options": ["The weights are reloaded once per user", "The KV cache is per-request and scales with the number of users", "Quantization stops working under load", "System RAM ran out, not VRAM"],
      "answer": 1,
      "explain": "Weights are loaded once and shared, but each active conversation needs its own KV cache. Twelve users means roughly twelve times the cache, which can overflow VRAM."
    }
  ]
}
