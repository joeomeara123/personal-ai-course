{
  "id": "function-calling",
  "order": 57,
  "section": "Agents & Workflows",
  "title": "Function Calling",
  "lesson": "<p>On its own, a language model can only do one thing: produce text. It cannot check the weather, run a database query, or send an email. It has no hands. <strong>Function calling</strong> is how we give it hands — not by letting it <em>run</em> code (it never does), but by letting it <em>ask, in a strict, machine-readable format,</em> for a function in your program to be run.</p>\n<p>Your code runs the function, hands the result back, and the model carries on. Function calling is the original, lower-level name for this mechanism; the broader product-level idea built on top of it is <a class=\"xref\" href=\"#tool-calling\">Tool Calling</a>. Here we zoom in on the actual nuts and bolts.</p>\n<h4>The waiter and the kitchen</h4>\n<p>Think of the model as a <em>waiter</em>. The waiter takes your order in plain English — \"I'd like the salmon\" — but the waiter never cooks. They write a ticket in the kitchen's exact format (table 4, salmon, no capers) and pin it up. The kitchen (your code) cooks and passes a plate back. The waiter then brings it to you and explains it.</p>\n<p>Function calling is the model writing that ticket. The crucial part: the ticket has a <em>fixed shape</em> the kitchen understands. A scribbled \"fish maybe?\" gets thrown back. So before any conversation, you hand the model a menu of functions it's allowed to order from, each with a precise <strong>schema</strong> (a written description of the function's name, what it does, and the exact arguments it takes).</p>\n<h4>The four-step dance</h4>\n<p>Almost every function-calling system follows the same loop:</p>\n<ol><li><strong>Declare:</strong> you send the model the user's message <em>plus</em> the list of available functions and their schemas.</li><li><strong>Decide:</strong> instead of plain text, the model may reply with a structured request — \"call <code>get_weather</code> with <code>{city: 'Paris'}</code>\". This is just text the model generated; nothing has run yet.</li><li><strong>Execute:</strong> <em>your</em> code reads that request, actually runs <code>get_weather('Paris')</code>, and gets back, say, <code>18°C, rain</code>.</li><li><strong>Return:</strong> you feed that result back into the model, which now writes a normal human reply: \"It's 18°C and rainy in Paris — bring a coat.\"</li></ol>\n<p class=\"callout tip\">💡 The single most important thing to grasp: <em>the model only proposes the call. You always execute it.</em> The model is a very good order-taker, not a chef.</p>\n<h4>The schema is everything</h4>\n<p>How does the model know which arguments to send? You tell it, in a structured description usually written as a JSON-style schema (JSON is a plain-text format of labelled key-value pairs that both humans and code can read). This is where most of your effort goes — clear names and descriptions act like a <a class=\"xref\" href=\"#prompt-engineering\">prompt</a> for the model.</p>\n<pre><code class=\"lang-json\">{\n  \"name\": \"get_weather\",\n  \"description\": \"Get the current weather for a city.\",\n  \"parameters\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"city\": { \"type\": \"string\", \"description\": \"City name, e.g. Paris\" },\n      \"units\": { \"type\": \"string\", \"enum\": [\"celsius\", \"fahrenheit\"] }\n    },\n    \"required\": [\"city\"]\n  }\n}</code></pre>\n<p>That <code>enum</code> (a fixed list of allowed values) stops the model inventing <code>units: 'kelvin'</code>. The model reads these descriptions like instructions, so vague ones (\"the input\") produce vague calls. Treat schema-writing as part of <a class=\"xref\" href=\"#system-prompts\">system-prompt</a> design.</p>\n<h4>A second analogy: filling in a form</h4>\n<p>Picture a paper form at the bank with labelled boxes: NAME, AMOUNT, DATE. You can scrawl anything in the margins, but the teller only reads the boxes. The schema is that form. Function calling forces the model to stop free-writing and <em>fill in the boxes</em> — which is exactly why it's so reliable.</p>\n<p>The output isn't a paragraph you have to parse with fragile pattern-matching; it's clean, typed data your code can use directly. This is why the same machinery is reused for plain <strong>structured output</strong> — sometimes you define a \"function\" you never even run, just to force the model to answer in a strict shape (e.g. extract <code>{name, email}</code> from a messy email).</p>\n<pre><code class=\"lang-python\"># The loop your code runs\nmsg = model.chat(user_text, functions=[weather_schema])\nif msg.function_call:                       # model proposed a call\n    args = json.loads(msg.function_call.arguments)\n    result = get_weather(**args)            # YOU run the real function\n    final = model.chat(user_text, function_result=result)\n    print(final.text)</code></pre>\n<h4>Why this unlocks real products</h4>\n<p>Function calling is the bridge between a chatbot and software that <em>does things</em>. A travel assistant calls <code>search_flights</code>; a support bot calls <code>refund_order</code>; a coding helper calls <code>run_tests</code>.</p>\n<p>Chain several of these calls together and let the model decide the order, and you've built the core of an <a class=\"xref\" href=\"#ai-agents\">AI Agent</a> running an <a class=\"xref\" href=\"#agentic-workflows\">Agentic Workflow</a>. But it all rests on this humble loop: declare, decide, execute, return. Master the loop, and everything fancier is just more functions on the menu.</p>",
  "mentalModel": "The model is a waiter, not a chef. It writes a precise kitchen ticket (the function call in your schema's exact shape) but never cooks — your code runs the real function and hands the plate back for the model to serve.",
  "takeaways": [
    "The model <strong>proposes</strong> a function call; <em>your</em> code runs it.",
    "A clear JSON schema (name, description, <code>enum</code>, <code>required</code>) is most of the work.",
    "Declare → decide → execute → return: that loop is the seed of agents."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"The four-step function-calling loop between model and your code\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"34\" text-anchor=\"middle\">The model proposes, your code disposes</text><rect class=\"dg-box dg-box-accent\" x=\"40\" y=\"80\" width=\"160\" height=\"70\" rx=\"12\"/><text class=\"dg-label\" x=\"120\" y=\"110\" text-anchor=\"middle\">1. Declare</text><text class=\"dg-sub\" x=\"120\" y=\"130\" text-anchor=\"middle\">message + schemas</text><rect class=\"dg-box dg-box-2\" x=\"280\" y=\"80\" width=\"160\" height=\"70\" rx=\"12\"/><text class=\"dg-label\" x=\"360\" y=\"110\" text-anchor=\"middle\">2. Decide (model)</text><text class=\"dg-sub\" x=\"360\" y=\"130\" text-anchor=\"middle\">get_weather{city}</text><rect class=\"dg-box dg-box-good\" x=\"520\" y=\"80\" width=\"160\" height=\"70\" rx=\"12\"/><text class=\"dg-label\" x=\"600\" y=\"110\" text-anchor=\"middle\">3. Execute (you)</text><text class=\"dg-sub\" x=\"600\" y=\"130\" text-anchor=\"middle\">run real function</text><rect class=\"dg-box\" x=\"280\" y=\"230\" width=\"160\" height=\"70\" rx=\"12\"/><text class=\"dg-label\" x=\"360\" y=\"260\" text-anchor=\"middle\">4. Return</text><text class=\"dg-sub\" x=\"360\" y=\"280\" text-anchor=\"middle\">result → human reply</text><line class=\"dg-edge\" x1=\"200\" y1=\"115\" x2=\"280\" y2=\"115\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"440\" y1=\"115\" x2=\"520\" y2=\"115\" marker-end=\"url(#arrow)\"/><path class=\"dg-edge\" d=\"M600,150 L600,200 Q600,230 540,235 L444,250\" fill=\"none\" marker-end=\"url(#arrow)\"/><path class=\"dg-edge\" d=\"M300,235 Q220,235 200,160 L160,155\" fill=\"none\" marker-end=\"url(#arrow)\"/><text class=\"dg-note\" x=\"360\" y=\"325\" text-anchor=\"middle\">The result loops back so the model can answer in plain English</text></svg>",
    "caption": "The model only decides which function to call; your code executes it and feeds the result back for a human reply."
  },
  "quiz": [
    {
      "q": "A model replies with a structured request to call get_weather({city: 'Paris'}). What has actually happened at this moment?",
      "options": [
        "The weather has been fetched and is ready to show the user",
        "The model has generated a proposed call as text; nothing has run yet",
        "Your code has run get_weather and is waiting on the model",
        "An error occurred because the model can't run functions"
      ],
      "answer": 1,
      "explain": "The model only emits a structured request — it is just generated text. Your code must read it and actually execute the function; no real call happens until then."
    },
    {
      "q": "Why does adding \"enum\": [\"celsius\", \"fahrenheit\"] to the units field help?",
      "options": [
        "It makes the model run faster by caching values",
        "It lets the model call the function without your code",
        "It restricts units to allowed values, so the model can't invent 'kelvin'",
        "It is required for the function to return JSON"
      ],
      "answer": 2,
      "explain": "An enum lists the only valid values for a field, acting as a guardrail. Without it the model might supply an out-of-range value your function can't handle."
    },
    {
      "q": "You want to pull {name, email} out of a messy support email into clean fields, but you have no real function to run. What's the idiomatic approach?",
      "options": [
        "Define a 'function' schema purely to force structured output, then never execute it",
        "Ask the model in plain prose and parse the paragraph with regex",
        "Fine-tune a new model just for extraction",
        "It's impossible — function calling always runs code"
      ],
      "answer": 0,
      "explain": "The same machinery powers structured output: you declare a schema to force a strict shape, even if you never run a real function. Regex on free prose is fragile and unreliable."
    },
    {
      "q": "A support bot must refund an order based on the model's function call. What's the safest design?",
      "options": [
        "Trust the model's arguments and run refund_order immediately",
        "Let the model execute the refund itself for speed",
        "Validate and sanitise the arguments before executing the refund",
        "Offer all 30 internal functions so the model has full context"
      ],
      "answer": 2,
      "explain": "Models can hallucinate or malform arguments, so always validate before any action that pays, deletes, or emails. The model never executes — and a bloated menu only confuses it."
    }
  ],
  "mistakes": [
    "<strong>Thinking the model runs the function.</strong> It never does. It only emits a request in your schema's format; <em>your</em> code must execute it and feed the result back. Forgetting the 'return' step leaves the model talking to itself with no answer.",
    "<strong>Writing vague schemas.</strong> The model reads your <code>name</code> and <code>description</code> fields like instructions. 'the data' or 'input1' gets you garbage arguments. Treat each description as careful <a class=\"xref\" href=\"#prompt-engineering\">prompt engineering</a>.",
    "<strong>Trusting the arguments blindly.</strong> The model can hallucinate values, miss required fields, or output slightly malformed JSON. Always validate and sanitise arguments before running anything — especially calls that delete, pay, or email.",
    "<strong>Offering 30 functions at once.</strong> A bloated menu confuses the model into picking the wrong one and bloats every prompt. Keep the function list short and relevant; fetch a smaller set per task if you have many.",
    "<strong>Skipping <code>required</code> and <code>enum</code> constraints.</strong> Without them the model invents optional fields or out-of-range values (like an unsupported currency). Tight schemas are guardrails, not red tape.",
    "<strong>Confusing function calling with <a class=\"xref\" href=\"#tool-calling\">tool calling</a>.</strong> They're the same machinery; 'function calling' is the original low-level term for the request format, while 'tool calling' is the broader, product-level wrapper. Don't expect two different mechanisms."
  ],
  "exercise": "<p>Pick one simple function you could write, e.g. <code>convert_currency(amount, from_ccy, to_ccy)</code>. On paper (or in code), write its JSON schema: a clear <code>name</code>, a one-line <code>description</code>, each parameter with a <code>type</code> and <code>description</code>, an <code>enum</code> of allowed currencies, and a <code>required</code> list. Now write out, by hand, what the model would emit for the message 'How much is 50 dollars in euros?' — then what your code does with it, and the final human reply. Trace all four steps: declare, decide, execute, return.</p><p><strong>Stretch goal:</strong> Add a second function (<code>get_exchange_rate</code>) and write the message 'Convert 50 USD to the currency with the strongest rate against the dollar today.' Show how the model would need to call <em>two</em> functions in sequence — and where your code feeds the first result back so the model can decide the second call.</p>",
  "solution": "<p>For the single function, the model should emit a call to <code>convert_currency</code> with <code>{amount: 50, from_ccy: 'USD', to_ccy: 'EUR'}</code>. Note it correctly maps 'dollars' to the <code>enum</code> value 'USD'; that mapping is exactly what a clear description buys you. Your code runs the real conversion (say it returns 46.2), you feed <code>46.2</code> back, and only <em>then</em> does the model write 'About €46.20'. The key insight learners miss: the model produced no euros itself — it just filled the form, your code did the maths, and the model phrased the answer.</p><p>For the stretch goal, the model can't answer in one shot because it doesn't know today's rates. It first calls <code>get_exchange_rate</code>, your code returns live numbers, you feed those back, and the model then decides which currency is strongest and issues the <code>convert_currency</code> call. That back-and-forth — call, return, call again — is the seed of an <a class=\"xref\" href=\"#ai-agents\">agent</a>: the same loop, just repeated until the job's done.</p>",
  "summary": "<p>Function calling lets a text-only model <em>request</em> that one of your functions be run, in a strict machine-readable shape it cannot easily get wrong. You declare a menu of functions with clear schemas; the model decides which to call and with what arguments; your code executes the real function and returns the result; the model then writes a human reply. The model proposes, your code disposes — it never runs anything itself. Clear schemas (names, descriptions, <code>enum</code>, <code>required</code>) are most of the work, and chaining these calls is the foundation of AI agents.</p>",
  "keyTerms": [
    { "term": "Function calling", "def": "A mechanism where the model outputs a structured request to run one of your functions, with specific arguments, instead of plain text. Your code runs it and returns the result." },
    { "term": "Schema", "def": "A written description of a function — its name, what it does, and the exact arguments it accepts — that tells the model how to call it correctly." },
    { "term": "Arguments", "def": "The specific values the model fills in for a function's inputs, e.g. {city: 'Paris'}. The model proposes them; your code uses them." },
    { "term": "Function call (vs execution)", "def": "The model only generates a call request as text. Actually running the function ('execution') is done by your code, never by the model." },
    { "term": "enum", "def": "A schema rule listing the only allowed values for a field, so the model can't invent an out-of-range option like an unsupported currency." },
    { "term": "required", "def": "A schema rule naming which arguments must always be present, so the model doesn't omit a value the function needs to run." },
    { "term": "Structured output", "def": "Using the same function-calling machinery purely to force a strict response shape (like extracting {name, email}), sometimes without ever running a real function." },
    { "term": "The call loop", "def": "The four-step cycle behind every function call: declare available functions, model decides a call, your code executes it, you return the result for the model's final reply." }
  ]
}
