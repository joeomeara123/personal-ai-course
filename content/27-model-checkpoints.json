{
  "id": "model-checkpoints",
  "order": 27,
  "section": "Fine-Tuning",
  "title": "Model Checkpoints",
  "lesson": "<p>A <strong>model checkpoint</strong> is a saved snapshot of a model's state at one moment in time. Think of it like a save file in a video game. If the boss kills you, you do not start the whole game over — you reload the last save and try again. A checkpoint does the same job for training: it lets you stop, reload, and continue without throwing away hours (or weeks) of expensive work.</p>\n<h4>What is actually inside a checkpoint?</h4>\n<p>The most important thing in a checkpoint is the <strong>weights</strong> — the millions or billions of numbers the model has learned (see <a class=\"xref\" href=\"#parameters\">Parameters</a>). But a <em>training</em> checkpoint usually holds more than just weights. It also saves:</p>\n<ul><li>The <strong>optimizer state</strong> — the optimizer is the part of training that decides how much to nudge each weight, and it keeps running notes (like the average direction of recent changes).</li><li>The <strong>step count</strong> — how far into training you are.</li><li>The <strong>learning-rate schedule</strong> — how fast the model is currently learning.</li><li>The random-number seed, so results stay reproducible.</li></ul>\n<p>Why save all that extra baggage? Imagine you are following a long recipe and have to stop halfway. Saving only the half-cooked dish is not enough — you also need to know which step you were on, the oven temperature, and how much salt you have already added.</p>\n<p>Reload only the weights and you can <em>use</em> the model, but resuming training would lurch because the optimizer forgot its momentum. So checkpoints come in two flavours: a heavy <em>resume-training</em> checkpoint with everything, and a lighter <em>inference-only</em> checkpoint with just the weights for running the model (see <a class=\"xref\" href=\"#training-vs-inference\">Training vs Inference</a>).</p>\n<h4>Why checkpoints matter in real life</h4>\n<p>Training a large model can run for days on rented GPUs that cost real money. Machines crash. Cloud instances get reclaimed. If you saved nothing, a crash at hour 40 means starting from hour 0. With checkpoints written every, say, 500 steps, you lose only minutes of work.</p>\n<p>Checkpoints also let you <strong>compare</strong>. You might save one every epoch (one full pass over your data) and discover that the checkpoint from epoch 2 is actually better than epoch 5, because the model started <em>overfitting</em> — memorising the training data instead of learning general patterns. You simply keep the good save and discard the rest.</p>\n<h4>What a checkpoint looks like on disk</h4>\n<p>In the Hugging Face world, saving during a fine-tune produces a folder, not a single file:</p>\n<pre><code class=\"lang-text\">checkpoint-1500/\n  model.safetensors        # the weights\n  optimizer.pt             # optimizer state (only for resuming)\n  scheduler.pt             # learning-rate schedule\n  trainer_state.json       # step count, loss history\n  config.json              # model shape/settings</code></pre>\n<p>The weights live in a <code>.safetensors</code> file. <strong>Safetensors</strong> is the modern, safe format: older <code>.bin</code> / <code>.pt</code> files used Python <em>pickle</em>, which can hide malicious code that runs the moment you load it. Safetensors stores only plain numbers, so loading a downloaded checkpoint can't execute anything nasty. Always prefer it.</p>\n<h4>Saving and loading in practice</h4>\n<pre><code class=\"lang-python\">from transformers import TrainingArguments\n\nargs = TrainingArguments(\n    output_dir=\"./out\",\n    save_steps=500,            # write a checkpoint every 500 steps\n    save_total_limit=3,        # keep only the 3 newest, delete older ones\n    load_best_model_at_end=True,\n)\n# Resume a crashed run from the last save:\ntrainer.train(resume_from_checkpoint=True)\n</code></pre>\n<p>Two settings here save real grief:</p>\n<ul><li><code>save_total_limit</code> stops your disk filling up — each checkpoint of a big model can be tens of gigabytes, and they pile up fast.</li><li><code>load_best_model_at_end</code> automatically keeps the checkpoint with the best validation score, not just the last one.</li></ul>\n<h4>Checkpoints vs final models vs adapters</h4>\n<p>People mix these up. A <strong>checkpoint</strong> is an intermediate save during training. A <strong>final model</strong> is the polished version you ship. And if you used <a class=\"xref\" href=\"#lora\">LoRA</a> or <a class=\"xref\" href=\"#qlora\">QLoRA</a>, your checkpoint might be tiny — only the small adapter weights, not the whole base model. That is why a LoRA checkpoint can be a few megabytes while a full-model checkpoint is many gigabytes.</p>\n<table class=\"cmp\"><thead><tr><th>Resume checkpoint</th><th>Inference checkpoint</th></tr></thead><tbody><tr><td>Weights + optimizer + scheduler + step count</td><td>Just the weights (+ config)</td></tr><tr><td>Large; for continuing training</td><td>Smaller; for running the model</td></tr><tr><td>Saved mid-run, often deleted later</td><td>The version you actually deploy</td></tr></tbody></table>\n<p class=\"callout tip\">💡 Rule of thumb: save often enough that a crash costs minutes not hours, but not so often that writing to disk slows training. Every few hundred steps, keeping the best 2–3, is a sane default.</p>",
  "mentalModel": "A checkpoint is a save file in a video game. Saving only the weights is like saving your character but forgetting which level you were on — fine for showing off the character, useless for continuing the quest. A full training checkpoint remembers the level, the score, and your position, so you can pick up exactly where you left off.",
  "takeaways": [
    "A checkpoint is a save file: stop, reload, resume training.",
    "Resume save = weights + optimizer + scheduler; inference save = weights only.",
    "Cap saved copies, prefer <code>.safetensors</code>, keep the best not the last."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"A training checkpoint splits into a heavy resume save and a light inference save\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"34\" text-anchor=\"middle\">One save, two kinds of checkpoint</text><rect class=\"dg-box dg-box-accent\" x=\"270\" y=\"66\" width=\"180\" height=\"64\" rx=\"12\"/><text class=\"dg-label\" x=\"360\" y=\"92\" text-anchor=\"middle\">Training run</text><text class=\"dg-sub\" x=\"360\" y=\"112\" text-anchor=\"middle\">save every 500 steps</text><line class=\"dg-edge\" x1=\"320\" y1=\"130\" x2=\"200\" y2=\"196\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"400\" y1=\"130\" x2=\"520\" y2=\"196\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box dg-box-2\" x=\"40\" y=\"200\" width=\"260\" height=\"110\" rx=\"12\"/><text class=\"dg-label\" x=\"170\" y=\"228\" text-anchor=\"middle\">Resume checkpoint</text><text class=\"dg-sub\" x=\"170\" y=\"252\" text-anchor=\"middle\">weights + optimizer</text><text class=\"dg-sub\" x=\"170\" y=\"272\" text-anchor=\"middle\">+ scheduler + step count</text><text class=\"dg-note\" x=\"170\" y=\"296\" text-anchor=\"middle\">heavy — continue training</text><rect class=\"dg-box dg-box-good\" x=\"420\" y=\"200\" width=\"260\" height=\"110\" rx=\"12\"/><text class=\"dg-label\" x=\"550\" y=\"228\" text-anchor=\"middle\">Inference checkpoint</text><text class=\"dg-sub\" x=\"550\" y=\"252\" text-anchor=\"middle\">weights + config only</text><text class=\"dg-sub\" x=\"550\" y=\"272\" text-anchor=\"middle\">.safetensors</text><text class=\"dg-note\" x=\"550\" y=\"296\" text-anchor=\"middle\">light — deploy and run</text></svg>",
    "caption": "A training run saves snapshots; a full resume checkpoint lets you continue, while a lighter inference checkpoint just runs the model."
  },
  "quiz": [
    {
      "q": "Your GPU job crashes at hour 40. You reload the latest checkpoint and want to <em>continue training</em> smoothly. What must the checkpoint contain beyond the weights?",
      "options": [
        "Nothing extra — weights alone fully restore a training run",
        "The optimizer state and learning-rate schedule",
        "A copy of the entire training dataset",
        "The model's benchmark scores"
      ],
      "answer": 1,
      "explain": "Resuming smoothly needs the optimizer's momentum and the learning-rate schedule; without them the run lurches and the learning rate restarts wrong. Weights alone only let you run the model."
    },
    {
      "q": "Why is <code>save_total_limit=3</code> a smart setting on a long fine-tune?",
      "options": [
        "It makes the model train three times faster",
        "It keeps only the 3 newest checkpoints so big saves don't fill the disk",
        "It limits training to 3 epochs",
        "It saves a checkpoint every 3 steps"
      ],
      "answer": 1,
      "explain": "Each checkpoint of a large model can be tens of gigabytes, so they pile up fast. The limit auto-deletes older ones to stop the disk filling and crashing the run."
    },
    {
      "q": "Your LoRA fine-tune produced a checkpoint that is only a few megabytes. Why so small, and what do you still need to run it?",
      "options": [
        "It is corrupt; a real checkpoint is always many gigabytes",
        "It holds only the small adapter weights — you still need the matching base model",
        "It compressed the full model losslessly into a few MB",
        "It contains just the tokenizer; the weights are downloaded at runtime"
      ],
      "answer": 1,
      "explain": "A LoRA checkpoint saves only the tiny adapter weights, not the whole base model. On its own it can't run — you must pair it with the original base model it was trained on."
    },
    {
      "q": "Validation loss is lowest at epoch 2 but you trained for 5 epochs. Which checkpoint should you ship, and why?",
      "options": [
        "Epoch 5 — the last checkpoint is always the most trained and best",
        "Epoch 2 — later epochs likely overfit, getting worse on new data",
        "An average of all five checkpoints' files",
        "Epoch 1 — earlier is always safer"
      ],
      "answer": 1,
      "explain": "Models often overfit late in training, memorising the training set and scoring worse on unseen data. Keep the checkpoint with the best validation score, which here is epoch 2."
    }
  ],
  "mistakes": [
    "<strong>Saving only the weights, then trying to resume training.</strong> Without the optimizer and scheduler state, resuming makes the model lurch because the optimizer lost its momentum and the learning rate restarts wrong. Save the full checkpoint when you intend to continue.",
    "<strong>Letting checkpoints fill the disk.</strong> Each save of a large model can be tens of gigabytes. Without <code>save_total_limit</code>, a long run silently fills the drive and crashes hours in. Cap how many you keep.",
    "<strong>Assuming the last checkpoint is the best.</strong> Models often <em>overfit</em> late in training — they memorise the training set and get worse on new data. Track a validation score and keep the best checkpoint, not automatically the final one.",
    "<strong>Trusting <code>.bin</code> / pickle files from strangers.</strong> The old pickle format can run hidden code on load. Prefer <code>.safetensors</code>, which stores only numbers and cannot execute anything.",
    "<strong>Confusing a <a class=\"xref\" href=\"#lora\">LoRA</a> adapter checkpoint with a full model.</strong> A tiny adapter checkpoint is useless on its own — you still need the matching base model to run it. Save or note which base it belongs to.",
    "<strong>Forgetting to save the tokenizer and config alongside the weights.</strong> Reload the weights with a mismatched tokenizer or config and the model produces garbage. Keep them in the same folder so the checkpoint is self-contained."
  ],
  "exercise": "<p>Start a tiny fine-tune (even on a small model and a few dozen examples) with the Hugging Face <code>Trainer</code>. Set <code>save_steps=50</code> and <code>save_total_limit=2</code>. Let it run past 150 steps, then stop it by hand. Look in <code>output_dir</code> and confirm you see exactly two <code>checkpoint-*</code> folders, and that the older ones were deleted. Open one folder and list the files inside it.</p><p><strong>Stretch goal:</strong> Restart the script with <code>trainer.train(resume_from_checkpoint=True)</code> and watch the logs. Confirm the step counter picks up where it stopped (e.g. step 150) rather than restarting at 0 — proving the optimizer and scheduler state were restored, not just the weights.</p>",
  "solution": "<p><strong>Hint:</strong> Inside a <code>checkpoint-100/</code> folder you should find <code>model.safetensors</code> (weights), <code>optimizer.pt</code> and <code>scheduler.pt</code> (the resume state), and <code>trainer_state.json</code> (step count and loss history). Because <code>save_total_limit=2</code>, only the two newest folders survive — older ones are auto-deleted. On resume, the log line will read something like <code>Continuing training from step 150</code>; if it restarted at 0, the checkpoint wasn't found or only the weights were loaded. That step-resume working is the proof that a checkpoint is more than just weights.</p>",
  "summary": "<p>A model checkpoint is a saved snapshot of training — like a video-game save file. A resume checkpoint holds weights plus optimizer and scheduler state so you can continue exactly where you stopped; an inference checkpoint holds just the weights for running the model. Save often (every few hundred steps), cap how many you keep, prefer the safe <code>.safetensors</code> format, and keep the best validation checkpoint rather than blindly the last one.</p>",
  "keyTerms": [
    { "term": "Checkpoint", "def": "A saved snapshot of a model's state during training, so you can stop, reload, and continue without losing work." },
    { "term": "Weights", "def": "The learned numbers that make up the model. They are the core of any checkpoint." },
    { "term": "Optimizer state", "def": "The optimizer's running notes (like momentum) about how to nudge each weight. Needed to resume training smoothly, not just to run the model." },
    { "term": "Scheduler / learning-rate schedule", "def": "The plan for how fast the model learns over time. Saved so a resumed run continues at the right learning speed." },
    { "term": "Safetensors", "def": "A safe file format that stores only plain numbers, so loading a checkpoint can't run hidden code — unlike the older pickle-based .bin/.pt files." },
    { "term": "Epoch", "def": "One full pass over the entire training dataset. People often save a checkpoint at the end of each epoch." },
    { "term": "Overfitting", "def": "When a model memorises the training data instead of learning general patterns, so it gets worse on new inputs. A reason to keep an earlier checkpoint." },
    { "term": "Resume checkpoint", "def": "A full save (weights + optimizer + scheduler + step count) used to continue an interrupted training run." },
    { "term": "Inference checkpoint", "def": "A lighter save with just the weights and config, used to run the finished model rather than keep training it." }
  ]
}
