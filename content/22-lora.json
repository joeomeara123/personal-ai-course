{
  "id": "lora",
  "order": 22,
  "section": "Fine-Tuning",
  "title": "LoRA",
  "lesson": "<p><strong>LoRA</strong> (short for <em>Low-Rank Adaptation</em>) is the trick that made <a class=\"xref\" href=\"#fine-tuning-basics\">fine-tuning</a> cheap enough to do on a single gaming GPU instead of a room full of servers. Instead of editing the whole model, you bolt on a tiny set of extra <a class=\"xref\" href=\"#parameters\">parameters</a> and train only those. The big model stays frozen; you teach a small add-on.</p>\n<h4>The problem LoRA solves</h4>\n<p>A model's knowledge lives in its <strong>weights</strong> — huge grids of numbers (called <em>matrices</em>) that get multiplied against your text as it flows through the network. A 7-billion-parameter model has billions of these numbers.</p>\n<p>Classic fine-tuning nudges <em>every single one</em> of them. To do that you must hold the whole model in GPU memory, plus a full set of training bookkeeping — gradients and optimiser state, roughly the model's size again several times over. That is why full fine-tuning a 7B model can need 60GB or more of <a class=\"xref\" href=\"#vram-basics\">VRAM</a> and serious hardware.</p>\n<p>Here is the key insight behind LoRA: when you adapt a model to a <em>new task</em>, the change you actually need is small and simple — even though the model is enormous. You do not need to repaint the whole house to change one room.</p>\n<h4>Analogy 1: sticky notes on a textbook</h4>\n<p>Imagine a 2,000-page reference textbook (the frozen model). You do not own a printing press, so you cannot reprint it. But you <em>can</em> add sticky notes in the margins that say \"in our company, do it this way.\"</p>\n<p>The book underneath never changes; your notes ride on top and steer how it gets read. LoRA's add-on is that thin layer of sticky notes — a few megabytes against a several-gigabyte book.</p>\n<h4>Analogy 2: a thin lens clipped over a camera</h4>\n<p>The camera body and its expensive sensor are the frozen model. Clip a small correction lens in front and the picture shifts — sharper here, warmer there — without touching the sensor. Swap lenses and you get a different look from the same camera. A LoRA is that clip-on lens: small, removable, and you can keep a drawer full of them for different jobs.</p>\n<h4>The \"low-rank\" idea, in plain English</h4>\n<p>A weight update is itself a big grid of numbers. LoRA's bet is that this update is <em>low-rank</em> — a fancy way of saying it can be rebuilt from two skinny grids instead of one fat one.</p>\n<p>Picture a 1000&times;1000 square (a million numbers). Replace it with a 1000&times;8 strip times an 8&times;1000 strip. Multiply those two and you get back a 1000&times;1000 grid — but you only had to store and train 16,000 numbers, not a million. That little number <code>8</code> is the <strong>rank</strong> (often written <code>r</code>): how skinny the strips are. The two strips are usually called <strong>A</strong> and <strong>B</strong>.</p>\n<p>During training the original weight <code>W</code> is frozen. The model uses <code>W + (B&times;A)</code>, and only <code>A</code> and <code>B</code> get gradient updates (their numbers get adjusted). At the end you have a tiny file — often just a few megabytes — called the <strong>adapter</strong>.</p>\n<pre><code class=\"lang-python\">from peft import LoraConfig, get_peft_model\n\nconfig = LoraConfig(\n    r=8,                 # rank: the skinniness of the strips\n    lora_alpha=16,       # scaling on the update\n    target_modules=[\"q_proj\", \"v_proj\"],  # which layers to adapt\n    lora_dropout=0.05,\n)\nmodel = get_peft_model(base_model, config)\nmodel.print_trainable_parameters()\n# trainable params: ~4M  ||  all params: ~7B  ||  trainable%: 0.06</code></pre>\n<p>That last line is the whole story: you are training roughly 0.06% of the model. This is why LoRA belongs to the family called <a class=\"xref\" href=\"#peft\">PEFT</a> (Parameter-Efficient Fine-Tuning), and why it sits right next to plain <a class=\"xref\" href=\"#adapter-tuning\">adapter tuning</a>.</p>\n<h4>Why this is a big deal in practice</h4>\n<ul><li><strong>Cheap:</strong> trains on one consumer GPU; fast to run because there are few numbers to update.</li><li><strong>Tiny outputs:</strong> a 7B base model is ~14GB, but its LoRA adapter might be 10–50MB. You ship the small file, not a whole new model.</li><li><strong>Swappable:</strong> keep the base model loaded once and hot-swap different adapters — one for legal tone, one for code, one for a customer's brand voice.</li><li><strong>Mergeable:</strong> for deployment you can fold the adapter back into the weights (<code>W + B&times;A</code> becomes a new <code>W</code>) so there is zero extra cost at <a class=\"xref\" href=\"#training-vs-inference\">inference</a> time.</li></ul>\n<h4>Where it fits in the toolkit</h4>\n<p>The famous companion is <a class=\"xref\" href=\"#qlora\">QLoRA</a>, which first squeezes the frozen base model down with <a class=\"xref\" href=\"#quantization\">quantization</a> (storing weights in fewer bits) so even a 70B model fits on a single card while LoRA does the learning on top. Tools like <a class=\"xref\" href=\"#unsloth\">Unsloth</a>, <a class=\"xref\" href=\"#axolotl\">Axolotl</a>, and the <a class=\"xref\" href=\"#trl-library\">TRL library</a> turn a LoRA job into a short config file rather than a research project.</p>\n<p class=\"callout tip\">💡 Two dials matter most: <code>r</code> (rank — capacity of the add-on) and <code>lora_alpha</code> (how strongly the add-on speaks). A common starting point is <code>r=8</code> or <code>16</code> with <code>alpha</code> set to roughly twice <code>r</code>.</p>",
  "takeaways": [
    "Freeze the big model; train two skinny matrices (<code>A</code>, <code>B</code>) instead.",
    "You tune well under 1% of parameters and ship a tiny adapter.",
    "Adapters are swappable and mergeable — great for style, not new facts."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"LoRA freezes the base weight and trains two skinny matrices A and B\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"34\" text-anchor=\"middle\">LoRA: train a low-rank add-on, freeze the rest</text><rect class=\"dg-box\" x=\"50\" y=\"120\" width=\"150\" height=\"90\" rx=\"12\"/><text class=\"dg-label\" x=\"125\" y=\"158\" text-anchor=\"middle\">Frozen W</text><text class=\"dg-sub\" x=\"125\" y=\"180\" text-anchor=\"middle\">1000 x 1000</text><text class=\"dg-note\" x=\"125\" y=\"198\" text-anchor=\"middle\">not trained</text><text class=\"dg-label\" x=\"232\" y=\"172\" text-anchor=\"middle\">+</text><rect class=\"dg-box dg-box-accent\" x=\"258\" y=\"120\" width=\"70\" height=\"90\" rx=\"12\"/><text class=\"dg-label\" x=\"293\" y=\"160\" text-anchor=\"middle\">B</text><text class=\"dg-sub\" x=\"293\" y=\"180\" text-anchor=\"middle\">1000 x 8</text><text class=\"dg-label\" x=\"344\" y=\"172\" text-anchor=\"middle\">x</text><rect class=\"dg-box dg-box-accent\" x=\"360\" y=\"120\" width=\"90\" height=\"90\" rx=\"12\"/><text class=\"dg-label\" x=\"405\" y=\"160\" text-anchor=\"middle\">A</text><text class=\"dg-sub\" x=\"405\" y=\"180\" text-anchor=\"middle\">8 x 1000</text><text class=\"dg-note\" x=\"349\" y=\"234\" text-anchor=\"middle\">only A and B get trained (rank r = 8)</text><line class=\"dg-edge\" x1=\"450\" y1=\"165\" x2=\"540\" y2=\"165\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box dg-box-good\" x=\"540\" y=\"120\" width=\"150\" height=\"90\" rx=\"12\"/><text class=\"dg-label\" x=\"615\" y=\"152\" text-anchor=\"middle\">Adapted model</text><text class=\"dg-sub\" x=\"615\" y=\"174\" text-anchor=\"middle\">W + (B x A)</text><text class=\"dg-note\" x=\"615\" y=\"194\" text-anchor=\"middle\">~10-50MB adapter</text><text class=\"dg-note\" x=\"360\" y=\"300\" text-anchor=\"middle\">One million numbers in W vs only 16,000 trainable in A and B</text></svg>",
    "caption": "LoRA leaves the huge weight W frozen and learns its update as two skinny matrices B and A, so only a tiny fraction of numbers are trained."
  },
  "quiz": [
    {
      "q": "In LoRA, what happens to the original model weights (W) during training?",
      "options": [
        "They are frozen and never updated",
        "Every weight is updated, just more slowly",
        "They are deleted to save memory",
        "They are converted into the A and B matrices"
      ],
      "answer": 0,
      "explain": "LoRA freezes W entirely and only trains the small added matrices A and B. The base model is untouched, which is why an adapter can be removed to get the original model back exactly."
    },
    {
      "q": "If you raise the rank from r=8 to r=64, what is the most likely effect?",
      "options": [
        "The base model shrinks on disk",
        "More trainable parameters, more memory, and higher overfitting risk",
        "Training becomes mathematically impossible",
        "The adapter automatically merges into the base"
      ],
      "answer": 1,
      "explain": "Rank sets how skinny the A and B strips are, so a bigger r adds more trainable numbers and capacity at the cost of memory and a greater chance of memorising a small dataset."
    },
    {
      "q": "A teammate wants the model to know 500 pages of brand-new internal facts. Why is plain LoRA a poor fit?",
      "options": [
        "LoRA only works on vision models",
        "A thin low-rank add-on is best for style and behaviour, not absorbing large new knowledge",
        "LoRA cannot be saved to disk",
        "LoRA always overwrites the base model's facts"
      ],
      "answer": 1,
      "explain": "A small adapter excels at tone, format, and behaviour but can't pour in a whole new field of facts; for that, reach for RAG or continued pretraining instead."
    },
    {
      "q": "You want to serve one fine-tuned style with zero extra cost at inference. What should you do?",
      "options": [
        "Increase lora_dropout to 1.0",
        "Set the rank r to 0",
        "Merge the adapter so W + (B x A) becomes a new W",
        "Load the adapter onto a different base model"
      ],
      "answer": 2,
      "explain": "Merging folds the B x A update back into the weights, producing a standard model with no adapter overhead at inference. Loading onto a different base would instead cause shape mismatches or garbage output."
    }
  ],
  "mentalModel": "LoRA freezes the giant model like a printed textbook and trains a thin stack of sticky-note adapters in the margins. Two skinny matrices stand in for one huge weight update — so you tune about 0.1% of the parameters, save a few-megabyte file, and clip it on or off at will.",
  "mistakes": [
    "<strong>Thinking LoRA changes the base model.</strong> It does not. The base weights are frozen; the learning lives in a separate adapter. Delete the adapter and you are back to the original model exactly.",
    "<strong>Cranking the rank <code>r</code> sky-high \"to be safe.\"</strong> Bigger <code>r</code> means more trainable numbers, more memory, and a bigger chance of overfitting (memorising your small dataset instead of learning the pattern). Start low (<code>8</code> or <code>16</code>) and only raise it if quality is clearly capped.",
    "<strong>Forgetting to scale <code>lora_alpha</code> with <code>r</code>.</strong> The effective strength is roughly <code>alpha/r</code>. Change <code>r</code> but leave <code>alpha</code> fixed and you silently change how loud the adapter speaks, which can wreck a run that 'worked yesterday'.",
    "<strong>Adapting too few layers.</strong> Old guides only touched the attention query/value layers. Adapting more modules (including the MLP/feed-forward layers) usually helps, especially for harder tasks. Check what your tool targets by default.",
    "<strong>Expecting LoRA to teach big new knowledge.</strong> A small adapter is great at <em>style, format, and behaviour</em>, but a thin update cannot pour in a whole new field of facts. For fresh knowledge prefer <a class=\"xref\" href=\"#rag\">RAG</a> or <a class=\"xref\" href=\"#continued-pretraining\">continued pretraining</a>.",
    "<strong>Mismatching the adapter and base at load time.</strong> An adapter is trained against one specific base model and chat template. Load it onto a different base (or the wrong quantization) and you get garbage or an outright shape-mismatch error."
  ],
  "exercise": "<p>Using Hugging Face <a class=\"xref\" href=\"#peft\">PEFT</a> and the <a class=\"xref\" href=\"#trl-library\">TRL library</a>, fine-tune a small base model (say a 1B–3B Llama or Qwen) on a tiny dataset of ~50 examples teaching one narrow behaviour — for instance, always replying in a cheerful, emoji-free customer-support tone. Set <code>r=8</code>, <code>lora_alpha=16</code>, train for 1–3 epochs, then run <code>print_trainable_parameters()</code> and note what percentage of the model you actually trained. Save the adapter and check its file size on disk.</p><p><strong>Stretch goal:</strong> Train a <em>second</em> adapter on the same base for a totally different style (e.g. a terse legal tone). Then load the base once and hot-swap between the two adapters on the same prompt, proving you can keep one heavy base model in memory and switch personalities by clipping on a different few-megabyte lens.</p>",
  "solution": "<p><strong>Hint:</strong> Wrap the base model with <code>get_peft_model(base, LoraConfig(...))</code>, hand it to TRL's <code>SFTTrainer</code>, and train. <code>print_trainable_parameters()</code> should report well under 1% trainable — that is the win. The saved adapter (via <code>model.save_pretrained(\"my-adapter\")</code>) will be tiny, often 10–50MB, because it stores only the <code>A</code> and <code>B</code> matrices, not the billions of frozen weights. For the stretch goal, load the base once, then call <code>model.load_adapter(path)</code> for each style and switch the active one with <code>model.set_adapter(name)</code>; the heavy base never reloads, so swapping is near-instant. To deploy a single style with zero overhead, <code>merge_and_unload()</code> folds <code>B&times;A</code> back into <code>W</code>.</p>",
  "summary": "<p>LoRA freezes the giant base model and trains two skinny matrices (<code>A</code> and <code>B</code>) that stand in for a full weight update, exploiting the fact that task-specific changes are 'low-rank' (simple). You tune a fraction of a percent of the parameters, ship a few-megabyte adapter instead of a whole new model, and can swap, merge, or stack adapters at will. It is the default cheap fine-tuning method and the foundation under QLoRA.</p>",
  "keyTerms": [
    { "term": "LoRA (Low-Rank Adaptation)", "def": "A fine-tuning method that freezes the original model and trains a small add-on made of two skinny matrices, so you tune a tiny fraction of the parameters." },
    { "term": "Weights / weight matrix", "def": "The grids of numbers inside a model that get multiplied against your input as it flows through the network. They hold what the model has learned." },
    { "term": "Rank (r)", "def": "How skinny the two LoRA matrices are. A small rank like 8 keeps the add-on tiny; a larger rank gives it more capacity but more cost and overfitting risk." },
    { "term": "Adapter", "def": "The small saved file (often 10–50MB) containing the trained LoRA matrices. You attach it to the base model to get the new behaviour." },
    { "term": "lora_alpha", "def": "A scaling number that controls how strongly the adapter's update is applied. The effective strength is roughly alpha divided by rank." },
    { "term": "Frozen weights", "def": "Parameters that are not updated during training. In LoRA the entire base model is frozen; only the adapter learns." },
    { "term": "PEFT (Parameter-Efficient Fine-Tuning)", "def": "The family of methods, including LoRA, that train only a small slice of a model's parameters instead of all of them." },
    { "term": "Merging", "def": "Folding a trained adapter's update back into the base weights so the deployed model needs no extra computation at inference time." },
    { "term": "Overfitting", "def": "When a model memorises its small training set instead of learning the general pattern, hurting performance on new inputs." }
  ]
}
