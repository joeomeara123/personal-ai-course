{
  "id": "dense-models",
  "order": 64,
  "section": "Model Types",
  "title": "Dense Models",
  "lesson": "<p>A <strong>dense model</strong> is the plain, original shape of a large language model: every single one of its <a class=\"xref\" href=\"#parameters\">parameters</a> (the learned numbers that make up the model) is used to process every single <a class=\"xref\" href=\"#tokens\">token</a> (the chunk of text the model reads or writes). Nothing is skipped.</p>\n<p>If the model has 8 billion parameters, all 8 billion do work on every word, every time. For years this was the <em>only</em> shape anyone built, which is why \"dense\" wasn't even a word people used — it was just \"a model\". The name only appeared once a different shape, <a class=\"xref\" href=\"#moe-models\">Mixture-of-Experts</a> (MoE), arrived and people needed a way to say \"the normal kind\".</p>\n<h4>The whole orchestra plays every note</h4>\n<p>Picture a full symphony orchestra. In a dense model, every musician plays on every single bar of music — strings, brass, woodwind, percussion, all of them, the whole way through.</p>\n<p>The sound is rich and consistent, but you are paying every player's full salary for the entire concert, even during a quiet passage that only really needs a violin. That is a dense model: maximum participation, maximum predictability, maximum cost. (Contrast that with MoE, where a conductor waves in only the few sections needed for each bar — we'll get there.)</p>\n<h4>A second analogy: the all-hands meeting</h4>\n<p>Imagine a company where, for <em>every</em> decision — big or tiny — the entire staff files into one room and everyone weighs in. You get the full collective brainpower on each question, and the process is dead simple to run: there's only ever one room and one routine.</p>\n<p>But it's slow and pricey, because you are paying and feeding the whole company to answer \"what's for lunch?\" just as you do for \"should we open a new office?\". A dense model treats every token like that all-hands meeting.</p>\n<h4>Why dense models are still everywhere in 2026</h4>\n<p>Despite the buzz around MoE, dense models remain the default for a lot of real products, for three down-to-earth reasons:</p>\n<ul><li><strong>Predictable cost and speed.</strong> Because the work per token never changes, you can forecast <a class=\"xref\" href=\"#latency-vs-quality\">latency</a> (how long a reply takes) and memory use almost exactly. No surprises.</li><li><strong>Easy to run anywhere.</strong> Most <a class=\"xref\" href=\"#slms\">small language models</a> you run on a laptop or phone are dense. Tools like <a class=\"xref\" href=\"#ollama\">Ollama</a> and <a class=\"xref\" href=\"#llama-cpp\">llama.cpp</a> handle dense models with zero fuss.</li><li><strong>Simpler to fine-tune.</strong> Techniques like <a class=\"xref\" href=\"#lora\">LoRA</a> and <a class=\"xref\" href=\"#qlora\">QLoRA</a> were designed and battle-tested on dense models first.</li></ul>\n<h4>The memory and cost reality</h4>\n<p>Because all parameters fire every time, a dense model's running cost scales almost directly with its size. The same rule of thumb from the parameters topic applies cleanly here — <em>params x bytes-per-param</em> tells you the <a class=\"xref\" href=\"#vram-basics\">VRAM</a> (fast GPU memory) you need:</p>\n<pre><code class=\"lang-text\">Dense 8B model, 16-bit:  8,000,000,000 x 2 bytes  = ~16 GB VRAM\nDense 8B model, 4-bit:   8,000,000,000 x 0.5 byte = ~4 GB VRAM\nEvery token: ALL 8B parameters do the math.</code></pre>\n<p>With a dense model there's no asterisk: the \"sticker number\" of parameters is also roughly the number doing work. That honesty is part of why people trust them for budgeting.</p>\n<h4>Dense vs MoE at a glance</h4>\n<table class=\"cmp\"><thead><tr><th>Dense model</th><th>MoE model</th></tr></thead><tbody><tr><td>All parameters active per token</td><td>Only a few \"experts\" active per token</td></tr><tr><td>Cost scales with total size</td><td>Cheaper to run than its total size suggests</td></tr><tr><td>Predictable, simple to serve</td><td>Faster for the quality, but trickier to serve</td></tr><tr><td>Great on laptops/phones</td><td>Usually needs lots of memory for all experts</td></tr></tbody></table>\n<p class=\"callout warn\">⚠️ One trap worth naming early: in an MoE model, only a slice of parameters fire per token, but <em>all</em> of them must still be loaded into memory. A 100B-parameter MoE can be as fast as a 12B dense model yet still demands enough VRAM to hold the full 100B. A dense model has no such gap — what you load is what you use.</p>\n<h4>A quick code check</h4>\n<p>You usually can't tell dense from MoE by the name alone. The model's config file gives it away — MoE configs list a number of experts; dense ones don't:</p>\n<pre><code class=\"lang-python\">from transformers import AutoConfig\ncfg = AutoConfig.from_pretrained(\"meta-llama/Llama-3.1-8B\")\nis_moe = getattr(cfg, \"num_local_experts\", None) is not None\nprint(\"MoE\" if is_moe else \"Dense\")   # Dense\n</code></pre>\n<p>So the takeaway: a dense model is the \"everyone shows up, every time\" design. It costs more per unit of quality than the newer MoE shape, but it is simple, predictable, and friendly to small hardware — which is exactly why it's still the workhorse for <a class=\"xref\" href=\"#on-device-ai\">on-device</a> and budget-sensitive products in 2026.</p>",
  "mentalModel": "A full orchestra where every musician plays on every bar of music. Rich and reliable, but you pay every player for the whole concert. In a dense model, all parameters do work on every token — no shortcuts, no skipping.",
  "takeaways": [
    "Every parameter fires on every token — nothing is skipped.",
    "Cost and memory scale straight with total size; very predictable.",
    "Simple to serve and fine-tune — still the default for on-device models."
  ],
  "diagram": {
    "svg": "<svg class=\"dg\" viewBox=\"0 0 720 340\" role=\"img\" aria-label=\"Dense model versus Mixture-of-Experts: which parameters fire per token\"><defs><marker id=\"arrow\" markerWidth=\"12\" markerHeight=\"12\" refX=\"9\" refY=\"5\" orient=\"auto\"><path class=\"dg-arrowhead\" d=\"M0,0 L11,5 L0,10 z\"/></marker></defs><text class=\"dg-title\" x=\"360\" y=\"34\" text-anchor=\"middle\">One token in — who does the work?</text><text class=\"dg-sub\" x=\"180\" y=\"66\" text-anchor=\"middle\">Dense: ALL fire</text><text class=\"dg-sub\" x=\"540\" y=\"66\" text-anchor=\"middle\">MoE: only a few fire</text><rect class=\"dg-box-accent\" x=\"320\" y=\"148\" width=\"80\" height=\"44\" rx=\"12\"/><text class=\"dg-label\" x=\"360\" y=\"175\" text-anchor=\"middle\">token</text><line class=\"dg-edge\" x1=\"320\" y1=\"170\" x2=\"250\" y2=\"170\" marker-end=\"url(#arrow)\"/><line class=\"dg-edge\" x1=\"400\" y1=\"170\" x2=\"470\" y2=\"170\" marker-end=\"url(#arrow)\"/><rect class=\"dg-box-good\" x=\"60\" y=\"90\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box-good\" x=\"124\" y=\"90\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box-good\" x=\"60\" y=\"150\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box-good\" x=\"124\" y=\"150\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box-good\" x=\"60\" y=\"210\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box-good\" x=\"124\" y=\"210\" width=\"56\" height=\"40\" rx=\"12\"/><text class=\"dg-note\" x=\"120\" y=\"282\" text-anchor=\"middle\">all 6 blocks active</text><rect class=\"dg-box-good\" x=\"540\" y=\"90\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box\" x=\"604\" y=\"90\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box\" x=\"540\" y=\"150\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box\" x=\"604\" y=\"150\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box\" x=\"540\" y=\"210\" width=\"56\" height=\"40\" rx=\"12\"/><rect class=\"dg-box-good\" x=\"604\" y=\"210\" width=\"56\" height=\"40\" rx=\"12\"/><text class=\"dg-note\" x=\"600\" y=\"282\" text-anchor=\"middle\">2 of 6 active, all loaded</text></svg>",
    "caption": "In a dense model every block of parameters activates for each token; in MoE only a chosen few do, though all must stay loaded in memory."
  },
  "mistakes": [
    "<strong>Thinking \"dense\" is a special or fancy feature.</strong> It's the opposite — dense is the plain, original design. The word only exists to distinguish it from the newer <a class=\"xref\" href=\"#moe-models\">Mixture-of-Experts</a> shape.",
    "<strong>Assuming bigger dense always beats smaller dense.</strong> Size is <a class=\"xref\" href=\"#parameters\">capacity</a>, not skill. A well-trained dense 8B in 2026 can beat a poorly trained dense 70B from a few years back. Training quality matters as much as count.",
    "<strong>Comparing a dense model's size to an MoE's <em>total</em> size.</strong> A 100B MoE may only fire ~12B parameters per token, so it can be as fast as a dense 12B. Comparing 100B-dense to 100B-MoE on speed is apples to oranges.",
    "<strong>Forgetting dense cost scales straight with size.</strong> Doubling a dense model's parameters roughly doubles its per-token compute and memory. There's no free lunch like MoE's selective activation — plan your <a class=\"xref\" href=\"#vram-basics\">VRAM</a> accordingly.",
    "<strong>Believing MoE has made dense obsolete.</strong> Dense still wins for <a class=\"xref\" href=\"#on-device-ai\">on-device</a> and laptop use because it's predictable and memory-light per active parameter. Most models running on phones today are dense.",
    "<strong>Mixing up \"dense\" the architecture with \"dense\" embeddings.</strong> A dense <em>model</em> means all parameters fire. A \"dense vector\" in <a class=\"xref\" href=\"#embeddings\">embeddings</a> means a list of numbers with no empty slots. Same word, totally different idea."
  ],
  "exercise": "<p>Open the model cards of two well-known open models on <a class=\"xref\" href=\"#hugging-face\">Hugging Face</a>: a dense one (e.g. a Llama 8B) and an MoE one (e.g. a Mixtral or Qwen MoE). For each, write down (1) the total parameter count and (2) the <em>active</em> parameters per token if stated. Then use <em>params x bytes-per-param</em> to estimate the 4-bit VRAM each needs to <strong>load</strong>, and note which number drives <em>speed</em> (active params) versus which drives <em>memory</em> (total params).</p><p><strong>Stretch goal:</strong> Run the <code>AutoConfig</code> snippet from the lesson on three models of your choice and have it correctly print \"Dense\" or \"MoE\" for each. Then explain in one sentence why a dense 12B and a 100B MoE with 12B active might feel similarly fast yet need wildly different amounts of memory.</p>",
  "solution": "<p><strong>Hint / worked answer:</strong> For the dense model, total and active parameters are the same number, so one figure drives both speed and memory. For the MoE, the <em>active</em> count (small) drives speed while the <em>total</em> count (large) drives memory — all experts must be loaded even though only a few fire. So a dense 12B and a 100B-MoE-with-12B-active feel similarly fast because each does ~12B parameters of math per token, but the MoE needs ~8x more <a class=\"xref\" href=\"#vram-basics\">VRAM</a> because it must hold all 100B in memory. The <code>AutoConfig</code> check works because MoE configs expose a field like <code>num_local_experts</code> while dense configs don't.</p>",
  "summary": "<p>A dense model is the original LLM shape: every parameter works on every token — like an orchestra where all players play every bar. It's predictable, simple to serve, and friendly to small hardware, but its cost scales straight with size. Its counterpart, MoE, fires only a few \"experts\" per token to go faster, at the price of more memory and complexity.</p>",
  "keyTerms": [
    { "term": "Dense model", "def": "A model where every parameter is used to process every token. The plain, original LLM design with no skipping." },
    { "term": "Active parameters", "def": "The parameters that actually do math for a given token. In a dense model this equals the total; in an MoE it's far fewer." },
    { "term": "Mixture-of-Experts (MoE)", "def": "A model split into many sub-networks (\"experts\"), where only a few fire per token — faster than its size suggests, but hungrier for memory." },
    { "term": "Parameter", "def": "A single number the model learned during training. A model is mostly a huge pile of these numbers." },
    { "term": "VRAM", "def": "The fast memory on a GPU where a model's parameters live while it runs. Roughly: params x bytes-per-param." },
    { "term": "Latency", "def": "How long the model takes to produce a reply. With dense models it's very predictable because work per token never changes." },
    { "term": "Precision", "def": "How many bytes each parameter uses (16-bit, 8-bit, 4-bit). Fewer bytes means less VRAM and faster inference." },
    { "term": "Architecture", "def": "The fixed wiring of a model — how its layers connect. \"Dense\" and \"MoE\" are two different architectures, not training choices." }
  ],
  "quiz": [
    {
      "q": "In a dense model, how many parameters are used to process a single token?",
      "options": [
        "Only the parameters in the first layer",
        "All of them — every parameter does work on every token",
        "A small subset chosen by a router",
        "It depends on how long the token is"
      ],
      "answer": 1,
      "explain": "\"Dense\" means full participation: all parameters fire for every token. The subset-by-router behaviour describes MoE, not dense."
    },
    {
      "q": "Roughly how much VRAM does a dense 8B-parameter model need at 4-bit precision just to load?",
      "options": [
        "~2 GB",
        "~4 GB",
        "~16 GB",
        "~32 GB"
      ],
      "answer": 1,
      "explain": "params x bytes-per-param = 8B x 0.5 byte ≈ 4 GB. At 16-bit it would be ~16 GB, so precision matters a lot for memory."
    },
    {
      "q": "Why might a 100B-parameter MoE feel as fast as a dense 12B model yet need far more memory?",
      "options": [
        "MoE only fires ~12B active params per token (speed), but all 100B must be loaded (memory)",
        "MoE compresses its weights to 12B once loaded",
        "Dense models always run slower regardless of size",
        "The MoE quietly deletes unused experts to save memory"
      ],
      "answer": 0,
      "explain": "Speed tracks the active parameters per token (~12B), but every expert still has to sit in VRAM, so memory tracks the full 100B."
    },
    {
      "q": "A teammate says \"MoE has made dense models obsolete.\" What's the most accurate response?",
      "options": [
        "True — nobody ships dense models anymore",
        "True, because dense models can't be quantized",
        "Not quite — dense stays popular for on-device use thanks to predictable cost and low memory per active param",
        "Not quite — dense models are actually always faster than MoE"
      ],
      "answer": 2,
      "explain": "Dense remains the workhorse for laptops and phones because its cost and memory are predictable and there's no need to load extra inactive experts."
    }
  ]
}
