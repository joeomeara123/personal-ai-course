{
  "id": "hugging-face",
  "order": 43,
  "section": "Local AI Ecosystem",
  "title": "Hugging Face",
  "lesson": "<p><strong>Hugging Face</strong> is, in plain terms, the GitHub of AI models. It is two things at once: a giant <em>website</em> where people share models, datasets, and demos, and a family of <em>software libraries</em> that let your Python code download and run those things in a few lines. If you build with open models at all, you will touch Hugging Face within your first hour. The name comes from the original company's chatbot mascot — the slang has stuck even though almost nobody uses that first chatbot today.</p>\n<h4>The two halves: the Hub and the libraries</h4>\n<p>Think of a public library building. The <strong>Hub</strong> (hosted at huggingface.co) is the building and its shelves: roughly two million model files, hundreds of thousands of datasets, and interactive demos called <em>Spaces</em>. Anyone can browse, and most things are free to download. The <strong>libraries</strong> are the librarian who fetches an exact book for you and opens it to the right page. The most important library is <code>transformers</code>; its job is to take any model's name and turn it into a working object in your code without you reading the model's manual.</p>\n<p>A second analogy: app stores. The Hub is the App Store, where developers publish their apps (models). The libraries are the phone's operating system that knows how to install and launch any app you tap. You do not rebuild the phone for each app; you just press install. Hugging Face plays that operating-system role for AI.</p>\n<h4>A model is just a folder</h4>\n<p>This trips up beginners, so let it sink in: a model on the Hub is simply a <em>folder of files</em> sitting at a repository address like <code>meta-llama/Llama-3.1-8B-Instruct</code>. Inside are the learned <a class=\"xref\" href=\"#parameters\">parameters</a> (the model's numbers, usually in <code>.safetensors</code> files), a small <code>config.json</code> describing the shape, and the <a class=\"xref\" href=\"#tokenization\">tokenizer</a> files that turn text into <a class=\"xref\" href=\"#tokens\">tokens</a>. When you ask for a model by name, the libraries clone that folder to a cache on your disk, then load it. Nothing magic — just a smart download manager wired into your code. The same pattern holds for datasets: a dataset is a folder of data files at an address like <code>tatsu-lab/alpaca</code>, fetched by the <code>datasets</code> library the same way. Once you see that <em>everything on the Hub is a versioned folder</em>, the whole platform stops feeling mysterious and starts feeling like Git for big files.</p>\n<h4>Loading a model in three lines</h4>\n<p>Here is the move you will repeat for the rest of your career. A <code>pipeline</code> bundles tokenizer, model, and the glue logic into one callable object:</p>\n<pre><code class=\"lang-python\">from transformers import pipeline\n\nchat = pipeline(\"text-generation\", model=\"Qwen/Qwen2.5-7B-Instruct\")\nout = chat(\"Explain VRAM to a 10-year-old.\", max_new_tokens=120)\nprint(out[0][\"generated_text\"])\n</code></pre>\n<p>That single <code>pipeline</code> call downloads the model folder the first time, caches it under <code>~/.cache/huggingface</code>, and reuses it forever after. For more control you load the pieces yourself:</p>\n<pre><code class=\"lang-python\">from transformers import AutoModelForCausalLM, AutoTokenizer\n\ntok = AutoTokenizer.from_pretrained(\"Qwen/Qwen2.5-7B-Instruct\")\nmodel = AutoModelForCausalLM.from_pretrained(\n    \"Qwen/Qwen2.5-7B-Instruct\", device_map=\"auto\")\n</code></pre>\n<p>The <code>Auto</code> classes are the clever bit: they read <code>config.json</code> and pick the right code for that architecture, so the same two lines work for Llama, Qwen, Mistral, and hundreds more. The <code>device_map=\"auto\"</code> argument tells the <code>accelerate</code> library to place the model's layers wherever they fit — across your GPU, then spilling onto regular memory if the GPU runs out — so a model slightly too big for your card can still load instead of crashing outright.</p>\n<h4>The ecosystem around it</h4>\n<p>Hugging Face is not one library but a toolbox, and most of the open-source AI world is built on top of it:</p>\n<ul><li><strong>transformers</strong> — load and run models.</li><li><strong>datasets</strong> — download and stream training data, even sets too big for your disk.</li><li><strong>tokenizers</strong> — the fast text-to-token engine.</li><li><strong>accelerate</strong> — spread one model across several GPUs without rewriting code.</li><li><strong><a class=\"xref\" href=\"#peft\">PEFT</a></strong> and <strong><a class=\"xref\" href=\"#trl-library\">TRL</a></strong> — the official tools for cheap fine-tuning (<a class=\"xref\" href=\"#lora\">LoRA</a>, <a class=\"xref\" href=\"#dpo\">DPO</a>, and friends).</li></ul>\n<p>Tools you have already met lean on the Hub too: <a class=\"xref\" href=\"#ollama\">Ollama</a> and <a class=\"xref\" href=\"#llama-cpp\">llama.cpp</a> pull <a class=\"xref\" href=\"#gguf-models\">GGUF</a> files from it, and trainers like <a class=\"xref\" href=\"#unsloth\">Unsloth</a> and <a class=\"xref\" href=\"#axolotl\">Axolotl</a> read models straight from Hub names. It is the shared warehouse the whole <a class=\"xref\" href=\"#open-vs-closed-source\">open-source</a> world ships from.</p>\n<p class=\"callout tip\">💡 Use <code>safetensors</code> files when given a choice. The older <code>.bin</code> (pickle) format can hide and run malicious code on load; <code>safetensors</code> is a plain, safe data format that cannot. The Hub flags the format on each model page.</p>\n<p class=\"callout warn\">⚠️ Many strong models (Llama, some Gemma builds) are <em>gated</em>: you must accept a license on the website and use an access token before <code>from_pretrained</code> will download them. A 401 error almost always means \"log in and accept the terms first.\"</p>",
  "mentalModel": "Hugging Face is the App Store plus the phone's operating system for AI: the Hub is the store full of published models, datasets, and demos, and the libraries are the OS that installs and launches any of them with one tap (<code>from_pretrained</code>).",
  "mistakes": [
    "<strong>Confusing the company with the library.</strong> \"Hugging Face\" is the website and the firm; <code>transformers</code> is the main Python library. Saying \"I trained it in Hugging Face\" is vague — name the actual tool (transformers, PEFT, TRL).",
    "<strong>Assuming everything is free to run anywhere.</strong> The <em>download</em> is usually free, but you still need the hardware — a 70B model needs serious <a class=\"xref\" href=\"#vram-basics\">VRAM</a>. The Hub hands you the model; it does not hand you a GPU.",
    "<strong>Ignoring gated-model licenses.</strong> Trying to <code>from_pretrained</code> a gated model (e.g. Llama) without accepting its terms and setting an access token gives a confusing 401/403. Accept the license on the model page first, then log in with <code>huggingface-cli login</code>.",
    "<strong>Trusting <code>.bin</code> files blindly.</strong> The legacy pickle format can execute arbitrary code when loaded. Prefer <code>safetensors</code>, and be cautious downloading random unverified repos — treat them like downloading an .exe from a stranger.",
    "<strong>Forgetting the cache fills your disk.</strong> Every model lands in <code>~/.cache/huggingface</code> and stays. After a week of experiments you can easily have 100+ GB sitting there. Clean it with <code>huggingface-cli delete-cache</code>.",
    "<strong>Pinning nothing and getting surprised.</strong> Model repos can be updated by their authors. For reproducible work, pin a specific commit with the <code>revision=</code> argument so a silent upstream change does not alter your results."
  ],
  "exercise": "<p>Pick a small instruct model on the Hub (e.g. <code>Qwen/Qwen2.5-1.5B-Instruct</code> or a similar sub-3B model). In a Python script or notebook, use <code>pipeline(\"text-generation\", model=...)</code> to load it and generate a reply to one prompt. Then open your <code>~/.cache/huggingface</code> folder and confirm the model files actually appeared on disk. Note how long the first run took versus the second (cached) run.</p><p><strong>Stretch goal:</strong> Load the same model again but with the <code>AutoTokenizer</code> and <code>AutoModelForCausalLM</code> classes instead of <code>pipeline</code>, and pass <code>revision=</code> set to the model's current commit hash (find it under \"Files and versions\" on the Hub page). Confirm you get the same output, proving the pipeline was just a wrapper around those two pieces.</p>",
  "solution": "<p><strong>Hint:</strong> The first run is slow because it is downloading several gigabytes; the second run is fast because <code>from_pretrained</code> reads the cached folder instead of the network. To prove the pipeline is just a wrapper, the manual version needs three steps: tokenize the prompt with <code>tok(prompt, return_tensors=\"pt\")</code>, call <code>model.generate(**inputs, max_new_tokens=120)</code>, then decode with <code>tok.decode(...)</code>. Same model, same numbers in, same numbers out — the <code>pipeline</code> was only hiding that plumbing. Pinning <code>revision=\"&lt;commit-hash&gt;\"</code> guarantees you always pull that exact snapshot of the folder.</p>",
  "summary": "<p>Hugging Face is the open-AI ecosystem's shared warehouse: a <strong>Hub</strong> of models, datasets, and demos, plus <strong>libraries</strong> (<code>transformers</code>, <code>datasets</code>, PEFT, TRL) that download and run them in a few lines. A model is just a folder of files fetched by name and cached on disk. Prefer <code>safetensors</code>, mind gated licenses and disk cache, and pin a revision when you need reproducibility.</p>",
  "keyTerms": [
    { "term": "Hugging Face", "def": "Both a company and the central website/library ecosystem for sharing and running open AI models, datasets, and demos. Often called the GitHub of AI." },
    { "term": "The Hub", "def": "The huggingface.co website that hosts millions of model and dataset repositories plus interactive demos, free to browse and mostly free to download." },
    { "term": "transformers", "def": "Hugging Face's flagship Python library that loads and runs almost any model from the Hub with a uniform interface like from_pretrained()." },
    { "term": "pipeline", "def": "A high-level transformers helper that bundles tokenizer, model, and glue logic into one callable for a task such as text-generation." },
    { "term": "Auto classes", "def": "Helpers like AutoModelForCausalLM and AutoTokenizer that read a model's config.json and automatically pick the correct code for its architecture." },
    { "term": "safetensors", "def": "A safe, plain file format for storing model weights that, unlike the older pickle .bin format, cannot execute hidden code when loaded." },
    { "term": "Gated model", "def": "A model whose download requires accepting a license on the website and using an access token first; otherwise you get a 401/403 error." },
    { "term": "Model cache", "def": "The local folder (default ~/.cache/huggingface) where downloaded models and datasets are stored and reused, so you only fetch each once." },
    { "term": "Spaces", "def": "Hosted interactive demos on the Hub where anyone can try a model in the browser without installing anything." }
  ]
}
