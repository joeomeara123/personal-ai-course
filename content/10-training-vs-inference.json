{
  "id": "training-vs-inference",
  "order": 10,
  "section": "Foundations",
  "title": "Training vs Inference",
  "lesson": "<p>Every AI model lives two very different lives. First it goes to <strong>school</strong>. Then it goes to <strong>work</strong>. School is called <strong>training</strong>: the slow, expensive process where the model learns from huge piles of text. Work is called <strong>inference</strong>: the fast, cheap process where the finished model actually answers your questions. Confusing the two is one of the most common beginner mistakes, so let's pull them apart clearly.</p><h4>The cooking analogy</h4><p>Think of a chef. <strong>Training</strong> is the years she spends in culinary school: tasting, failing, adjusting, building up instincts. It's a one-time investment, it's grueling, and it happens before any customer walks in. <strong>Inference</strong> is a Tuesday lunch service: a customer orders, she cooks the dish, it's gone in twenty minutes. She isn't <em>learning</em> during service. She's <em>using</em> what she already knows. An LLM is the same. By the time you type into a chatbot, the model has already finished school. It is not learning from your message.</p><h4>What actually happens during training</h4><p>During training, the model reads text and tries to predict the next <a class=\"xref\" href=\"#tokens\">token</a> (a token is a chunk of text, often a word or part of a word). When it guesses wrong, a step called <strong>backpropagation</strong> (working backwards through the network to see which knobs caused the error) nudges its millions or billions of <a class=\"xref\" href=\"#parameters\">parameters</a> (the internal numbers, or 'knobs', that hold everything the model knows). Each nudge is tiny. The model sees trillions of tokens and makes the nudge trillions of times. This is why training a frontier model can cost millions of dollars and run for weeks on thousands of <a class=\"xref\" href=\"#gpu-basics\">GPUs</a> (specialised chips built for the heavy maths AI needs).</p><h4>What actually happens during inference</h4><p>Inference is a one-way street. Your prompt goes in, flows <em>forward</em> through the frozen parameters once, and out comes a prediction for the next token. Then it does it again for the next token, and the next. No knobs move. No backpropagation. That's why inference is thousands of times cheaper per request than training, and why it can run on a laptop or phone while training cannot. Here is the whole difference in two lines of pseudo-code:</p><pre><code class=\"lang-python\"># TRAINING: measure error, then adjust the knobs\nloss = compare(model(text), correct_next_token)\nloss.backward()      # backpropagation: find the blame\noptimizer.step()     # nudge every parameter a little\n\n# INFERENCE: just run forward, change nothing\nnext_token = model(prompt)   # no .backward(), no .step()</code></pre><p>That missing <code>.backward()</code> is the entire distinction. Training adjusts; inference only runs.</p><h4>The library analogy</h4><p>Picture a librarian who has memorised an enormous library. <strong>Training</strong> was the lifetime spent reading every book and organising the shelves in her head. <strong>Inference</strong> is you walking up and asking 'where's a good mystery novel?' She answers instantly from memory. Crucially, your question does <em>not</em> add a new book to the library. Many people assume that chatting with ChatGPT 'teaches it' things. It doesn't. Within a single conversation it can <em>remember</em> what you said (that's the <a class=\"xref\" href=\"#context-windows\">context window</a>, its short-term working memory), but once the chat ends, that memory is gone and the model's permanent knowledge is unchanged.</p><h4>Side by side</h4><table class=\"cmp\"><thead><tr><th>Training</th><th>Inference</th></tr></thead><tbody><tr><td>Model <em>learns</em>; parameters change</td><td>Model <em>uses</em>; parameters frozen</td></tr><tr><td>Runs forward AND backward</td><td>Runs forward only</td></tr><tr><td>Done once (or occasionally), takes days–weeks</td><td>Happens on every request, takes milliseconds–seconds</td></tr><tr><td>Hugely expensive, needs many GPUs</td><td>Cheap per call, can run on modest hardware</td></tr><tr><td>Needs labelled or raw text datasets</td><td>Needs only the user's prompt</td></tr></tbody></table><h4>Where this matters in real products</h4><p>This split shapes almost every decision you'll make. Want the model to know your company's private documents? You usually do <em>not</em> retrain it. Instead you feed those documents into the prompt at inference time using <a class=\"xref\" href=\"#rag\">RAG</a> (fetching relevant text and pasting it into the prompt). Retraining is the heavy, rare option. When you <em>do</em> want to genuinely change the model's behaviour, you use <a class=\"xref\" href=\"#fine-tuning-basics\">fine-tuning</a> (a short, cheaper round of extra training on your own examples), often via <a class=\"xref\" href=\"#lora\">LoRA</a> so you only nudge a small slice of the parameters. And your monthly bill is almost entirely inference: training is paid once, but you pay for inference every single time a user hits 'send'.</p><p class=\"callout tip\">💡 Rule of thumb: if you want the model to <em>know more facts right now</em>, reach for the prompt and RAG. If you want it to <em>behave differently in general</em>, reach for fine-tuning. Reach for full training almost never.</p>",
  "mentalModel": "Training is school: slow, expensive, done once, and it rewires the brain. Inference is the day job: fast, cheap, done constantly, and the brain stays exactly as it was. Chatting with a model is hiring it for an hour, not sending it back to school.",
  "mistakes": [
    "<strong>Thinking your chat 'teaches' the model.</strong> During inference the parameters are frozen. Your message lives only in the context window and vanishes when the chat ends; the model's permanent knowledge never changes.",
    "<strong>Confusing the context window with learning.</strong> A model 'remembering' earlier messages in a conversation is short-term memory inside one request, not training. Close the tab and it's forgotten.",
    "<strong>Reaching for fine-tuning to add facts.</strong> If you just need the model to know your docs, use <a class=\"xref\" href=\"#rag\">RAG</a> at inference time. Fine-tuning changes <em>behaviour and style</em>, and is a poor, expensive way to inject fresh facts.",
    "<strong>Budgeting for training but forgetting inference.</strong> Training is a one-time cost; inference is paid on every request forever. At scale, inference usually dominates the bill.",
    "<strong>Assuming inference needs the same giant hardware as training.</strong> Inference runs forward only, so a quantized model can run on a laptop or phone, even though training it needed a GPU cluster.",
    "<strong>Believing a model 'updates itself' over time.</strong> A deployed model is static until a human deliberately retrains or fine-tunes it and ships a new version. It does not drift or improve on its own."
  ],
  "exercise": "<p>Grab any chatbot and run a small experiment to <em>feel</em> the training/inference line.</p><ol><li>Tell it a made-up fact about yourself, e.g. 'My pet dragon is named Kevin.'</li><li>In the <em>same</em> conversation, ask 'What is my dragon's name?' It should answer Kevin (context window at work).</li><li>Open a brand-new chat and ask the same question. It won't know. Write one sentence explaining why, using the words 'inference' and 'parameters'.</li></ol><p>Then list three things you'd want a model to know for a real app of your own, and decide for each: prompt/RAG, fine-tuning, or full training?</p><p><strong>Stretch goal:</strong> Estimate rough monthly cost for a chatbot with 10,000 users sending 20 messages each. Look up a real API's price per million tokens, assume roughly 1,000 tokens per message, and explain why this is an <em>inference</em> cost, not a training cost.</p>",
  "solution": "<p>New-chat answer: the model can't recall Kevin because nothing was learned. During <em>inference</em> the <em>parameters</em> stayed frozen, and the only place 'Kevin' existed was the previous chat's context window, which a fresh conversation doesn't share.</p><p>Routing the three facts: a private knowledge base → RAG; a consistent tone or output format → fine-tuning (often LoRA); a brand-new base capability → full training (almost never worth it for a single app).</p><p>Stretch: 10,000 × 20 × 1,000 ≈ 200 million tokens/month. At, say, roughly $0.50 per million input tokens that's around $100/month, before counting the model's replies. It's an inference cost because you pay it on every message; the model's training was already paid for once by whoever built it.</p>",
  "summary": "<p>Training is the rare, expensive 'school' phase where the model reads mountains of text and its parameters change via backpropagation. Inference is the everyday 'work' phase: a single forward pass that produces an answer while the parameters stay frozen. To add knowledge, prefer prompts and RAG; to change behaviour, fine-tune; full training almost never. And remember your ongoing bill is mostly inference.</p>",
  "keyTerms": [
    { "term": "Training", "def": "The learning phase where a model reads huge amounts of text and adjusts its internal numbers (parameters) to predict text better. Slow, expensive, done occasionally." },
    { "term": "Inference", "def": "The using phase where a finished model takes your prompt and produces an answer. Fast, cheap per call, and the parameters never change." },
    { "term": "Parameters", "def": "The millions or billions of internal numbers, like knobs, that store everything the model has learned. Training tunes them; inference just reads them." },
    { "term": "Backpropagation", "def": "The 'work backwards to assign blame' step in training that figures out how to nudge each parameter to reduce the model's error. It only happens during training." },
    { "term": "Forward pass", "def": "Running data through the model from input to output once, producing a prediction. Inference is forward-only; training also runs a backward pass." },
    { "term": "Frozen parameters", "def": "Parameters that are held fixed and not updated. During inference they are frozen, which is why your chat can't permanently teach the model anything." },
    { "term": "Fine-tuning", "def": "A short, cheaper extra round of training on your own examples to change how a model behaves, without building it from scratch." },
    { "term": "Context window", "def": "The model's short-term working memory within one conversation. It lets a model recall earlier messages during inference, but it is not learning and it resets each new chat." }
  ]
}
