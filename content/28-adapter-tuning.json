{
  "id": "adapter-tuning",
  "order": 28,
  "section": "Fine-Tuning",
  "title": "Adapter Tuning",
  "lesson": "<p><strong>Adapter tuning</strong> is the idea of teaching a frozen model a new skill by bolting on a few small, trainable pieces instead of editing the model itself. \"Frozen\" means the model's billions of <a class=\"xref\" href=\"#parameters\">parameters</a> (the learned numbers that turn input into output) are locked and never changed. You only train the little add-ons. Those add-ons are called <strong>adapters</strong>. This is the family that <a class=\"xref\" href=\"#lora\">LoRA</a> belongs to — but adapters came first, and the family is bigger than LoRA alone.</p>\n<h4>The clip-on lens analogy</h4>\n<p>Think of an expensive camera you do not want to take apart. You want a fisheye shot, then later a macro close-up. You do not rebuild the camera each time. You snap a small <em>clip-on lens</em> over the front, shoot, then swap it for another. The camera (the frozen base model) is untouched and reusable. Each clip-on lens is an adapter: small, cheap to make, easy to carry, and instantly removable. Keep a drawer of lenses — one per task — and clip on whichever you need. This is exactly how teams ship one base model with many adapters in production.</p>\n<h4>The classic adapter: a bottleneck</h4>\n<p>The original technique (Houlsby and colleagues, 2019) inserts a tiny new layer inside each block of the model. It is shaped like an hourglass, so people call it a <strong>bottleneck adapter</strong>. It takes the wide stream of numbers flowing through the model, squeezes it down to a small size (say from 1024 down to 64), applies a quick non-linear step, then expands it back to the original width. Only this little squeeze-and-expand sandwich is trained — usually under 1% of the model's size. A second analogy: it is like a customs checkpoint added to a highway. The road (the model) is unchanged, but every car passing through gets briefly inspected and re-stamped by your small booth before continuing.</p>\n<h4>How adapters differ from LoRA</h4>\n<p>Both freeze the base and train something small, but they attach differently. A classic adapter is an <em>extra layer in series</em> — the data must flow through it, so it adds a little delay at inference (the moment the model actually answers, see <a class=\"xref\" href=\"#training-vs-inference\">Training vs Inference</a>). <a class=\"xref\" href=\"#lora\">LoRA</a> instead learns a side change that sits <em>parallel</em> to an existing weight and can be <strong>merged</strong> back into it, leaving zero extra delay. That is why LoRA became the default. But classic adapters still shine for one job: serving many tasks at once.</p>\n<table class=\"cmp\"><thead><tr><th>Classic adapter</th><th>LoRA adapter</th></tr></thead><tbody><tr><td>New layer inserted in series</td><td>Side change added in parallel</td></tr><tr><td>Adds small latency, cannot fully merge away</td><td>Can merge into base, zero extra latency</td></tr><tr><td>Easy to stack many per model</td><td>One active set at a time (unless merged)</td></tr></tbody></table>\n<h4>Why this matters in real products</h4>\n<p>Imagine a SaaS where each customer wants the model tuned to their tone. Full fine-tuning would mean storing a separate multi-gigabyte model per customer — impossible at scale. With adapters you store <em>one</em> frozen base plus a tiny adapter file per customer (often just a few megabytes). At request time you load the right adapter, like pulling the right SIM card for a phone. Tools like <a class=\"xref\" href=\"#vllm\">vLLM</a> can even hot-swap adapters per request, serving hundreds of customers from a single GPU.</p>\n<p>The economics are the real story. Training touches only a fraction of a percent of the weights, so it needs far less memory and finishes in minutes to hours instead of days. A single mid-range GPU can do work that full fine-tuning would need a small cluster for. And because each adapter is a few megabytes, you can version them like code, A/B test two of them against each other, or roll one back instantly if it misbehaves — none of which is practical when every experiment produces a fresh multi-gigabyte model.</p>\n<h4>Doing it in code</h4>\n<p>The standard toolkit is the <a class=\"xref\" href=\"#peft\">PEFT</a> library (PEFT = Parameter-Efficient Fine-Tuning, the umbrella term for all these tricks). It treats every adapter type the same way:</p>\n<pre><code class=\"lang-python\">from peft import LoraConfig, get_peft_model\nfrom transformers import AutoModelForCausalLM\n\nbase = AutoModelForCausalLM.from_pretrained(\"some-7b-model\")\ncfg = LoraConfig(r=16, target_modules=[\"q_proj\", \"v_proj\"])\nmodel = get_peft_model(base, cfg)   # base frozen, adapter added\nmodel.print_trainable_parameters()  # e.g. ~0.2% of total\n# ...train as usual, then:\nmodel.save_pretrained(\"my-adapter\")  # a few MB, not GB</code></pre>\n<p>Notice you save <code>my-adapter</code>, not the whole model. To use it later you reload the base and attach the adapter on top. You can also keep several adapter folders and switch between them by name, all sharing the one frozen base in memory.</p>\n<p class=\"callout tip\">💡 Rule of thumb: reach for LoRA by default. Reach for classic bottleneck adapters when you need to <em>stack</em> many skills in one model or do research on adapter design.</p>",
  "mentalModel": "One expensive camera body you never open (the frozen base model), plus a drawer of small clip-on lenses (adapters). Snap on the lens you need for this shot, swap it for the next — the body stays the same, and each lens is a tiny file you can share or throw away.",
  "mistakes": [
    "<strong>Thinking adapters change the base model.</strong> They do not. The base is frozen; the adapter is a separate small file. If you lose the matching base, the adapter is useless on its own.",
    "<strong>Assuming \"adapter\" always means LoRA.</strong> LoRA is one popular member of the adapter family. Classic bottleneck adapters, prefix-tuning, and others are also adapters and behave differently (e.g. they add latency).",
    "<strong>Forgetting classic adapters add inference latency.</strong> A bottleneck adapter is an extra layer the data must pass through every time. LoRA can be merged to avoid this; classic adapters generally cannot.",
    "<strong>Mixing adapters trained on different base models or versions.</strong> An adapter is shaped to fit one specific frozen base. Load it onto a different model (or even a different quantization) and you get garbage or an error.",
    "<strong>Setting the bottleneck size or rank too small for a hard task.</strong> A tiny squeeze (like dimension 8) can't capture a big behaviour change, so the model underfits. Scale the adapter up if quality stalls.",
    "<strong>Shipping the merged model when you wanted swappability.</strong> Merging a LoRA into the base gives one fixed model and throws away the ability to hot-swap tasks. Keep adapters separate if you serve many tasks from one base."
  ],
  "exercise": "<p>Using the <a class=\"xref\" href=\"#peft\">PEFT</a> library, attach a LoRA adapter to any small open model (a 1B-class model is fine on a laptop or free cloud GPU). Call <code>print_trainable_parameters()</code> and write down what percentage of the model is actually trainable. Then run one short training loop on a handful of example sentences and <code>save_pretrained()</code> the adapter. Check the saved folder size on disk and compare it to the full base model's size.</p><p><strong>Stretch goal:</strong> Train a <em>second</em> adapter for a different style on the same frozen base, then load both folders and switch between them by name without reloading the base. Confirm the two adapters produce noticeably different outputs from the same prompt.</p>",
  "solution": "<p>Trainable parameters for a sensible LoRA (rank 8-16 on a couple of projection layers) usually land around 0.1-0.5% of the total — that is the whole point. The saved adapter folder will be a few megabytes versus gigabytes for the base, because it only stores the small added weights plus a config file.</p><p>For the stretch goal: in PEFT, <code>model.load_adapter(path, adapter_name=\"style_b\")</code> registers a second adapter, and <code>model.set_adapter(\"style_b\")</code> activates it. The frozen base stays in memory once; only the tiny adapter weights swap. Same base, two personalities, almost no extra memory.</p>",
  "summary": "<p>Adapter tuning teaches a frozen model new skills by training small bolt-on pieces, not the model itself. Classic bottleneck adapters insert tiny squeeze-and-expand layers in series; LoRA adds a parallel, mergeable change and is today's default. The big win is shipping one base model plus many tiny, swappable adapter files — cheap to train, store, and serve, especially across many tasks or customers.</p>",
  "keyTerms": [
    { "term": "Adapter", "def": "A small, trainable add-on attached to a frozen model that teaches it a new skill. Stored as a tiny file, separate from the base." },
    { "term": "Adapter tuning", "def": "The fine-tuning approach where the base model is frozen and only the small adapters are trained." },
    { "term": "Frozen base model", "def": "A pretrained model whose parameters are locked and never updated during training; the adapter does all the learning." },
    { "term": "Bottleneck adapter", "def": "The classic adapter design: an hourglass-shaped layer that squeezes the data down to a small size, transforms it, then expands it back. Inserted in series." },
    { "term": "PEFT", "def": "Parameter-Efficient Fine-Tuning: the umbrella term (and a popular library) for methods that train a tiny fraction of a model, including adapters and LoRA." },
    { "term": "Merging", "def": "Folding an adapter's learned change directly into the base model's weights, producing one fixed model with no extra inference delay (mainly a LoRA feature)." },
    { "term": "Hot-swapping", "def": "Switching which adapter is active at request time while keeping one frozen base loaded in memory, so a single GPU can serve many tasks." },
    { "term": "Latency", "def": "The extra delay added when answering. Classic adapters add a little because data passes through an extra layer; merged LoRA adds none." }
  ]
}
