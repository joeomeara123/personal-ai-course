{
  "id": "batch-inference",
  "order": 35,
  "section": "Inference & Optimization",
  "title": "Batch Inference",
  "lesson": "<p><strong>Batch inference</strong> means handling <em>many</em> requests through a model <em>at the same time</em> instead of one after another. \"Inference\" is just the fancy word for using a trained model to produce an answer (as opposed to <a class=\"xref\" href=\"#training-vs-inference\">training</a> it). A <strong>batch</strong> is simply a group of requests bundled together and pushed through the model in one go. Done right, batching is the single biggest reason a server can answer hundreds of users without melting — and the reason an API can charge you fractions of a penny per request.</p>\n<h4>Why batching wins: the GPU is a giant kitchen</h4>\n<p>Picture a restaurant kitchen built for a hundred chefs. If one customer orders, ninety-nine chefs stand around doing nothing while one cooks. That is a single request on a <a class=\"xref\" href=\"#gpu-basics\">GPU</a> (the chip that runs the model). A GPU is built to do thousands of small calculations in parallel, so feeding it one request at a time wastes almost all of it. Batching is taking ten orders at once so most of the chefs are busy. The clever part: cooking ten meals together barely takes longer than cooking one, because the slow step was getting everyone moving, not the cooking itself.</p>\n<p>The technical reason is that, during generation, the model is usually <strong>memory-bound</strong> — meaning the bottleneck is shovelling the model's <a class=\"xref\" href=\"#parameters\">weights</a> (its learned numbers) from memory into the chip, not the math itself. You pay that cost to load the weights <em>once</em>, then you may as well run many requests through them while they are sitting there. Ten requests for nearly the price of one.</p>\n<h4>Two flavours: offline batching vs online batching</h4>\n<p>The first flavour is <strong>offline batch inference</strong>: you have a big pile of inputs sitting in a file — say 50,000 customer reviews to classify — and no human waiting. You hand the whole pile to the model and collect results when ready. This is the laundromat model: don't wash one sock at a time, fill the drum.</p>\n<p>The second flavour is <strong>online (or dynamic) batching</strong>, used by live servers. Requests arrive one by one from different users at random times. The server waits a tiny moment, gathers whatever requests showed up, and runs them as a batch. The modern trick is <strong>continuous batching</strong> (also called in-flight batching): instead of waiting for every request in a batch to finish, the server slots a new request in the instant an old one is done. Think of a bus that never parks — as one passenger steps off, another steps on, so the bus is always full and always moving. This is what makes servers like <a class=\"xref\" href=\"#vllm\">vLLM</a> dramatically faster than naive batching, and it pairs tightly with the <a class=\"xref\" href=\"#kv-cache\">KV cache</a> (the model's short-term memory of the conversation so far).</p>\n<h4>The trade-off you cannot escape: throughput vs latency</h4>\n<p>Two words run this whole topic. <strong>Throughput</strong> is how many requests (or tokens) you finish per second across everyone. <strong>Latency</strong> is how long one single user waits. Bigger batches lift throughput — more work done overall — but can raise latency, because your request might wait a moment for the batch to fill, then share the GPU with others. It is the supermarket checkout: one big till serving a long queue is efficient for the shop (high throughput) but slower for you personally (high latency) than your own private till. Picking a batch size is choosing where to sit on that see-saw, which connects directly to <a class=\"xref\" href=\"#latency-vs-quality\">latency trade-offs</a> and <a class=\"xref\" href=\"#cost-per-token\">cost-per-token</a>.</p>\n<h4>What offline batching looks like in code</h4>\n<p>With vLLM, offline batching is almost embarrassingly simple — you pass a <em>list</em> of prompts and it figures out the batching for you:</p>\n<pre><code class=\"lang-python\">from vllm import LLM, SamplingParams\n\nllm = LLM(model=\"meta-llama/Llama-3.1-8B-Instruct\")\nparams = SamplingParams(max_tokens=128)\n\nprompts = [f\"Summarise this review: {r}\" for r in reviews]  # e.g. 50,000 items\noutputs = llm.generate(prompts, params)   # one call, batched internally\n\nfor out in outputs:\n    print(out.outputs[0].text)\n</code></pre>\n<p>You did not manage any batch yourself. The engine groups the prompts, packs the GPU, and streams results back. For hosted APIs there is an even lazier path: providers like OpenAI and Anthropic offer a <strong>Batch API</strong> where you upload a file of requests, wait up to roughly 24 hours, and get results back at about <em>half</em> the normal price — because they batch your work into spare GPU time.</p>\n<p class=\"callout tip\">💡 Rule of thumb: if no human is waiting, batch it. Offline batch jobs and Batch APIs are the cheapest tokens you will ever buy.</p>",
  "mentalModel": "A GPU is a kitchen with a hundred chefs. One request leaves ninety-nine idle; batching takes many orders at once so nearly everyone is cooking. Continuous batching is a bus that never parks — as one passenger gets off, another gets on, so it is always full and always moving.",
  "mistakes": [
    "<strong>Looping requests one at a time.</strong> Calling the model in a <code>for</code> loop, one prompt per call, leaves the GPU almost idle. This is the number-one performance mistake — you can be 10–20x slower than batching the same work, for no benefit.",
    "<strong>Confusing throughput with latency.</strong> A bigger batch raises total requests-per-second but can make each individual user wait longer. Optimising for one silently hurts the other; always know which one you actually care about.",
    "<strong>Thinking batch size is free.</strong> Every request in a batch needs its own <a class=\"xref\" href=\"#kv-cache\">KV cache</a> memory. Push the batch too big and you run out of <a class=\"xref\" href=\"#vram-basics\">VRAM</a> (graphics-card memory), which crashes the server or silently forces requests to queue.",
    "<strong>Using static batching for a live API.</strong> Old-style \"wait for the whole batch to finish before starting the next\" wastes the GPU whenever one request is much longer than the others. Use <strong>continuous batching</strong> (vLLM, TGI) for live traffic.",
    "<strong>Batching wildly different request lengths together.</strong> Naive batching pads everything to the longest prompt, so one 4,000-token outlier slows down forty short ones. Group similar lengths, or rely on an engine that handles ragged batches well.",
    "<strong>Paying full price for work that could wait.</strong> Reaching for the real-time API to crunch 100,000 background documents. If there is no human watching, a Batch API or an offline job does the same work for roughly half the cost."
  ],
  "exercise": "<p>Take a list of 200 short prompts (you can reuse the same question 200 times). First, time how long it takes to answer them <em>one at a time</em> in a Python <code>for</code> loop, calling the model once per prompt. Then answer all 200 in a <em>single</em> batched call — for example <code>llm.generate(prompts, params)</code> with vLLM, or the equivalent batched call in your framework. Record total seconds for each approach and compute requests-per-second for both.</p><p><strong>Stretch goal:</strong> Re-run the batched version at batch sizes of 1, 8, 32, and 128 and plot throughput (requests/second) against batch size. Find the point where throughput stops climbing — that flat part is where your GPU is saturated, and pushing the batch higher only adds latency.</p>",
  "solution": "<p><strong>Hint:</strong> The one-at-a-time loop will be dramatically slower — often 10x or worse — because each call leaves the GPU mostly idle while it loads weights to serve a single prompt. The single batched call reuses those loaded weights for all 200 prompts, so total time barely rises with more prompts until the GPU fills up. On the stretch goal you should see throughput rise steeply from batch size 1, then level off into a plateau once the GPU is saturated; beyond that, latency per request keeps growing while requests-per-second stays flat. That plateau is your sweet spot — the largest batch that still fits in <a class=\"xref\" href=\"#vram-basics\">VRAM</a> without queuing.</p>",
  "summary": "<p>Batch inference runs many requests through a model together so the GPU stays busy instead of idle, because loading the model's weights — not the math — is the slow part. Offline batching crunches a big pile of inputs with no one waiting; online continuous batching keeps a live server full by slotting new requests in as old ones finish. The core trade-off is throughput (total work) versus latency (one user's wait): bigger batches help the first and can hurt the second. If nobody is waiting, batch it — it is the cheapest way to run a model.</p>",
  "keyTerms": [
    { "term": "Batch inference", "def": "Running many requests through a model at the same time instead of one after another, to use the GPU efficiently." },
    { "term": "Batch / batch size", "def": "A group of requests processed together in one pass. Batch size is how many are in that group at once." },
    { "term": "Throughput", "def": "How much work the system finishes per second across all users — usually counted in requests or tokens per second." },
    { "term": "Latency", "def": "How long a single request takes from sent to answered. The wait one individual user experiences." },
    { "term": "Continuous (in-flight) batching", "def": "Adding new requests to a running batch the moment an old one finishes, so the GPU never sits half-empty. Used by vLLM and similar servers." },
    { "term": "Memory-bound", "def": "When the bottleneck is moving the model's weights from memory into the chip, not the math. This is why batching is nearly free extra work." },
    { "term": "Offline batch inference", "def": "Processing a large pile of inputs all at once with no human waiting on the results — e.g. classifying a million documents overnight." },
    { "term": "Batch API", "def": "A hosted service (OpenAI, Anthropic, others) where you upload a file of requests and get results back within hours at roughly half the normal price." },
    { "term": "Padding", "def": "Filling shorter prompts with filler tokens so every request in a naive batch is the same length — wasteful if lengths vary a lot." }
  ]
}
